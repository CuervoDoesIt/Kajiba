---
phase: 14-live-experiment-capture
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - src/kajiba/collector.py
  - tests/test_collector.py
autonomous: true
requirements: [ECAP-01]
must_haves:
  truths:
    - "on_session_end branches to the experiment finalize BEFORE the contribution_mode read and returns cleanly when self._experiment_mode is true"
    - "An opted-in session emits exactly ONE exp_*.json in EXPERIMENTS_DIR despite turn-scoped on_session_end firing N times (finalize-once, Design B)"
    - "Experiment mode never writes STAGING_DIR/OUTBOX_DIR even under continuous contribution_mode (D-08)"
    - "Flag-absent capture is byte-for-byte the current coding behavior and never touches EXPERIMENTS_DIR (no-regression)"
    - "A zero-turn / interrupted on_session_end writes no malformed record (Pitfall 3)"
  artifacts:
    - path: "src/kajiba/collector.py"
      provides: "_finalize_experiment(session_id) self-cleaning finalize-once + the on_session_end experiment branch + _last_experiment_path tracking"
      contains: "def _finalize_experiment"
  key_links:
    - from: "src/kajiba/collector.py on_session_end"
      to: "src/kajiba/collector.py _finalize_experiment"
      via: "`if self._experiment_mode: ...; return` guard at the TOP of the try block (before contribution_mode read)"
      pattern: "if self\\._experiment_mode:"
    - from: "src/kajiba/collector.py _finalize_experiment"
      to: "src/kajiba/experiment_store.py update_experiment + EXPERIMENTS_DIR"
      via: "self-cleaning unlink of prior path + overwrite-safe write"
      pattern: "update_experiment\\("
---

<objective>
Close the load-bearing correctness item: add `_finalize_experiment(session_id)` (RESEARCH Pattern 2
Design B — session-stable identity + self-cleaning overwrite-latest) and branch `on_session_end` to
it BEFORE the `contribution_mode` read, returning cleanly. This makes an opted-in session emit
exactly ONE content-addressed `exp_*.json` despite the turn-scoped `on_session_end` firing N times,
while leaving the coding path byte-for-byte unchanged when the flag is absent (D-07/D-08, Pitfalls 1–3).

Purpose: This is the bridge proof — joining the v1.1 plugin/turn-buffer to the v1.2 experiment store
(ECAP-01, SC#1). The finalize-once-for-a-content-addressed-record problem is the one genuinely new
piece of logic in this phase; everything else is reuse.

Output: A self-cleaning `_finalize_experiment` that writes via `update_experiment` to
`EXPERIMENTS_DIR`, the `on_session_end` divergent-tail guard, and all six ECAP-01 tests GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-live-experiment-capture/14-RESEARCH.md
@.planning/phases/14-live-experiment-capture/14-PATTERNS.md
@.planning/phases/14-live-experiment-capture/14-VALIDATION.md
@.planning/phases/14-live-experiment-capture/14-02-SUMMARY.md
</context>

## Artifacts this phase produces (full phase scope; this plan's subset marked ▸)

- Test class `TestExperimentCapture` (tests/test_collector.py) — plan 01
- Method `_build_trajectory()` (collector.py) — plan 01
- Method `_build_experiment_record(session_id)` (collector.py) — plan 02
- Attrs `_experiment_mode` / `_experiment_type` / `_task_category` (collector.py) — plan 02
- Env vars `KAJIBA_EXPERIMENT` / `KAJIBA_EXPERIMENT_TYPE` / `KAJIBA_EXPERIMENT_CATEGORY` — plan 02
- ▸ Method `_finalize_experiment(session_id)` (collector.py)
- ▸ `on_session_end` experiment branch (collector.py)
- ▸ Attr `_last_experiment_path` tracking/use (declared in plan 02, consumed here)

<tasks>

<task type="auto">
  <name>Task 1: Add _finalize_experiment (Design B finalize-once) + branch on_session_end</name>
  <files>src/kajiba/collector.py</files>
  <read_first>
    - src/kajiba/collector.py:603-642 (current on_session_end: session-id mismatch warning, the `contribution_mode = _load_config_value(...)` read at 629, the `if self._finalized` continuous guard at 640-642)
    - src/kajiba/collector.py:631-636 (the ad-hoc fixed-filename overwrite trick — the coding-path finalize-once analog)
    - src/kajiba/collector.py:775-791 (`_save_to_staging` — confirms coding path writes STAGING_DIR; the experiment branch must NOT reach here)
    - src/kajiba/experiment_store.py:133-223 (`update_experiment` — overwrite-safe, D-13 structural guard, atomic os.replace, re-validate, ID recompute; INTENTIONALLY no skip-on-exists)
    - .planning/phases/14-live-experiment-capture/14-PATTERNS.md:27-79 (the branch insertion point + Design B finalize-once block to copy)
    - .planning/phases/14-live-experiment-capture/14-RESEARCH.md:163-195, 316-332 (Pattern 2 Design B + the on_session_end branch example)
  </read_first>
  <action>
    Add `_finalize_experiment(self, session_id: str) -> None`. Implement RESEARCH Pattern 2 Design B
    (self-cleaning overwrite-latest): (1) `if not self._conversations: return` — Pitfall 3, write
    nothing on a zero-turn / interrupted end; (2) `rec = self._build_experiment_record(session_id)`;
    (3) compute the destination `new_path = EXPERIMENTS_DIR / f"exp_{rec.record_id}.json"`; (4) if
    `self._last_experiment_path` is set and differs from `new_path`, call
    `self._last_experiment_path.unlink(missing_ok=True)` to drop the stale prior-turn file (the
    content ID moves because `local_model_output` = last gpt turn, D-03, changes each turn); (5)
    write via `update_experiment(rec, EXPERIMENTS_DIR)` (overwrite-safe — never `log_experiment`,
    whose skip-on-exists leaves orphan files as the ID moves); (6)
    `self._last_experiment_path = new_path`. Then in `on_session_end`, insert the divergent-tail
    guard as the FIRST thing inside the try block after the session-id mismatch warning and BEFORE
    the `contribution_mode` read at line 629: `if self._experiment_mode: self._finalize_experiment(
    session_id); return`. The branch MUST `return` unconditionally so the coding path below
    (contribution_mode / continuous auto-submit / `_save_to_staging`) is never reached in experiment
    mode (D-08). Do NOT modify any line of the existing coding path below the guard (Pitfall 2 —
    byte-for-byte). Keep the whole thing inside the existing outer `try/except Exception` →
    `logger.exception("Error in on_session_end")` (fault-tolerant; never propagate to Hermes). Add a
    Google-style docstring on `_finalize_experiment` citing D-07/D-08 + the finalize-once rationale.
  </action>
  <verify>
    <automated>python -m pytest tests/test_collector.py::TestExperimentCapture::test_opted_in_session_writes_one_record tests/test_collector.py::TestExperimentCapture::test_zero_turn_session_writes_nothing -x</automated>
  </verify>
  <acceptance_criteria>
    - `collector.py` contains `def _finalize_experiment(self, session_id: str)`.
    - `_finalize_experiment` begins with a `if not self._conversations: return` guard (Pitfall 3).
    - `_finalize_experiment` unlinks `self._last_experiment_path` when it differs from the new path, then writes via `update_experiment(...)` (grep: `update_experiment(` present; `log_experiment(` NOT used in the finalize).
    - `on_session_end` contains `if self._experiment_mode:` with an unconditional `return`, positioned BEFORE the `contribution_mode = _load_config_value(...)` line.
    - The coding-path lines below the guard are unchanged (no diff to the contribution_mode/continuous/_save_to_staging block).
    - `pytest tests/test_collector.py::TestExperimentCapture::test_opted_in_session_writes_one_record -x` exits 0 (exactly one exp_*.json across N turn-scoped ends).
    - `pytest tests/test_collector.py::TestExperimentCapture::test_zero_turn_session_writes_nothing -x` exits 0.
  </acceptance_criteria>
  <done>An opted-in session finalizes to exactly one content-addressed exp_*.json via the self-cleaning Design B path; zero-turn ends write nothing; the experiment branch returns before any coding-path code runs.</done>
</task>

<task type="auto">
  <name>Task 2: Prove no-regression + privacy isolation; full suite green</name>
  <files>tests/test_collector.py</files>
  <read_first>
    - tests/test_collector.py (the TestExperimentCapture scaffold from plan 01 — finalize the assertions in `test_flag_absent_unchanged_coding_path` and `test_no_staging_or_outbox_in_experiment_mode`)
    - src/kajiba/collector.py on_session_end (now branched) + _save_to_staging (STAGING_DIR target)
    - .planning/phases/14-live-experiment-capture/14-VALIDATION.md (the exact pytest commands for the no-regression + D-08 rows; full-suite sampling at wave merge)
    - .planning/phases/14-live-experiment-capture/14-RESEARCH.md:270-275 (Pitfall 2 no-regression: flag-absent writes STAGING_DIR, never EXPERIMENTS_DIR)
  </read_first>
  <action>
    Finalize the two privacy/regression tests. `test_flag_absent_unchanged_coding_path`: with
    KAJIBA_EXPERIMENT UNSET, drive a full lifecycle and assert a `session_*.json` lands in STAGING_DIR
    exactly as today AND that EXPERIMENTS_DIR is empty (no exp_*.json) — proving the coding path is
    byte-for-byte unchanged. `test_no_staging_or_outbox_in_experiment_mode`: with KAJIBA_EXPERIMENT=1
    AND `contribution_mode` forced to `"continuous"` (monkeypatch the config read /
    `_load_config_value` so the continuous branch WOULD run for a coding session), drive an opted-in
    session and assert STAGING_DIR and OUTBOX_DIR remain empty while exactly one exp_*.json exists in
    EXPERIMENTS_DIR (D-08: the experiment branch returns before the contribution_mode read). Do not
    weaken assertions. Then run the full suite to confirm no regression in the experiment store /
    exclusion guards.
  </action>
  <verify>
    <automated>python -m pytest tests/test_collector.py::TestExperimentCapture -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_collector.py::TestExperimentCapture::test_flag_absent_unchanged_coding_path -x` exits 0 (session_*.json in STAGING_DIR; EXPERIMENTS_DIR empty).
    - `pytest tests/test_collector.py::TestExperimentCapture::test_no_staging_or_outbox_in_experiment_mode -x` exits 0 (continuous mode forced; STAGING_DIR + OUTBOX_DIR empty; one exp_*.json present).
    - `python -m pytest tests/test_collector.py::TestExperimentCapture -x -q` reports 6 passed (all ECAP-01 behaviors GREEN).
    - `python -m pytest -q` exits 0 — full suite green, including `test_experiment_store.py`, `test_experiment_exclusion.py`, `test_schema_experiment.py` (no regression in store/guards).
  </acceptance_criteria>
  <done>All six ECAP-01 tests pass; the full suite is green; experiment mode is proven to never reach the publish path even under continuous mode, and the coding path is unchanged when the flag is absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hermes host → on_session_end hook | Turn-scoped events (N firings + CLI exit) drive finalize; a hook exception must never reach the host. |
| Collector finalize → filesystem | The ONLY write target in experiment mode is EXPERIMENTS_DIR; the publish path (STAGING_DIR/OUTBOX_DIR) must be unreachable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-priv | Information Disclosure (HIGH) | on_session_end experiment branch → must bypass STAGING_DIR/OUTBOX_DIR/continuous auto-submit | mitigate | D-08: the `if self._experiment_mode: ...; return` guard is placed BEFORE the `contribution_mode` read, so experiment mode never reaches `_save_to_staging`/auto-submit. Backstopped by the `experiment_store` D-13 structural guard (refuses writes outside EXPERIMENTS_DIR) + Phase 11 `record_kind=="model_experiment"` publish-refusal. Proven by `test_no_staging_or_outbox_in_experiment_mode` (forces continuous mode) — BLOCKING: this test must be GREEN before phase verification. |
| T-14-dos | Denial of Service (host) | `_finalize_experiment` (unlink + write) | mitigate | Runs inside the existing outer `try/except Exception` → `logger.exception("Error in on_session_end")`; `unlink(missing_ok=True)` cannot raise on a missing file; never propagates to Hermes. |
| T-14-orphan | Integrity (data) | content-addressed filename moves as `local_model_output` accumulates | mitigate | Design B self-cleaning unlink of `self._last_experiment_path` + overwrite-safe `update_experiment` → exactly one file; proven by `test_opted_in_session_writes_one_record`. |
| T-14-pii | Information Disclosure (LOW/accepted) | raw turns persisted to EXPERIMENTS_DIR at finalize | accept | D-09: store raw, scrub later via `kajiba experiment scrub` CLI (never in a hook). Local/private store, no network — acceptable at rest. |
</threat_model>

<verification>
- `python -m pytest tests/test_collector.py::TestExperimentCapture -x -q` → 6 passed.
- `python -m pytest -q` → full suite green (store/exclusion/schema-experiment tests included).
- Manual SC#1 live proof (out-of-band, per 14-VALIDATION.md Manual-Only): a multi-turn-then-exit
  Hermes v0.15.x session with `KAJIBA_EXPERIMENT=1` yields exactly one `exp_*.json` in EXPERIMENTS_DIR
  and nothing in STAGING_DIR/OUTBOX_DIR.
</verification>

<success_criteria>
- Exactly one `exp_*.json` per opted-in session (finalize-once, Design B) — `test_opted_in_session_writes_one_record` green.
- Experiment mode never writes STAGING_DIR/OUTBOX_DIR even under continuous mode (D-08) — `test_no_staging_or_outbox_in_experiment_mode` green.
- Flag-absent capture is byte-for-byte the current coding behavior and never touches EXPERIMENTS_DIR — `test_flag_absent_unchanged_coding_path` green.
- Zero-turn / interrupted end writes nothing (Pitfall 3) — `test_zero_turn_session_writes_nothing` green.
- Full suite green; ECAP-01 satisfied; SC#1 + SC#2 made TRUE.
</success_criteria>

<output>
Create `.planning/phases/14-live-experiment-capture/14-03-SUMMARY.md` when done
</output>
