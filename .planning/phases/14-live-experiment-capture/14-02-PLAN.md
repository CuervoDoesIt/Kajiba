---
phase: 14-live-experiment-capture
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/kajiba/collector.py
  - tests/test_collector.py
autonomous: true
requirements: [ECAP-01]
must_haves:
  truths:
    - "D-01: on_session_start reads KAJIBA_EXPERIMENT/KAJIBA_EXPERIMENT_TYPE/KAJIBA_EXPERIMENT_CATEGORY at call time and sets the mode flag (env-var opt-in trigger via the four confirmed v0.15.x hooks; flag absent => exactly today's coding-session capture, unchanged)"
    - "An opted-in session builds an ExperimentRecord whose fields map per D-03..D-06 (first user turn, last gpt turn, eval_score=0.0, populated trajectory, reused model metadata)"
    - "A live-captured record has the same metadata/outcome structure as a deliberate `kajiba experiment log` record (SC#2)"
    - "Captured runtime metadata (self._model_metadata) lands in experiment.local_model and RecordBase.model"
    - "collector.py imports the experiment_store MODULE (not a bound EXPERIMENTS_DIR name) so the store dir is referenced as experiment_store.EXPERIMENTS_DIR at call time"
  artifacts:
    - path: "src/kajiba/collector.py"
      provides: "experiment-mode env reads + state attrs + _build_experiment_record(session_id) record assembly + experiment_store module import"
      contains: "def _build_experiment_record"
  key_links:
    - from: "src/kajiba/collector.py on_session_start"
      to: "os.environ KAJIBA_EXPERIMENT*"
      via: "call-time env read setting self._experiment_mode/_experiment_type/_task_category"
      pattern: "os.environ.get\\(\"KAJIBA_EXPERIMENT\""
    - from: "src/kajiba/collector.py"
      to: "src/kajiba/experiment_store.py (module)"
      via: "module import so EXPERIMENTS_DIR is referenced as a live module attribute (monkeypatch-reachable), never bound at import time"
      pattern: "from kajiba import experiment_store"
    - from: "src/kajiba/collector.py _build_experiment_record"
      to: "src/kajiba/experiment_store.py build_experiment_record"
      via: "convenience constructor + **extra (model/hardware/trajectory)"
      pattern: "build_experiment_record\\("
    - from: "src/kajiba/collector.py _build_experiment_record"
      to: "ExperimentRecord.experiment.local_model"
      via: "post-construct rich-metadata promotion before write"
      pattern: "rec\\.experiment\\.local_model"
---

<objective>
Wire the experiment-mode trigger and record assembly into `KajibaCollector`: read the
`KAJIBA_EXPERIMENT*` env vars at `on_session_start`, store the mode/type/category state, and add
`_build_experiment_record(session_id)` that maps buffered turns to an `ExperimentRecord` via the
existing `build_experiment_record` write-path constructor (D-03..D-06, Pattern 3/4).

Purpose: Establish the divergent-tail inputs (mode flag + field mapping) that the finalize-once
branch (plan 03) will persist. Routing through `build_experiment_record` gives SC#2 structural
parity by construction.

Output: Experiment-mode state attrs + env reads in `on_session_start`, and a
`_build_experiment_record()` helper that returns a fully-mapped `ExperimentRecord` (NOT yet written
to disk — persistence is plan 03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-live-experiment-capture/14-RESEARCH.md
@.planning/phases/14-live-experiment-capture/14-PATTERNS.md
@.planning/phases/14-live-experiment-capture/14-01-SUMMARY.md
</context>

## Artifacts this phase produces (full phase scope; this plan's subset marked ▸)

- Test class `TestExperimentCapture` (tests/test_collector.py) — plan 01
- Method `_build_trajectory()` (collector.py) — plan 01
- ▸ Method `_build_experiment_record(session_id)` (collector.py)
- ▸ Attrs `_experiment_mode` / `_experiment_type` / `_task_category` (collector.py)
- ▸ Env vars `KAJIBA_EXPERIMENT` / `KAJIBA_EXPERIMENT_TYPE` / `KAJIBA_EXPERIMENT_CATEGORY`
- ▸ `from kajiba import experiment_store` module import + `import os` (collector.py)
- Method `_finalize_experiment(session_id)` + attr `_last_experiment_path` (collector.py) — plan 03

<tasks>

<task type="auto">
  <name>Task 1: Add experiment-mode state attrs + module imports + call-time env reads in on_session_start</name>
  <files>src/kajiba/collector.py</files>
  <read_first>
    - src/kajiba/collector.py:8-39 (the import block — confirms there is NO `import os` and NO `EXPERIMENTS_DIR`/experiment_store import today; STAGING_DIR/OUTBOX_DIR are defined at 44-45)
    - src/kajiba/collector.py:325-346 (__init__ attribute declarations, incl. `self._finalized` at 346 — the template for new bool/str/Optional attrs)
    - src/kajiba/collector.py:393-405 (on_session_start per-session reset block + `self._created_at` set at 402 + `_build_metadata_and_hardware` call at 403-405)
    - src/kajiba/plugin/hooks.py:36 (`KAJIBA_DEBUG == "1"` idiom to copy — but read at CALL time here, not import time)
    - src/kajiba/experiment_store.py:51 (module attribute `EXPERIMENTS_DIR`), 95-100 + 182-188 (the D-13 guard resolves `expected_base` from the LIVE `EXPERIMENTS_DIR` module attribute at call time — so the collector must reference it as `experiment_store.EXPERIMENTS_DIR`, never bind it)
    - .planning/phases/14-live-experiment-capture/14-PATTERNS.md:181-222 (instance-attr + env-read patterns)
    - src/kajiba/schema.py — `EXPERIMENT_TYPES` tuple (for runtime validation of the type env var)
  </read_first>
  <action>
    Add module imports at the top of collector.py: `import os` (for the env reads) and
    `from kajiba import experiment_store` (import the MODULE — do NOT do
    `from kajiba.experiment_store import EXPERIMENTS_DIR`, which would bind a stale name that a test's
    `monkeypatch.setattr(experiment_store, "EXPERIMENTS_DIR", tmp)` could never reach; the store dir
    MUST be referenced as the live module attribute `experiment_store.EXPERIMENTS_DIR` so the call-time
    D-13 guard resolves to the same value the collector passes as `store_dir`). Also ensure
    `EXPERIMENT_TYPES` is importable from `kajiba.schema`. In `__init__`, declare four new attrs
    mirroring the `self._finalized` declaration style: `self._experiment_mode: bool = False`,
    `self._experiment_type: str = "model_evaluation"`, `self._task_category: str = "coding"`,
    `self._last_experiment_path: Optional[Path] = None` (the last is consumed by plan 03; declare it
    here so __init__ is complete). In `on_session_start`, inside the existing try block alongside the
    resets at 393-402, read the env at CALL TIME (NOT import time — per-session semantics, intentional
    deviation from hooks.py:36): `self._experiment_mode = os.environ.get("KAJIBA_EXPERIMENT") == "1"`;
    read `KAJIBA_EXPERIMENT_TYPE` (default `"model_evaluation"`) and validate membership in
    `EXPERIMENT_TYPES`, falling back to `"model_evaluation"` if invalid; read
    `KAJIBA_EXPERIMENT_CATEGORY` (default `"coding"`, free-form str). Also reset
    `self._last_experiment_path = None` here. Use `Optional[Path]` (not `Path | None`) and double
    quotes per CLAUDE.md. Do NOT alter any coding-path reset line.
  </action>
  <verify>
    <automated>python -m pytest tests/test_collector.py -x -q -k "lifecycle or ExperimentCapture"</automated>
  </verify>
  <acceptance_criteria>
    - `collector.py` imports the experiment_store MODULE (grep: `from kajiba import experiment_store` present) and `import os` is present; there is NO `from kajiba.experiment_store import EXPERIMENTS_DIR` bound-name import.
    - `collector.py` contains `self._experiment_mode: bool = False`, `self._experiment_type: str`, `self._task_category: str`, and `self._last_experiment_path: Optional[Path] = None` in `__init__`.
    - `on_session_start` reads `os.environ.get("KAJIBA_EXPERIMENT")`, `KAJIBA_EXPERIMENT_TYPE`, and `KAJIBA_EXPERIMENT_CATEGORY` at call time (grep: `os.environ.get("KAJIBA_EXPERIMENT"` present inside the method).
    - The type env value is validated against `EXPERIMENT_TYPES` and falls back to `"model_evaluation"` when not a member.
    - `python -m pytest tests/test_collector.py -x -q -k "lifecycle"` exits 0 (coding-path resets unchanged).
  </acceptance_criteria>
  <done>Experiment-mode state is initialized in __init__ and populated per-session from the KAJIBA_EXPERIMENT* env vars at on_session_start; the experiment_store module + os are imported; coding-path resets untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add _build_experiment_record(session_id) mapping buffered turns → ExperimentRecord</name>
  <files>src/kajiba/collector.py, tests/test_collector.py</files>
  <read_first>
    - src/kajiba/collector.py:735-773 (`_build_record` — the buffered-turns→record analog; reuse `_build_trajectory()` from plan 01)
    - src/kajiba/experiment_store.py:231-284 (`build_experiment_record` signature + `**extra` forwarding to RecordBase.model/hardware/trajectory; scalar `local_model=ModelMetadata(model_name=...)`)
    - src/kajiba/schema.py:443-467 (ExperimentRecord.trajectory Optional; compute_record_id hashes experiment_id/task_description/local_model_name/local_model_output/started_at)
    - .planning/phases/14-live-experiment-capture/14-PATTERNS.md:83-147 (the _build_experiment_record assembly + Pattern 3 rich-metadata promotion + Pitfall 4 mutate-before-write)
    - .planning/phases/14-live-experiment-capture/14-RESEARCH.md:197-223, 289-314 (Pattern 3 + Code Examples)
  </read_first>
  <action>
    Add `_build_experiment_record(self, session_id: str) -> ExperimentRecord`. Derive defensively
    (Pitfall 3 — search, never index): `first_user = next((t.value for t in self._conversations if
    t.from_ == "human"), "")`; `last_gpt = next((t.value for t in reversed(self._conversations) if
    t.from_ == "gpt"), "")`. Call `build_experiment_record(experiment_id=f"live_{session_id}"`
    (session-stable identity), `experiment_type=self._experiment_type`,
    `task_category=self._task_category`, `task_description=first_user`,
    `local_model_name=(self._model_metadata.model_name if self._model_metadata else "unknown")`,
    `local_model_output=last_gpt`, `eval_score=0.0` (D-05 documented placeholder — captured-but-
    unscored, NOT "scored zero"), `started_at=self._created_at` (stable identity input set once at
    start), `model=self._model_metadata`, `hardware=self._hardware`,
    `trajectory=self._build_trajectory())`. After construction, promote rich metadata for analysis
    (Pattern 3): `if self._model_metadata: rec.experiment.local_model = self._model_metadata`
    (mutate BEFORE any write — Pitfall 4; the write path re-validates and recomputes IDs). Add a
    Google-style docstring citing D-03/D-04/D-05/D-06. Import `ExperimentRecord` /
    `build_experiment_record` from their existing modules (`build_experiment_record` is reachable via
    the `experiment_store` module import added in Task 1, e.g. `experiment_store.build_experiment_record`
    or a `from kajiba.experiment_store import build_experiment_record` name — either is fine, but the
    STORE DIR itself must still be referenced as `experiment_store.EXPERIMENTS_DIR`); do NOT add a new
    schema or store module. Keep `reviewer_model`/`reviewer_critique` unset (D-10). In
    tests/test_collector.py, make `test_field_mapping` and `test_structural_parity_with_deliberate_log`
    exercise this helper (drive a multi-turn session, call the assembly, assert the D-03..D-06 mapping
    and parity); these two should go GREEN now (the finalize-once + no-staging tests stay RED until
    plan 03).
  </action>
  <verify>
    <automated>python -m pytest tests/test_collector.py::TestExperimentCapture::test_field_mapping tests/test_collector.py::TestExperimentCapture::test_structural_parity_with_deliberate_log -x</automated>
  </verify>
  <acceptance_criteria>
    - `collector.py` contains `def _build_experiment_record(self, session_id: str)` returning an `ExperimentRecord`.
    - The record uses `experiment_id=f"live_{session_id}"`, `eval_score=0.0`, `started_at=self._created_at`, and passes `model`/`hardware`/`trajectory` via `build_experiment_record(**extra)`.
    - `task_description` resolves to the first `human` turn and `local_model_output` to the last `gpt` turn via `next(...)` (defensive — no `[0]`/`[-1]` indexing).
    - `rec.experiment.local_model` is set to `self._model_metadata` before any write when metadata is present.
    - `pytest tests/test_collector.py::TestExperimentCapture::test_field_mapping -x` exits 0.
    - `pytest tests/test_collector.py::TestExperimentCapture::test_structural_parity_with_deliberate_log -x` exits 0.
    - No new model class and no new persistence module added (grep confirms only `kajiba.schema` / `kajiba.experiment_store` imports).
  </acceptance_criteria>
  <done>An opted-in session can be assembled into a structurally-parity-correct ExperimentRecord with D-03..D-06 field mapping and reused runtime metadata; the field-mapping and parity tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Env (`KAJIBA_EXPERIMENT*`) → collector | Untrusted env values drive mode/type/category; the type value is validated against `EXPERIMENT_TYPES`. |
| Buffered turns → ExperimentRecord | Raw captured turns flow into the record; validated by Pydantic on construction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-priv | Information Disclosure (HIGH) | _build_experiment_record output destined for EXPERIMENTS_DIR | mitigate | This plan only ASSEMBLES the record; it writes nothing. The structural privacy guard (never staging/outbox) is enforced by the plan-03 finalize branch (D-08) + the `experiment_store` D-13 guard. `test_no_staging_or_outbox_in_experiment_mode` remains RED here and goes GREEN in plan 03. |
| T-14-input | Tampering / Input Validation | KAJIBA_EXPERIMENT_TYPE env value | mitigate | Validate the type env value against `EXPERIMENT_TYPES`; fall back to `"model_evaluation"` on any non-member value so an arbitrary env string cannot produce an invalid record. |
| T-14-pii | Information Disclosure (LOW/accepted) | raw first-user / last-gpt turn values in the record | accept | D-09: stored raw, scrubbed later via `kajiba experiment scrub` CLI; local/private store, no network. |
| T-14-dos | Denial of Service (host) | env read + record assembly inside on_session_start/finalize | mitigate | Assembly runs inside the existing fault-tolerant `try/except Exception` → `logger.exception` handler wrappers; never propagates to Hermes. |
</threat_model>

<verification>
- `python -m pytest tests/test_collector.py -x -q -k "lifecycle"` passes (coding path unchanged).
- `pytest tests/test_collector.py::TestExperimentCapture::test_field_mapping -x` and `::test_structural_parity_with_deliberate_log -x` exit 0.
- Full suite: the remaining finalize-once / no-staging tests are still RED (closed by plan 03) — that is expected.
</verification>

<success_criteria>
- `on_session_start` sets `self._experiment_mode` (+ type/category) from call-time env reads.
- collector.py imports the experiment_store MODULE (and `os`); the store dir is referenced as `experiment_store.EXPERIMENTS_DIR`, never a bound name.
- `_build_experiment_record` maps D-03..D-06 correctly and routes through `build_experiment_record`.
- Field-mapping and structural-parity tests pass (SC#2 satisfied by construction).
- Zero new schema, zero new persistence module.
</success_criteria>

<output>
Create `.planning/phases/14-live-experiment-capture/14-02-SUMMARY.md` when done
</output>
