---
phase: 14-live-experiment-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_collector.py
  - src/kajiba/collector.py
autonomous: true
requirements: [ECAP-01]
must_haves:
  truths:
    - "TestExperimentCapture exists in tests/test_collector.py with the six ECAP-01 test methods (RED — failing pending implementation)"
    - "Coding-path trajectory assembly is extracted into _build_trajectory() with no behavior change to _build_record"
    - "Existing coding-path lifecycle tests still pass after the extraction"
  artifacts:
    - path: "tests/test_collector.py"
      provides: "TestExperimentCapture scaffold + shared turn-scoped finalize fixture + parity helper"
      contains: "class TestExperimentCapture"
    - path: "src/kajiba/collector.py"
      provides: "_build_trajectory() extracted helper (shared by coding + experiment paths)"
      contains: "def _build_trajectory"
  key_links:
    - from: "tests/test_collector.py"
      to: "src/kajiba/experiment_store.py EXPERIMENTS_DIR"
      via: "SINGLE monkeypatch target (experiment_store.EXPERIMENTS_DIR) so the call-time D-13 guard resolves to the same tmp dir the collector writes to"
      pattern: "monkeypatch.setattr\\(experiment_store, \"EXPERIMENTS_DIR\""
    - from: "tests/test_collector.py"
      to: "src/kajiba/collector.py KajibaCollector"
      via: "KAJIBA_EXPERIMENT env flag + driving on_session_start → N×(on_llm_turn + on_session_end)"
      pattern: "monkeypatch.setenv.*KAJIBA_EXPERIMENT"
    - from: "src/kajiba/collector.py _build_record"
      to: "src/kajiba/collector.py _build_trajectory"
      via: "method call replacing the inline trajectory block"
      pattern: "self\\._build_trajectory\\("
---

<objective>
Lay the Wave 0 foundation for live experiment capture: write the failing `TestExperimentCapture`
test class (all six ECAP-01 behaviors) and extract the shared `_build_trajectory()` helper from the
coding path so both the coding and experiment finalize paths use ONE trajectory assembly.

Purpose: Establish the verification harness BEFORE implementation (Nyquist) and remove duplication
risk so the experiment finalize (plan 02) reuses the exact coding trajectory shape — guaranteeing
structural parity (SC#2) by construction.

Output: A failing `TestExperimentCapture` class + an extracted `_build_trajectory()` method that
leaves coding-path behavior byte-for-byte unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-live-experiment-capture/14-RESEARCH.md
@.planning/phases/14-live-experiment-capture/14-PATTERNS.md
@.planning/phases/14-live-experiment-capture/14-VALIDATION.md
</context>

## Artifacts this phase produces (full phase scope; this plan's subset marked ▸)

- ▸ Test class `TestExperimentCapture` (tests/test_collector.py) — six ECAP-01 methods
- ▸ Method `_build_trajectory()` (collector.py) — extracted shared helper
- Method `_build_experiment_record(session_id)` (collector.py) — plan 02
- Method `_finalize_experiment(session_id)` (collector.py) — plan 03
- Attrs `_experiment_mode` / `_experiment_type` / `_task_category` / `_last_experiment_path` (collector.py) — plans 02, 03
- Env vars `KAJIBA_EXPERIMENT` / `KAJIBA_EXPERIMENT_TYPE` / `KAJIBA_EXPERIMENT_CATEGORY` — plan 02
- Collector store-dir wiring: `from kajiba import experiment_store` referenced as a MODULE attribute (`experiment_store.EXPERIMENTS_DIR`) at the finalize use-site — plans 02, 03

<tasks>

<task type="auto">
  <name>Task 1: Extract _build_trajectory() shared helper from _build_record</name>
  <files>src/kajiba/collector.py</files>
  <read_first>
    - src/kajiba/collector.py:735-773 (current `_build_record`, including the inline trajectory block at 741-761)
    - .planning/phases/14-live-experiment-capture/14-PATTERNS.md § "_build_trajectory() (extracted helper)" (lines 150-178 — the exact block to extract and the free-form-safe note)
  </read_first>
  <action>
    Extract the trajectory-assembly block currently inline in `_build_record` (collector.py:741-761:
    `all_tool_calls` comprehension, `turn_count`/`total_tool_calls`/`successful_tool_calls`/
    `failed_tool_calls` tallies, and the `Trajectory(format="sharegpt_extended", conversations=...,
    ...)` construction) into a new method `_build_trajectory(self) -> Trajectory`. Replace the inline
    block in `_build_record` with `trajectory = self._build_trajectory()`. The extracted method MUST
    produce a byte-for-byte identical `Trajectory` to the prior inline block — same field values,
    same `self._conversations` source. Add a Google-style docstring (one-line summary + Returns).
    Do NOT change any other line of `_build_record`. This is a refactor-in-place: behavior identical.
  </action>
  <verify>
    <automated>python -m pytest tests/test_collector.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `collector.py` contains `def _build_trajectory(self)` returning a `Trajectory`.
    - `_build_record` calls `self._build_trajectory()` and no longer contains the inline tallies block.
    - `python -m pytest tests/test_collector.py -x -q` exits 0 (existing coding-path lifecycle tests unchanged).
    - `python -m pytest -q` exits 0 (no regression across the full suite).
  </acceptance_criteria>
  <done>The trajectory assembly lives in one shared `_build_trajectory()` method; all existing collector tests pass unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold TestExperimentCapture with the six ECAP-01 tests (RED)</name>
  <files>tests/test_collector.py</files>
  <read_first>
    - tests/test_collector.py:13-70 (TestCollectorLifecycle.test_full_session_lifecycle — the drive-through-lifecycle idiom to copy: instantiate KajibaCollector(), on_session_start, drive turns, on_session_end)
    - tests/test_experiment_store.py:1-53 (tmp_path store-isolation idiom + `_make_record(**overrides)` helper shape — copy the `monkeypatch.setattr(experiment_store, "EXPERIMENTS_DIR", ...)` isolation; this is the SINGLE patch target)
    - src/kajiba/collector.py:443-485 (`on_llm_turn(*, user_message, assistant_response, turn_id, **_)` — the v0.15.x plugin turn entry point; keyword-only)
    - src/kajiba/experiment_store.py:51 (module attribute `EXPERIMENTS_DIR`), 95-100 + 182-188 (the call-time D-13 guard: `expected_base` defaults to `EXPERIMENTS_DIR` read AT CALL TIME, then `store_dir.resolve() != expected_base.resolve()` raises ValueError)
    - src/kajiba/experiment_store.py:104-130, 133-223, 231-284 (log_experiment / update_experiment / build_experiment_record — needed to build the parity-target record directly)
    - .planning/phases/14-live-experiment-capture/14-VALIDATION.md (the six ECAP-01 rows + exact pytest command per row)
    - .planning/phases/14-live-experiment-capture/14-PATTERNS.md:227-250 (the six tests + isolation/lifecycle idioms)
  </read_first>
  <behavior>
    - test_opted_in_session_writes_one_record: drive on_session_start → N×(turn + on_session_end) with KAJIBA_EXPERIMENT=1 → exactly ONE exp_*.json in experiment_store.EXPERIMENTS_DIR (finalize-once); store_dir resolves equal to the patched experiment_store.EXPERIMENTS_DIR (no D-13 ValueError).
    - test_flag_absent_unchanged_coding_path: flag unset → session_*.json appears in STAGING_DIR, experiment_store.EXPERIMENTS_DIR untouched (no-regression).
    - test_structural_parity_with_deliberate_log: live-captured record's model_dump(by_alias=True) key/structure matches a record built directly via build_experiment_record (allow populated trajectory + eval_score==0.0).
    - test_field_mapping: task_description==first human turn, local_model_output==last gpt turn, eval_score==0.0, experiment.local_model==captured metadata, trajectory populated.
    - test_no_staging_or_outbox_in_experiment_mode: even with contribution_mode=="continuous", experiment mode writes nothing to STAGING_DIR/OUTBOX_DIR (D-08).
    - test_zero_turn_session_writes_nothing: zero-turn / interrupted on_session_end → no exp_*.json, no IndexError (Pitfall 3).
  </behavior>
  <action>
    Add a `TestExperimentCapture` class to tests/test_collector.py. Copy the tmp_path isolation idiom
    from tests/test_experiment_store.py and use a SINGLE store-dir patch target:
    `monkeypatch.setattr(experiment_store, "EXPERIMENTS_DIR", tmp_path / "experiments")` (import
    `from kajiba import experiment_store` at the top of the test module). Do NOT also patch a
    collector-bound `EXPERIMENTS_DIR` name — the collector references the store dir by MODULE attribute
    (`experiment_store.EXPERIMENTS_DIR`), so patching the module attribute alone is sufficient and
    makes the call-time D-13 guard (`expected_base = EXPERIMENTS_DIR` resolved inside
    `update_experiment`) resolve to the SAME tmp dir the collector passes as `store_dir` — i.e. no
    ValueError and writes land in the tmp store. Separately patch `STAGING_DIR`/`OUTBOX_DIR` on the
    collector module to tmp subdirs (those remain collector-owned constants). Set the opt-in flag with
    `monkeypatch.setenv("KAJIBA_EXPERIMENT", "1")`. Add a shared helper (module-level function or
    fixture) `_drive_turns(collector, session_id, turns)` that calls `on_session_start` once then loops
    N times calling `on_llm_turn(user_message=..., assistant_response=..., turn_id=...)` (the v0.15.x
    plugin turn entry point per RESEARCH diagram line 111: post_llm_call → on_llm_turn — keyword-only,
    do NOT use on_turn_complete) followed by `on_session_end(session_id)` each iteration — this
    reproduces the turn-scoped finalize scenario. Add a parity helper that builds a record directly via
    `build_experiment_record(...)` and asserts the live record's `model_dump(by_alias=True)` top-level
    keys + nested experiment/outcome keys match (permitting a populated `trajectory` and
    `eval_score==0.0`). Write all six test methods named EXACTLY as in 14-VALIDATION.md. These tests
    are EXPECTED TO FAIL now (implementation lands in plans 02–03) — they are the RED baseline; do not
    weaken assertions to make them pass. (RED execution is intentionally deferred to plans 02–03; this
    plan only needs the six tests to COLLECT cleanly.)
  </action>
  <verify>
    <automated>python -m pytest tests/test_collector.py::TestExperimentCapture --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_collector.py` contains `class TestExperimentCapture` with all six methods named exactly: `test_opted_in_session_writes_one_record`, `test_flag_absent_unchanged_coding_path`, `test_structural_parity_with_deliberate_log`, `test_field_mapping`, `test_no_staging_or_outbox_in_experiment_mode`, `test_zero_turn_session_writes_nothing`.
    - `python -m pytest tests/test_collector.py::TestExperimentCapture --collect-only -q` lists six collected tests (collection succeeds — no import/syntax errors).
    - Store isolation uses the SINGLE target `monkeypatch.setattr(experiment_store, "EXPERIMENTS_DIR", ...)` (grep: `monkeypatch.setattr(experiment_store, "EXPERIMENTS_DIR"` present); the class does NOT patch a collector-bound `EXPERIMENTS_DIR` name.
    - The class uses `monkeypatch.setenv("KAJIBA_EXPERIMENT", "1")` for the opt-in flag (grep: `monkeypatch.setenv.*KAJIBA_EXPERIMENT` present).
    - The `_drive_turns` helper invokes `on_llm_turn(...)` (the v0.15.x turn entry point), NOT `on_turn_complete` (grep: `on_llm_turn(` present in the helper; `on_turn_complete` absent from TestExperimentCapture).
    - `test_flag_absent_unchanged_coding_path` is the ONLY method that does not set KAJIBA_EXPERIMENT.
  </acceptance_criteria>
  <done>The six-method TestExperimentCapture class collects cleanly and fails (RED) pending the plan 02–03 implementation; isolation uses tmp_path + a SINGLE experiment_store.EXPERIMENTS_DIR monkeypatch and drives turns via on_llm_turn.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hermes host → Kajiba hook | Untrusted lifecycle events drive the collector; a hook exception must never reach the host. |
| Collector finalize → filesystem | Experiment records must land ONLY in EXPERIMENTS_DIR, never the publish path (staging/outbox). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-priv | Information Disclosure (HIGH) | collector finalize → STAGING_DIR/OUTBOX_DIR | mitigate | This plan installs the regression tests (`test_flag_absent_unchanged_coding_path`, `test_no_staging_or_outbox_in_experiment_mode`) that prove experiment mode never reaches the publish path; enforcement lands in plans 02–03 (D-08 guard + D-13 store guard). |
| T-14-dos | Denial of Service (host) | extracted `_build_trajectory` inside `_build_record`/finalize | mitigate | Extraction preserves the existing fault-tolerant `try/except Exception` → `logger.exception` wrapper on the calling handlers; no exception path is introduced. |
| T-14-pii | Information Disclosure (LOW/accepted) | raw turns captured at finalize | accept | D-09: store raw locally, scrub later via `kajiba experiment scrub` CLI. Local/private store, no network — acceptable at rest. |
</threat_model>

<verification>
- `python -m pytest tests/test_collector.py -x -q` passes (coding-path extraction is non-breaking).
- `python -m pytest -q` passes for all PRE-EXISTING tests (the new TestExperimentCapture is allowed to fail RED).
- `python -m pytest tests/test_collector.py::TestExperimentCapture --collect-only -q` collects six tests.
</verification>

<success_criteria>
- `_build_trajectory()` exists, is called by `_build_record`, and existing tests pass unchanged.
- `TestExperimentCapture` exists with the six exactly-named ECAP-01 methods and collects without error.
- Store isolation uses the single `experiment_store.EXPERIMENTS_DIR` monkeypatch target and turns are driven via `on_llm_turn` (matching the call-time D-13 guard and the v0.15.x plugin path).
- No experiment-mode implementation yet (that is plans 02–03); the six tests form the RED baseline (RED execution intentionally deferred to plans 02–03).
</success_criteria>

<output>
Create `.planning/phases/14-live-experiment-capture/14-01-SUMMARY.md` when done
</output>
