---
phase: 07-turn-capture-semantic-pii-scrubbing
plan: 05
type: execute
wave: 3
depends_on: ["07-04"]
files_modified:
  - src/kajiba/cli.py
  - tests/test_cli.py
autonomous: true
requirements: [PRIV-01, PRIV-02, PRIV-04]
must_haves:
  truths:
    - "kajiba preview shows GLiNER-redacted person/company/project names (not just regex)"
    - "Entities with confidence 0.4-0.7 surface in the flagged-for-review panel with label + confidence"
    - "Layer C composes after Layer B at every scrub_record call site (consistent helper)"
    - "preview degrades gracefully (no crash) when [llm-scrub] is not installed"
  artifacts:
    - path: "src/kajiba/cli.py"
      provides: "Layer-C wiring after scrub_record; semantic flags into the existing flagged channel; ScrubLog counts updated"
      contains: "scrub_record_semantic"
  key_links:
    - from: "src/kajiba/cli.py preview"
      to: "scrubber_semantic.scrub_record_semantic"
      via: "Layer C after Layer B scrub_record, inside try/except SemanticScrubUnavailable"
      pattern: "scrub_record_semantic"
    - from: "semantic FlaggedItems"
      to: "_render_preview(flagged_items=...)"
      via: "extend the existing all_flagged channel (D-08)"
      pattern: "all_flagged"
---

<objective>
Wire the Layer C semantic scrubber into the CLI so `kajiba preview` shows GLiNER-detected
names redacted and 0.4-0.7 entities surfaced in the existing flagged-for-review panel.
This makes Success Criteria #4 and #5 observably TRUE at the command line: GLiNER
redactions appear in the preview (not just regex), and flagged entities show snippet +
suggested label + confidence in the `_render_preview(flagged_items=...)` panel (D-08).
A shared helper applies Layer C consistently across the 4+ `scrub_record` call sites, and
the whole path degrades gracefully when `[llm-scrub]` is absent.

Purpose: Make the semantic privacy layer user-visible — the surfacing half of SC#4/#5.
Reuses the existing render channel; no new render surface (D-08), stateless recompute (D-09).
Output: CLI-wired Layer C with shared helper; flagged-panel test GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-turn-capture-semantic-pii-scrubbing/07-RESEARCH.md
@.planning/phases/07-turn-capture-semantic-pii-scrubbing/07-PATTERNS.md
@src/kajiba/cli.py
@src/kajiba/scrubber.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared Layer-C helper + wire into preview/submit/export/scrub call sites</name>
  <read_first>
    - src/kajiba/cli.py lines 591-629 (`preview` — `scrub_record` at 601, `all_flagged` build 604-610, `_render_preview(... flagged_items=all_flagged ...)`), lines 632-681 (`submit` — scrub_record at 649, all_flagged 652-658), lines 684-705+ (`export` — scrub_record at 694), and the 4th site near line 1761
    - src/kajiba/cli.py lines 486-500 (`_render_preview` flagged-panel rendering: reads `item.text` and `item.reason`)
    - src/kajiba/scrubber_semantic.py (07-04: `scrub_record_semantic`, `SemanticScrubUnavailable`)
    - src/kajiba/scrubber.py lines 357-366 (`ScrubLog.potential_names_redacted`, `items_flagged`)
    - 07-RESEARCH.md § Pattern 6 (composition snippet: set `scrub_log.potential_names_redacted`, `items_flagged += len(semantic_flags)`, extend `all_flagged`), § Common Pitfalls 5 (graceful degrade)
    - 07-PATTERNS.md § cli.py (4+ call sites; factor a shared helper)
  </read_first>
  <behavior>
    - `flagged_panel`: after a session with a 0.4-0.7 GLiNER entity is previewed, `_render_preview` shows it in the flagged panel with text + `GLiNER {label} (confidence X.XX)` reason (D-08); a `score>=0.7` person name appears REDACTED in the preview body, not flagged.
    - Graceful degrade: when `[llm-scrub]` is not installed, `kajiba preview` runs to completion (regex-only) without raising — semantic flags simply absent.
    - Consistency: every `scrub_record` call site that renders flags applies Layer C through the same helper (no site silently skips Layer C).
  </behavior>
  <action>
    Factor a single shared helper (e.g. `_apply_semantic_layer(scrubbed_record, regex_flagged, scrub_log)`)
    that runs `scrub_record_semantic(scrubbed_record)` inside `try/except SemanticScrubUnavailable`
    (RESEARCH Pattern 6 / Pitfall 5): on success it returns the semantically-scrubbed record, sets
    `scrub_log.potential_names_redacted = names_redacted`, does `scrub_log.items_flagged += len(semantic_flags)`,
    and returns the combined flagged list (regex `all_flagged` + semantic flags) so the SAME
    `_render_preview(flagged_items=...)` channel surfaces both (D-08 — no new render surface). On
    `SemanticScrubUnavailable` it returns the inputs unchanged (regex-only) so the command never crashes.
    Wire this helper into `preview` (after line 601 scrub_record), `submit` (after 649), `export`
    (after 694), and the 4th call site (~1761) so Layer C is applied consistently — but keep
    `export`/`submit` semantics intact (they currently feed the privacy pipeline; the helper must not
    change the persisted-record bytes beyond the intended semantic redactions; for `export`/`submit`
    apply the semantic redactions to the record that flows downstream as today's regex scrub already
    does). Honor D-09 stateless recompute: GLiNER loads once per CLI invocation via the within-run
    singleton (07-04) — do NOT persist scrubbed state and do NOT add `pipeline_stage` (Phase 8). Use the
    existing `_render_preview` unchanged. `Optional[...]` typing, Google-style docstrings per CLAUDE.md.
  </action>
  <acceptance_criteria>
    - Behavior assertion: `python -m pytest tests/test_cli.py -k "flagged_panel" -x` PASSES (07-01/07-02 surfacing scaffold GREEN; if the flagged_panel scaffold lives elsewhere, it is added/extended here).
    - Source assertion: a single shared helper applies `scrub_record_semantic` and is called from `preview`, `submit`, `export`, and the 4th `scrub_record` site (grep: helper invoked at ≥4 sites; no remaining flag-rendering `scrub_record` site lacks Layer C).
    - Source assertion: the helper wraps `scrub_record_semantic(...)` in `try/except SemanticScrubUnavailable` and returns regex-only results on that exception.
    - Source assertion: the helper sets `scrub_log.potential_names_redacted` and increments `scrub_log.items_flagged`, and extends the existing `all_flagged` list (reuses `_render_preview(flagged_items=...)`, no new render function).
    - Source assertion: no `pipeline_stage` field and no persisted-scrub-state write is added (D-09; grep absent).
    - Behavior assertion: `python -m pytest tests/test_cli.py -k "preview"` PASSES with `[llm-scrub]` absent (graceful degrade — no crash).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_cli.py -k "flagged_panel or preview" -x</automated>
  </verify>
  <done>Layer C composes after Layer B at all scrub_record sites via a shared helper; semantic redactions + flags surface in the existing preview panel; preview degrades gracefully without the extra.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flagged-panel surfacing test + full-suite wave gate</name>
  <read_first>
    - tests/test_cli.py (existing preview/submit test patterns; how a staging record is set up and `kajiba preview` is invoked; how console output is asserted)
    - src/kajiba/cli.py lines 486-500 (`_render_preview` flagged panel output format: `* {item.text} — {item.reason}`)
    - 07-VALIDATION.md SC#5/D-08 row (`-k flagged_panel`)
    - 07-RESEARCH.md § Pattern 7 (`FlaggedItem.reason` format)
  </read_first>
  <behavior>
    - `flagged_panel`: with `scrub_record_semantic` monkeypatched/stubbed to return a known semantic `FlaggedItem` (LANE-A style, no model needed), `kajiba preview` output contains the flagged entity's text and a `GLiNER ... (confidence ...)` reason in the flagged-for-review panel.
    - Degrade: with `scrub_record_semantic` raising `SemanticScrubUnavailable`, `kajiba preview` still completes and prints the regex-only summary.
  </behavior>
  <action>
    Add/extend a `flagged_panel`-selected test in `tests/test_cli.py` that does NOT require the real
    GLiNER model: monkeypatch the shared Layer-C helper's `scrub_record_semantic` to return a record plus
    a synthetic `FlaggedItem(reason="GLiNER company (confidence 0.55)", ...)` and a names-redacted count,
    then invoke `kajiba preview` via the Click test runner and assert the flagged panel shows the item
    text and reason (D-08 surfacing). Add a companion assertion that when the helper's
    `scrub_record_semantic` raises `SemanticScrubUnavailable`, `kajiba preview` exits 0 and still prints
    the regex scrub summary (graceful degrade). Keep these tests model-free so they run in the core suite.
    Then run the full suite as the wave-merge gate and confirm no regressions.
  </action>
  <acceptance_criteria>
    - Behavior assertion: `python -m pytest tests/test_cli.py -k "flagged_panel" -x` PASSES.
    - Source assertion: the `flagged_panel` test monkeypatches `scrub_record_semantic` (model-free) and asserts both the entity `text` and the `GLiNER ... (confidence ...)` reason appear in output.
    - Source assertion: a degrade test asserts `kajiba preview` exit code 0 when `SemanticScrubUnavailable` is raised.
    - Behavior assertion: `python -m pytest` (full suite) PASSES with no regressions vs the pre-phase baseline (the only allowed RED-by-design are LANE B GLiNER tests skipped without `[llm-scrub]`).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_cli.py -k "flagged_panel" -x && python -m pytest</automated>
  </verify>
  <done>Model-free flagged-panel surfacing test GREEN; degrade test GREEN; full suite green (LANE B GLiNER tests skipped without the extra).</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- **src/kajiba/cli.py**: shared `_apply_semantic_layer` helper (Layer C composition after Layer B), wired into `preview`/`submit`/`export`/4th site; semantic flags into the existing `all_flagged` → `_render_preview(flagged_items=...)` channel; `ScrubLog.potential_names_redacted`/`items_flagged` updated.
- **tests/test_cli.py**: model-free `flagged_panel` surfacing test + graceful-degrade test.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staged record → CLI scrub pipeline | Captured (possibly PII-bearing) session content flows into preview/submit/export |
| optional extra → CLI | `[llm-scrub]` may be absent at preview time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12 | Information Disclosure | submit/export path | mitigate | Layer C composes after Layer B at submit/export sites too (shared helper), so semantic name redactions apply before any record can leave the machine — not just in preview. |
| T-07-13 | Denial of Service | kajiba preview | mitigate | `SemanticScrubUnavailable` caught in the shared helper → preview degrades to regex-only instead of crashing when `[llm-scrub]` is absent (Task 1/2 degrade tests). |
| T-07-14 | Information Disclosure | flagged-but-unaddressed entities | accept (this phase) | Flagged 0.4-0.7 entities are left VISIBLE for reviewer context (D-08) and pass through on submit if unaddressed; resumable HITL enforcement is Phase 8 (VAL-02). Existing "flagged items will pass through" warning is shown. |
</threat_model>

<verification>
- `python -m pytest tests/test_cli.py -k "flagged_panel or preview"` GREEN.
- `python -m pytest` full suite GREEN (LANE B GLiNER tests skipped without `[llm-scrub]`).
- `python -c "from kajiba import cli"` imports clean without `[llm-scrub]`.
- Grep: Layer-C helper invoked at ≥4 `scrub_record` sites; no `pipeline_stage` added.
</verification>

<success_criteria>
- SC#4: `kajiba preview` shows GLiNER-detected personal/company/project names redacted (not just regex).
- SC#5: 0.4-0.7 entities surface in the flagged-for-review panel with label + confidence.
- Layer C applied consistently across scrub sites; graceful degrade without the extra; no Phase 8 scope (no pipeline_stage, no resumable review).
</success_criteria>

<output>
Create `.planning/phases/07-turn-capture-semantic-pii-scrubbing/07-05-SUMMARY.md` when done
</output>
