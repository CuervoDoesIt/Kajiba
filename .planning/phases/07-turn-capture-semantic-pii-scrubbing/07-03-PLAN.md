---
phase: 07-turn-capture-semantic-pii-scrubbing
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/kajiba/collector.py
  - src/kajiba/plugin/hooks.py
  - tests/test_collector.py
  - tests/test_plugin.py
autonomous: true
requirements: [CAPT-02, CAPT-03, CAPT-04]
must_haves:
  truths:
    - "One post_llm_call produces a human turn then a gpt turn with correct role attribution"
    - "Tool calls attach to the correct assistant turn via a turn_id-keyed pending buffer, deduped by tool_call_id"
    - "Hermes status='ok' maps to ToolStatusType 'success'"
    - "Turn-scoped on_session_end writes exactly ONE staging file per session_id"
    - "Model metadata is enriched from ollama.show() when local, degrades to slug inference when remote"
  artifacts:
    - path: "src/kajiba/collector.py"
      provides: "Paired-turn capture, tool buffer, ollama enrichment, finalize-once session-end"
      contains: "_pending_tools"
    - path: "src/kajiba/plugin/hooks.py"
      provides: "Promoted on_post_llm_call / on_post_tool_call dispatch to collector"
      contains: "on_post_llm_call"
  key_links:
    - from: "src/kajiba/plugin/hooks.py"
      to: "KajibaCollector paired-turn + tool-ingest methods"
      via: "kwarg extraction + dispatch in promoted hooks"
      pattern: "_collector\\.(on_llm_turn|on_tool_call|ingest)"
    - from: "src/kajiba/collector.py on_session_end"
      to: "session_{id}.json staging file"
      via: "idempotent overwrite keyed by session_id"
      pattern: "session_.*\\.json"
---

<objective>
Promote the Phase 6 debug-only hook stubs into real turn/tool capture and fix the
turn-scoped `on_session_end` correctness bug. This plan makes Success Criteria #1, #2,
and #3 (capture side) TRUE: user/assistant turns become `ConversationTurn` objects with
correct roles (CAPT-02), tool calls attach to the right turn via a pending buffer
(CAPT-03), and model metadata is enriched from `ollama.show()` with graceful remote
degradation (CAPT-04, mocked here; live proof is 07-05).

Purpose: Real Hermes session data flows into KajibaRecord objects — the ingest half of
the phase goal. Bakes in all four CRITICAL CORRECTIONS as enforced behavior.
Output: Capture-complete `collector.py` + promoted `plugin/hooks.py`; 07-01 capture
scaffolds turn GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-environment-plugin-foundation/06-HOOK-KWARGS.md
@.planning/phases/07-turn-capture-semantic-pii-scrubbing/07-RESEARCH.md
@.planning/phases/07-turn-capture-semantic-pii-scrubbing/07-PATTERNS.md
@src/kajiba/collector.py
@src/kajiba/plugin/hooks.py
@src/kajiba/schema.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Paired-turn capture + tool buffer in collector.py (CAPT-02/03)</name>
  <read_first>
    - src/kajiba/collector.py lines 172-180 (`__init__` state) and 205-252 (`on_session_start` reset block, `on_turn_complete` construction analog with `[:2000]` truncation)
    - src/kajiba/collector.py lines 379-417 (`_build_record`; line 393-395 counts `successful_tool_calls` by `tool_status == "success"`)
    - src/kajiba/schema.py lines 125-144 (ToolCall, ConversationTurn — `from` alias, `tool_status: ToolStatusType` required) and line 101 (`ToolStatusType` literal — no `ok`)
    - 06-HOOK-KWARGS.md Hook 2 + Hook 3 tables + findings 2, 3, 4
    - 07-RESEARCH.md § Pattern 1, § Pattern 2, § Code Examples (status mapping, band bucketing irrelevant here)
    - 07-PATTERNS.md § collector.py items 2 (turn assembly) — keep `on_turn_complete` for back-compat
  </read_first>
  <behavior>
    - `llm_turn`: paired-turn entry point appends exactly a `human` turn (value=`user_message`) then a `gpt` turn (value=`assistant_response`); `conversation_history` is used for ordering/dedup ONLY and never re-ingested as turns (no double-count).
    - `tool_buffer`: a tool event keyed by `turn_id` becomes a `ToolCall` with `tool_status="success"` when `status="ok"` and no error fields; duplicate `tool_call_id` is deduped; `result` JSON string is parsed before storage; `args` dict is serialized into `tool_input`; tool attaches to that `turn_id`'s gpt turn (buffer covers tool-before-turn and tool-after-turn orderings).
    - Error mapping: `error_type`/`error_message` populated → `tool_status` is `error` (or `timeout` if the error class/message indicates timeout); never stores raw `ok`.
  </behavior>
  <action>
    Add to `KajibaCollector.__init__` (and reset in `on_session_start` alongside `_conversations`) a
    `_pending_tools: dict[str, list[ToolCall]]` buffer and a `_last_gpt_turn_id` marker. Add a paired-turn
    method (name it `on_llm_turn`, keyword-only: `user_message`, `assistant_response`, `turn_id=None`,
    plus `**_` tolerance) that appends a `human` ConversationTurn then a `gpt` ConversationTurn (per
    RESEARCH Pattern 1), records `turn_id`, and flushes any tools already buffered under that `turn_id`
    onto the gpt turn's `tool_calls`. Add a tool-ingest method (name it `on_tool_call`, keyword-only
    matching the 06-HOOK-KWARGS Hook 3 kwargs) that: maps status via the verified rule
    (`error_type`/`error_message` present → `error`/`timeout`; else `status=="ok"` → `success`; map
    `"ok"`→`"success"` EXACTLY — Correction 2); `json.loads` the `result` JSON string with a try/except
    fallback to the raw string (finding 3), truncating to `[:2000]`; serialize the already-dict `args`
    into `tool_input` via `json.dumps(...)[:2000]`; build the `ToolCall`; dedup by `tool_call_id`; then
    either attach to the gpt turn for `turn_id` if present or buffer under `turn_id`. Keep the existing
    `on_turn_complete` method unchanged (test back-compat — Correction: do not break it). Every new
    method wraps its body in try/except + `logger.exception(...)` and never raises (fault-tolerant
    carried rule). Do NOT run any scrubbing here (scrub is a CLI step — carried hard rule). Use
    `Optional[...]` typing (not `X | None`) and Google-style docstrings per CLAUDE.md.
  </action>
  <acceptance_criteria>
    - Behavior assertion: `python -m pytest tests/test_collector.py -k "llm_turn or tool_buffer" -x` PASSES (the 07-01 RED scaffolds for these selectors are now GREEN).
    - Source assertion: `collector.py` defines `_pending_tools` in `__init__` and resets it in `on_session_start`.
    - Source assertion: the tool-ingest path maps `status="ok"` to the literal `"success"` (grep for the mapping; raw `"ok"` is never assigned to `tool_status`).
    - Source assertion: `result` is passed through `json.loads` inside a try/except (parse-then-fallback), and `args` is serialized with `json.dumps`.
    - Behavior assertion: pre-existing `on_turn_complete` tests still PASS (back-compat preserved).
    - Source assertion: no `import gliner`, `import ollama`, or scrub call appears in the new turn/tool methods (no scrubbing in capture path).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_collector.py -k "llm_turn or tool_buffer" -x</automated>
  </verify>
  <done>Paired-turn capture and turn_id-keyed tool buffer with ok→success mapping and JSON result parsing are GREEN; back-compat intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ollama metadata enrichment + remote degradation + finalize-once (CAPT-04, session-end fix)</name>
  <read_first>
    - src/kajiba/collector.py lines 48-154 (`_detect_hardware` psutil soft-import block lines 104-117 — the soft-dep template; `_extract_model_metadata` lines 136-154 to extend)
    - src/kajiba/collector.py lines 254-336 (`on_session_end`; continuous-mode auto-submit branch lines 286-333) and lines 419-435 (`_save_to_staging` already overwrites `session_{id}.json`)
    - src/kajiba/schema.py lines 166-189 (`ModelMetadata` all fields exist; `provider: ProviderType` literal lacks `anthropic`) and line 189 (`HardwareProfile.inference_backend: Optional[str]`)
    - 07-RESEARCH.md § Pattern 4 (ollama.show fields + is_local detection + provider mapping), § "Turn-Scoped on_session_end Fix", § Common Pitfalls 1/6, § Open Questions 3 (Anthropic → custom)
    - 07-PATTERNS.md § collector.py items 1 (soft-import), 3 (metadata), 4 (finalize-once)
  </read_first>
  <behavior>
    - `ollama_metadata`: with `ollama.show()` mocked to the documented shape, `ModelMetadata` carries non-null `parameter_count` (from `details.parameter_size`), `quantization` (`details.quantization_level`), `model_family` (`details.family`), `context_window` (`model_info["<arch>.context_length"]`), `model_hash` (`digest`).
    - `remote_degrade`: ollama absent/`show` raising → no exception; `is_local=false`; `provider`/`model_family` slug-inferred; `parameter_count`/`quantization`/`model_hash` stay None; `HardwareProfile.inference_backend` set to the real backend (e.g. `openrouter`/`anthropic` free-text). Anthropic maps `provider="custom"` (literal gap, Correction 5) with `inference_backend="anthropic"`.
    - `session_end_once`: N `on_session_end` firings for one `session_id` produce exactly ONE `session_{id}.json` whose trajectory reflects all accumulated turns; continuous-mode auto-submit fires at most once per session.
  </behavior>
  <action>
    Add a soft-import helper (name it `_enrich_from_ollama(model_name)`) mirroring the `_detect_hardware`
    psutil block: `try: import ollama / except ImportError: return {}`, then a second `try/except Exception`
    around `ollama.show(model_name)` so remote sessions NEVER error (D-01). Map its response fields per
    RESEARCH Pattern 4 (`details.parameter_size`→`parameter_count`, `quantization_level`→`quantization`,
    `family`→`model_family`, the `*.context_length` entry in `model_info`→`context_window`,
    `digest`→`model_hash`), handling both dict-like and object-like returns (Assumption A1). Extend
    `_extract_model_metadata` (or layer a new enrichment step at session start) to: always set
    `model_name`/`provider`/`platform`/`is_local` from hook kwargs; detect local ONLY when
    `provider=="ollama"` or the slug is a bare name (no provider prefix) and attempt `ollama.show()` only
    then (avoid a failing network call on remote sessions); for remote slugs do light slug inference for
    `provider`+`model_family` and set `HardwareProfile.inference_backend` (D-03). Map a remote Anthropic
    backend to `provider="custom"` and `inference_backend="anthropic"` — DO NOT add `anthropic` to the
    schema literal (Correction 5; no schema change). Record `telemetry_schema_version` (default
    `"hermes.observer.v1"`) on the metadata/record for forward-compat (storage location discretionary;
    a field already present on ModelMetadata is not available, so store it where a future schema bump can
    read it — e.g. fold into an existing free-text/metadata location without a schema change, and note the
    chosen location in the SUMMARY).
    For the session-end fix: change `on_session_end` so per-turn firings ACCUMULATE and write
    idempotently — keep calling `_save_to_staging()` (which already overwrites `session_{id}.json`) so N
    firings yield ONE file (Correction 3); record `completed`/`interrupted` as candidate outcome signals
    without finalizing per turn. Guard the continuous-mode auto-submit branch with a `_finalized` once-flag
    so it submits at most once per session. Hooks remain fault-tolerant.
  </action>
  <acceptance_criteria>
    - Behavior assertion: `python -m pytest tests/test_collector.py -k "ollama_metadata or remote_degrade or session_end_once" -x` PASSES (07-01 RED scaffolds now GREEN).
    - Source assertion: `_enrich_from_ollama` uses `try: import ollama / except ImportError` (soft-import) AND wraps `ollama.show(...)` in its own try/except (no propagation).
    - Source assertion: a remote Anthropic backend maps `provider="custom"` (NOT a new literal value) and sets `inference_backend`; `src/kajiba/schema.py` is unchanged (`git diff --quiet src/kajiba/schema.py`).
    - Behavior assertion: a `session_end_once` test confirms exactly ONE `session_{id}.json` after N firings.
    - Source assertion: continuous-mode auto-submit is gated by a once-flag (`_finalized` or equivalent).
    - Behavior assertion: `python -c "import kajiba.collector"` succeeds WITHOUT ollama installed (core import-clean).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_collector.py -k "ollama_metadata or remote_degrade or session_end_once" -x && git diff --quiet src/kajiba/schema.py && echo "schema untouched"</automated>
  </verify>
  <done>Ollama enrichment (mocked) populates ModelMetadata, remote path degrades safely with inference_backend set, session-end is finalize-once (one file), schema untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Promote on_post_llm_call / on_post_tool_call hooks to real dispatch</name>
  <read_first>
    - src/kajiba/plugin/hooks.py lines 64-129 (the `on_session_start` dispatch shell to mirror; the `on_post_llm_call`/`on_post_tool_call` stubs lines 87-114 to promote; `on_session_end` dispatch lines 117-129)
    - 06-HOOK-KWARGS.md Hook 2 + Hook 3 kwarg tables (exact kwarg names)
    - 07-PATTERNS.md § plugin/hooks.py (fault-tolerant dispatch pattern to COPY; keep `_log_kwargs` debug line)
    - collector.py paired-turn + tool-ingest methods from Tasks 1-2
  </read_first>
  <behavior>
    - `on_post_llm_call(**kwargs)` keeps the `_log_kwargs` debug line, then dispatches `user_message`/`assistant_response`/`turn_id` (and session/model context) to the collector paired-turn method when `_collector is not None`.
    - `on_post_tool_call(**kwargs)` keeps the debug line, then dispatches `tool_name`/`args`/`result`/`tool_call_id`/`turn_id`/`status`/`error_type`/`error_message`/`duration_ms` to the collector tool-ingest method.
    - A raising collector method does not propagate out of either hook (try/except contract preserved; Hermes never disrupted).
  </behavior>
  <action>
    Promote `on_post_llm_call` and `on_post_tool_call` from debug-only stubs to real dispatch, COPYING the
    `on_session_start` shell exactly: keep the `_log_kwargs(...)` call, wrap the body in
    `try/except Exception: logger.exception(...)`, and guard with `if _collector is not None`. Extract the
    live kwargs by the names verified in 06-HOOK-KWARGS (use `kwargs.get(...)` so missing/extra kwargs are
    tolerated — MP-2) and call the collector methods added in Tasks 1-2. Do NOT register `pre_llm_call`
    (RESEARCH Pattern 3 — `post_llm_call.user_message` already carries the user turn; `pre_llm_call` is
    reserved for Phase 8 PLUG-05). Do NOT scrub in hooks. Update the module/handler docstrings to reflect
    that these are now real-capture handlers (Phase 7), not debug stubs. The `on_session_end` dispatch
    shell is unchanged (the finalize-once fix lives in the collector method it calls, per Task 2).
  </action>
  <acceptance_criteria>
    - Behavior assertion: `python -m pytest tests/test_plugin.py -k "post_llm or post_tool or dispatch or fault" -x` PASSES (07-01 RED scaffolds now GREEN).
    - Source assertion: both promoted hooks retain `_log_kwargs(...)`, the `try/except Exception: logger.exception(...)` shell, and the `if _collector is not None` guard.
    - Source assertion: `pre_llm_call` is NOT registered anywhere in `src/kajiba/plugin/` (grep confirms absence).
    - Behavior assertion: pre-existing `tests/test_plugin.py` register/kwargs/debug tests still PASS.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_plugin.py -k "post_llm or post_tool or dispatch or fault" -x</automated>
  </verify>
  <done>Promoted hooks dispatch live kwargs to the collector fault-tolerantly; no pre_llm_call registered; plugin tests green.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- **collector.py** new symbols: `on_llm_turn` (paired-turn capture), `on_tool_call` (tool ingest + ok→success mapping + JSON-result parse), `_enrich_from_ollama` (ollama soft-import helper), `_pending_tools`/`_last_gpt_turn_id`/`_finalized` state, finalize-once `on_session_end`.
- **plugin/hooks.py**: promoted `on_post_llm_call`/`on_post_tool_call` from debug stubs to real collector dispatch.
- `telemetry_schema_version` capture storage (location recorded in SUMMARY).
- Test groups `llm_turn`/`tool_buffer`/`ollama_metadata`/`remote_degrade`/`session_end_once` (test_collector) and `post_llm`/`post_tool`/`dispatch`/`fault` (test_plugin) move RED→GREEN.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hermes hook payload → collector | Untrusted session content (user/assistant text, tool args/results, error messages) crosses into capture |
| local Ollama service → collector | `ollama.show()` network/service call (optional, soft) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | ToolCall construction | mitigate | Map Hermes `status="ok"` → `"success"` so `ToolStatusType` validation passes; bad status would otherwise raise and be swallowed by the fault-tolerant try/except (silent data loss). Validated by `tool_buffer` test. |
| T-07-05 | Denial of Service | Hermes session | mitigate | Every promoted hook + new collector method wraps its body in try/except and never propagates (carried fault-tolerant rule); finalize never raises. Validated by `fault` test. |
| T-07-06 | Information Disclosure | tool error_message / capture | accept (this phase) | `error_message` may carry PII; raw capture is intentional (scrub runs at the CLI step, 07-04). No PII leaves the machine at capture time — staging is local-only under HERMES_HOME. |
| T-07-07 | Tampering | core import surface | mitigate | `ollama` is soft-imported inside `_enrich_from_ollama`; `import kajiba.collector` stays clean offline (Task 2 verify). |
</threat_model>

<verification>
- `python -m pytest tests/test_collector.py tests/test_plugin.py` — all Phase 7 capture selectors GREEN.
- `git diff --quiet src/kajiba/schema.py` — schema untouched (no `anthropic` literal added).
- `python -c "import kajiba.collector, kajiba.plugin.hooks"` succeeds without ollama/gliner installed.
- Full suite: `python -m pytest` (wave-merge gate) — no regressions.
</verification>

<success_criteria>
- SC#1: user/assistant turns captured as ConversationTurn with correct roles (CAPT-02).
- SC#2: tool calls attached to the correct assistant turn via the pending buffer (CAPT-03).
- SC#3 (mocked): model metadata captured from `ollama.show()` into ModelMetadata; remote path degrades (live proof deferred to 07-05).
- Exactly one staging file per session; ok→success; no double-count; no schema change.
</success_criteria>

<output>
Create `.planning/phases/07-turn-capture-semantic-pii-scrubbing/07-03-SUMMARY.md` when done
</output>
