---
phase: 07-turn-capture-semantic-pii-scrubbing
plan: 04
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - src/kajiba/scrubber_semantic.py
  - src/kajiba/scrubber_llm.py
  - tests/test_scrubber_semantic.py
autonomous: true
requirements: [PRIV-01, PRIV-02, PRIV-03]
must_haves:
  truths:
    - "scrubber_semantic loads nvidia/gliner-PII (capital PII) and detects person/company/project/location"
    - "Spans bucket by float score: >=0.7 redact, 0.4-0.7 flag, <0.4 ignore (D-05)"
    - "Tool fields are FLAG-only; ConversationTurn.value redacts >=0.7 + flags 0.4-0.7 (D-07)"
    - "Calibration gate: zero auto-redact >=0.7 on the code fixture (D-06 hard gate)"
    - "Core imports clean without [llm-scrub]; SemanticScrubUnavailable raised when extra absent"
  artifacts:
    - path: "src/kajiba/scrubber_semantic.py"
      provides: "GLiNER Layer-C detector: band classification, asymmetric composition, deep-copy scrub, soft-import"
      contains: "nvidia/gliner-PII"
    - path: "src/kajiba/scrubber_llm.py"
      provides: "Retired stub re-pointed/deprecated toward scrubber_semantic"
  key_links:
    - from: "src/kajiba/scrubber_semantic.py"
      to: "kajiba.scrubber.FlaggedItem"
      via: "reuse the existing flagged-item container (D-08)"
      pattern: "from kajiba.scrubber import.*FlaggedItem"
    - from: "src/kajiba/scrubber_semantic.py"
      to: "gliner.GLiNER.from_pretrained"
      via: "soft-import singleton with within-run cache"
      pattern: "from gliner import GLiNER"
---

<objective>
Replace the `scrubber_llm.py` stub with a real GLiNER semantic PII layer
(`scrubber_semantic.py`). This plan makes Success Criteria #4 and #5 implementable:
GLiNER detects personal/company/project/location names with float scores, buckets them
into the locked confidence bands (D-05), applies asymmetric coverage (D-07: redact in
prose, flag-only in tool fields), and passes the D-06 calibration hard gate (zero
auto-redact on code identifiers). The module composes AFTER regex scrub (D-11) on a deep
copy, reusing the existing `FlaggedItem` container so flags flow into the existing
preview channel (wired in 07-05).

Purpose: The semantic privacy layer — the second of the two scrub layers the phase goal
requires. Core stays import-clean and offline; GLiNER lives behind the `[llm-scrub]`
extra.
Output: `scrubber_semantic.py` with band/asymmetric/composition logic + soft-import;
retired `scrubber_llm.py`; 07-02 semantic scaffolds turn GREEN (LANE A always; LANE B
when `[llm-scrub]` installed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-turn-capture-semantic-pii-scrubbing/07-RESEARCH.md
@.planning/phases/07-turn-capture-semantic-pii-scrubbing/07-PATTERNS.md
@src/kajiba/scrubber.py
@src/kajiba/scrubber_llm.py
@src/kajiba/schema.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GLiNER detector + band classification + soft-import (PRIV-01/02)</name>
  <read_first>
    - src/kajiba/scrubber.py lines 150-159 (`FlaggedItem` to reuse), lines 20-28 (`[REDACTED_*]` placeholder convention), lines 314-370 (`scrub_record` deep-copy discipline to mirror)
    - src/kajiba/collector.py lines 104-117 (psutil soft-import block — the soft-dep template)
    - src/kajiba/scrubber_llm.py (obsolete string-confidence stub being replaced)
    - 07-RESEARCH.md § Pattern 5 (load/inference; threshold-once-at-0.4 strategy; CUDA-with-CPU-fallback device), § Code Examples (band bucketing), § Common Pitfalls 3 (capital PII) and 5 (soft-import)
    - 07-PATTERNS.md § scrubber_semantic.py (mirror containers, replacement tag, within-run singleton)
  </read_first>
  <behavior>
    - `bands`: given synthetic spans `{start,end,text,label,score}`, the band classifier returns redactions for `score>=0.7`, flags for `0.4<=score<0.7`, and ignores `score<0.4` (D-05).
    - `soft_import`: `import kajiba.scrubber_semantic` succeeds WITHOUT gliner/torch installed; invoking the detector path without the extra raises the defined `SemanticScrubUnavailable` (not `ModuleNotFoundError`).
    - `detect` (LANE B, importorskip): `_get_model()` loads `nvidia/gliner-PII` (capital PII) and `predict_entities(text, LABELS, threshold=0.4)` returns spans with float `score`; the true-positive prose seeds yield person/company spans.
  </behavior>
  <action>
    Create `src/kajiba/scrubber_semantic.py`. Define a module-level `LABELS` from D-04
    (`person`, `company`, `organization`, `project`, `location`) and band constants matching D-05
    (`REDACT_THRESHOLD=0.7`, `FLAG_THRESHOLD=0.4`). Define a `SemanticScrubUnavailable(Exception)` class.
    Add a `_get_model()` singleton: `global _MODEL`; on first call soft-import `from gliner import GLiNER`
    and `import torch` inside the function (mirror the psutil block) — catch `ImportError` and raise
    `SemanticScrubUnavailable`; select device `"cuda" if torch.cuda.is_available() else "cpu"`; load
    `GLiNER.from_pretrained("nvidia/gliner-PII")` (CAPITAL PII — Correction 1/Pitfall 3) `.to(device)`;
    cache in `_MODEL` (within-run cache so GLiNER loads once per CLI invocation, D-09). Add
    `detect_entities(text, threshold=0.4)` that returns `[]` for empty text else
    `model.predict_entities(text, LABELS, threshold=threshold)` — ONE inference pass at the 0.4 flag floor
    (Pattern 5 threshold strategy). Add a pure band-classification helper (no model) that takes a list of
    span dicts and returns `(redactions, flags)` by the D-05 float bands. Use `[REDACTED_NAME]` /
    `[REDACTED_PERSON]` style placeholders matching the `scrubber.py` convention so the existing
    `\[REDACTED_\w+\]` highlight regex catches them. Import `FlaggedItem` from `kajiba.scrubber` (do NOT
    redefine). Soft-import only — NO top-level `import gliner`/`import torch`. `Optional[...]` typing,
    Google-style docstrings, module logger per CLAUDE.md.
  </action>
  <acceptance_criteria>
    - Behavior assertion: `python -m pytest tests/test_scrubber_semantic.py -k "bands or soft_import" -x` PASSES (07-02 LANE A scaffolds now GREEN).
    - Source assertion: `scrubber_semantic.py` uses the literal `nvidia/gliner-PII` (capital PII), never lowercase.
    - Source assertion: `from gliner import GLiNER` and `import torch` appear only INSIDE a function (no module-top ML imports); `import kajiba.scrubber_semantic` succeeds without the extra.
    - Source assertion: `SemanticScrubUnavailable` is defined and raised on `ImportError` of gliner.
    - Source assertion: `FlaggedItem` is imported from `kajiba.scrubber` (not redefined in this module).
    - Behavior assertion: `python -c "import kajiba.scrubber_semantic"` exits 0 without gliner/torch installed.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_scrubber_semantic.py -k "bands or soft_import" -x && python -c "import kajiba.scrubber_semantic; print('import-clean')"</automated>
  </verify>
  <done>GLiNER detector loads capital-PII model via soft-import singleton; band classifier honors D-05; core import-clean; LANE A band/soft_import tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Asymmetric composition (scrub_record_semantic) + calibration gate + retire stub (PRIV-03, D-06/07/11)</name>
  <read_first>
    - src/kajiba/scrubber.py lines 314-370 (`scrub_record` returns `(record, ScrubLog)` via `model_dump`→mutate→`model_validate`; the composition surface Layer C runs after) and lines 357-366 (`ScrubLog` assembly: `potential_names_redacted`, `items_flagged`)
    - src/kajiba/schema.py lines 135-144 (ConversationTurn) and 125-132 (ToolCall — `tool_input`/`tool_output`)
    - 07-RESEARCH.md § Pattern 6 (asymmetric composition + the `scrub_record_semantic` shape), § Pattern 7 (`FlaggedItem.reason` = `GLiNER {label} (confidence {score:.2f})`), § Validation Architecture (calibration test shape)
    - tests/fixtures/code_content_pii.json (07-02) and the KNOWN_SAFE_TOKENS list
    - src/kajiba/scrubber_llm.py (the stub to retire)
  </read_first>
  <behavior>
    - `asymmetric`: identical high-confidence spans (`score>=0.7`) in a `ConversationTurn.value` get REDACTED (text replaced with `[REDACTED_*]`) while the SAME spans in `tool_input`/`tool_output` are FLAG-ONLY (text byte-unchanged) regardless of confidence (D-07). 0.4-0.7 spans flag in both.
    - `calibration` (LANE B): on the code fixture, the set of auto-redactions (`score>=0.7`) across ALL fields is EMPTY (D-06 hard gate); flag-band hits are allowed; the observed FP rate is recorded.
    - Composition: `scrub_record_semantic` runs on the already-regex-scrubbed record, returns `(record, names_redacted_count, list[FlaggedItem])`, mirrors the deep-copy discipline, and never mutates the input record.
  </behavior>
  <action>
    Add `scrub_record_semantic(record) -> tuple[KajibaRecord, int, list[FlaggedItem]]` to
    `scrubber_semantic.py` (RESEARCH Pattern 6). Mirror `scrub_record`'s deep-copy discipline:
    `record.model_dump(by_alias=True)` → mutate the copy → `KajibaRecord.model_validate(data)`. For each
    `ConversationTurn.value`: run `detect_entities`, REDACT spans with `score>=0.7` (replace span text with
    a `[REDACTED_*]` placeholder by label; person→`[REDACTED_PERSON]`, others→`[REDACTED_NAME]`) and
    collect `0.4<=score<0.7` spans as `FlaggedItem`s. For `tool_input`/`tool_output`: collect ALL spans
    with `score>=0.4` as `FlaggedItem`s ONLY — NEVER mutate the text (D-07 asymmetric, the core anti-
    corruption rule). Build each `FlaggedItem` with `reason=f"GLiNER {label} (confidence {score:.2f})"`
    (Pattern 7) so the preview panel shows snippet+label+confidence. Count person-label redactions for
    `potential_names_redacted`. Wrap GLiNER access so a missing extra surfaces `SemanticScrubUnavailable`
    to the caller (07-05 catches it). Implement the calibration gate test logic against
    `code_content_pii.json`: assert ZERO `score>=0.7` spans on the code identifiers (hard gate) and record
    the FP rate (`len(flag_band_hits)/len(KNOWN_SAFE_TOKENS)`) as a recorded artifact value. Finally,
    RETIRE `scrubber_llm.py`: re-point it to deprecate toward `scrubber_semantic` (keep the file so any
    import path does not hard-break — e.g. raise a clear deprecation/`SemanticScrubUnavailable`-style
    message or re-export the new entry points) and remove the dead `model_fn`/string-confidence interface.
    Do NOT modify `scrubber.py` (Layer B stays unchanged — compose-with, not modify).
  </action>
  <acceptance_criteria>
    - Behavior assertion: `python -m pytest tests/test_scrubber_semantic.py -k "asymmetric" -x` PASSES (LANE A asymmetric scaffold GREEN).
    - Behavior assertion (LANE B, when `[llm-scrub]` installed): `python -m pytest tests/test_scrubber_semantic.py -k "calibration or detect" -x` PASSES; without the extra it SKIPS (importorskip), and the core suite stays green.
    - Source assertion: in `scrub_record_semantic`, the `tool_input`/`tool_output` branch appends `FlaggedItem`s and NEVER assigns a `[REDACTED_*]` placeholder into tool field text (asymmetric coverage — grep the function: redaction-text-assignment occurs only under the turn-value branch).
    - Source assertion: `scrub_record_semantic` uses `model_dump(by_alias=True)`→`KajibaRecord.model_validate(...)` (deep-copy discipline; input record not mutated).
    - Source assertion: `FlaggedItem.reason` is built as `GLiNER {label} (confidence {score:.2f})`.
    - Source assertion: `scrubber_llm.py` no longer exposes the `model_fn`/`SemanticRedaction(confidence: str)` interface (the obsolete string-confidence shape is gone).
    - Source assertion: `src/kajiba/scrubber.py` is unchanged (`git diff --quiet src/kajiba/scrubber.py`).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_scrubber_semantic.py -k "asymmetric" -x && git diff --quiet src/kajiba/scrubber.py && echo "scrubber.py untouched"</automated>
  </verify>
  <done>Asymmetric composition redacts prose ≥0.7 / flags tool fields only; calibration hard gate enforced; stub retired; Layer B untouched.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- **src/kajiba/scrubber_semantic.py** (NEW): `LABELS`, `REDACT_THRESHOLD`/`FLAG_THRESHOLD`, `SemanticScrubUnavailable`, `_get_model` (GLiNER singleton, capital-PII), `detect_entities`, band-classification helper, `scrub_record_semantic(record) -> (record, names_redacted, list[FlaggedItem])`.
- **src/kajiba/scrubber_llm.py**: retired/deprecated toward `scrubber_semantic` (dead `model_fn`/string-confidence interface removed).
- **tests/test_scrubber_semantic.py**: LANE A (`bands`,`asymmetric`,`soft_import`) GREEN; LANE B (`detect`,`calibration`) GREEN when `[llm-scrub]` installed, skipped otherwise. Recorded FP-rate artifact value (D-06).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted session content → semantic scrubber | User/assistant prose + tool args/results flow into GLiNER detection |
| HuggingFace model download → local cache | `nvidia/gliner-PII` weights fetched on first run (LANE B / live only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Information Disclosure | PII leak (names in prose) | mitigate | GLiNER `score>=0.7` auto-redacts person/company/project/location in `ConversationTurn.value`; flag band routes 0.4-0.7 to human review (D-05). |
| T-07-09 | Tampering | code training data corruption | mitigate | D-07 asymmetric coverage (tool/code fields FLAG-only, never auto-redacted) + D-06 calibration gate (zero ≥0.7 on code identifiers). Validated by `asymmetric` + `calibration` tests. |
| T-07-10 | Tampering | model supply chain | mitigate | `nvidia/gliner-PII` is the PRIV-01-named model (slopcheck-clean loader); pinned via `[llm-scrub]`; weights download only on opt-in extra; capital-PII repo id avoids resolving an unexpected/typo-squatted lowercase repo. |
| T-07-11 | Tampering | core import surface | mitigate | gliner/torch soft-imported inside functions; `SemanticScrubUnavailable` fallback keeps `kajiba preview` degrading gracefully without the extra (Task 1 verify). |
</threat_model>

<verification>
- `python -m pytest tests/test_scrubber_semantic.py` — LANE A GREEN, LANE B skipped (no extra) or GREEN (extra installed); no collection errors.
- `git diff --quiet src/kajiba/scrubber.py` — Layer B untouched.
- `python -c "import kajiba.scrubber_semantic, kajiba.scrubber_llm"` succeeds without `[llm-scrub]`.
- Full suite: `python -m pytest` (wave-merge gate) — no regressions.
</verification>

<success_criteria>
- SC#4 (implementable): GLiNER-detected person/company/project names redact in conversation prose (wired to preview in 07-05).
- SC#5 (implementable): 0.4-0.7 entities flagged for review rather than auto-redacted.
- D-06 calibration hard gate enforced; D-07 asymmetric coverage enforced; D-11 compose-after-regex; core import-clean.
</success_criteria>

<output>
Create `.planning/phases/07-turn-capture-semantic-pii-scrubbing/07-04-SUMMARY.md` when done
</output>
