---
phase: 07-turn-capture-semantic-pii-scrubbing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/test_collector.py
  - tests/test_plugin.py
autonomous: true
requirements: [CAPT-02, CAPT-03, CAPT-04, PRIV-04]
must_haves:
  truths:
    - "Core kajiba package imports clean without [llm-scrub] installed"
    - "Capture test scaffolds exist and FAIL on not-yet-built collector behavior (RED)"
    - "[llm-scrub] extra lists gliner, torch, transformers, ollama"
  artifacts:
    - path: "pyproject.toml"
      provides: "Populated [llm-scrub] optional-dependencies extra (D-10/PRIV-04)"
      contains: "gliner"
    - path: "tests/test_collector.py"
      provides: "RED scaffolds for paired-turn, tool-buffer, session-end-once, ollama-metadata, remote-degrade"
      contains: "llm_turn"
    - path: "tests/test_plugin.py"
      provides: "RED scaffolds proving promoted hooks dispatch to collector (not debug-log only)"
  key_links:
    - from: "pyproject.toml"
      to: "pip install kajiba[llm-scrub]"
      via: "optional-dependencies table"
      pattern: "llm-scrub\\s*=\\s*\\["
---

<objective>
Lay the Wave 0 foundation for the capture half of Phase 7: populate the `[llm-scrub]`
packaging extra (PRIV-04/D-10) and author the RED test scaffolds that pin the
collector + plugin-hook capture behavior before any implementation exists. These tests
encode the four CRITICAL CORRECTIONS (one staging file per session, `ok`→`success`,
no double-count, ollama soft-import) as executable assertions so the implementation
plans (07-03) cannot reintroduce the bugs.

Purpose: Nyquist compliance — every capture requirement gets an automated test that
exists and is RED before implementation. Packaging must land early so the GLiNER lane
(07-02) can install the extra.
Output: Filled `[llm-scrub]` extra; failing `tests/test_collector.py` +
`tests/test_plugin.py` capture scaffolds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-environment-plugin-foundation/06-HOOK-KWARGS.md
@.planning/phases/07-turn-capture-semantic-pii-scrubbing/07-VALIDATION.md
@.planning/phases/07-turn-capture-semantic-pii-scrubbing/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate the [llm-scrub] extra in pyproject.toml (PRIV-04/D-10)</name>
  <read_first>
    - pyproject.toml lines 31-42 (the `[project.optional-dependencies]` block; `llm-scrub = []` on line 33 is the fill target; `upload`/`dev` blocks are the shape analog)
    - 07-RESEARCH.md § Standard Stack (verified versions) and § Package Legitimacy Audit (slopcheck OK for all four)
    - 07-PATTERNS.md § pyproject.toml (fill pattern)
  </read_first>
  <action>
    Replace the empty `llm-scrub = []` with the four verified packages from RESEARCH §Standard Stack,
    using lower-bound pins consistent with the existing `upload`/`dev` style: `gliner>=0.2.26`,
    `torch>=2.12.0`, `transformers>=5.0`, `ollama>=0.6.2`. Do NOT add these to the core `dependencies`
    array (core must stay import-clean and offline — D-10). Leave the `all` aggregate extra untouched
    (it already references `kajiba[llm-scrub]`). Do NOT install the packages in this task — packaging
    declaration only; the GLiNER install lane is handled in 07-02 and the live run in 07-05.
    These packages passed slopcheck (4 OK, RESEARCH §Package Legitimacy Audit) so no legitimacy
    checkpoint is required.
  </action>
  <acceptance_criteria>
    - Source assertion: `pyproject.toml` `[project.optional-dependencies] llm-scrub` lists exactly `gliner`, `torch`, `transformers`, `ollama` (each with a `>=` pin).
    - Source assertion: the core `[project] dependencies` array still contains only `pydantic`, `click`, `rich` (no ML deps leaked into core).
    - Behavior assertion: `python -c "import kajiba; import kajiba.cli; import kajiba.scrubber; import kajiba.collector"` exits 0 WITHOUT gliner/torch/ollama installed (core import-clean).
  </acceptance_criteria>
  <verify>
    <automated>python -c "import tomllib,sys; d=tomllib.load(open('pyproject.toml','rb')); e=d['project']['optional-dependencies']['llm-scrub']; names={p.split('>=')[0].split('[')[0].strip() for p in e}; assert {'gliner','torch','transformers','ollama'}<=names, names; assert set(d['project']['dependencies'])=={p for p in d['project']['dependencies'] if not any(x in p for x in ('gliner','torch','ollama'))}; print('ok')"</automated>
  </verify>
  <done>`[llm-scrub]` extra lists gliner/torch/transformers/ollama; core imports clean without them.</done>
</task>

<task type="auto">
  <name>Task 2: RED capture scaffolds in test_collector.py (CAPT-02/03/04)</name>
  <read_first>
    - tests/test_collector.py (existing structure: `_make_record`/fixture helpers, how `on_turn_complete`/`on_session_start`/`on_session_end` are exercised today)
    - src/kajiba/collector.py lines 172-336 (`__init__`, `on_session_start`, `on_turn_complete`, `on_session_end`) and lines 379-435 (`_build_record`, `_save_to_staging`)
    - 06-HOOK-KWARGS.md § Phase 7 implications (turn + tool kwarg → collector input tables)
    - 07-RESEARCH.md § Pattern 1, Pattern 2, Pattern 4, and the "Turn-Scoped on_session_end Fix" section
    - 07-VALIDATION.md Per-Requirement Verification Map (exact `-k` test selectors)
  </read_first>
  <action>
    Add failing tests that target the not-yet-built collector capture surface using the EXACT `-k`
    selectors from 07-VALIDATION.md so the validation map resolves. Author these test groups:
    (1) `llm_turn` — calling the new paired-turn entry point with `user_message`+`assistant_response`+`turn_id`
        appends exactly one `human` turn then one `gpt` turn (CAPT-02); passing a populated
        `conversation_history` does NOT add extra turns (no double-count — Correction 4).
    (2) `tool_buffer` — a tool event keyed by `turn_id` with `status="ok"` produces a `ToolCall` with
        `tool_status == "success"` (Correction 2) attached to that turn's `gpt` turn; a duplicate
        `tool_call_id` is deduped to one ToolCall; `result` (a JSON string per 06-HOOK-KWARGS finding 3)
        is parsed before storage; `args` (already a dict) is serialized into `tool_input`.
    (3) `session_end_once` — N `on_session_end` firings for one `session_id` (turn-scoped, finding 2)
        result in EXACTLY ONE `session_{id}.json` staging file (Correction 3), and the final file
        reflects the full accumulated trajectory (turn_count grows, does not reset).
    (4) `ollama_metadata` — with `ollama.show()` monkeypatched to return the documented shape
        (`details.parameter_size`/`quantization_level`/`family`, `model_info["<arch>.context_length"]`,
        `digest`), the captured `ModelMetadata` carries non-null `parameter_count`/`quantization`/
        `model_family`/`context_window`/`model_hash` (CAPT-04, mocked).
    (5) `remote_degrade` — with ollama import absent/`ollama.show` raising, a remote model
        (e.g. an Anthropic/openrouter slug) yields `is_local=false`, slug-inferred `provider`/`model_family`,
        `parameter_count`/`quantization`/`model_hash` left None, and `HardwareProfile.inference_backend`
        set (D-03). No exception propagates.
    Tests must reference symbols/behaviors that DO NOT YET EXIST so they fail at collection or assertion
    (RED). Use monkeypatch for ollama; do NOT require the real `[llm-scrub]` extra for these collector
    tests (ollama is mocked). Keep existing `on_turn_complete` tests untouched (back-compat must survive).
    Mark the live-only D-02 path out of scope here (it is 07-05, manual).
  </action>
  <acceptance_criteria>
    - Behavior assertion: `python -m pytest tests/test_collector.py -k "llm_turn or tool_buffer or session_end_once or ollama_metadata or remote_degrade" -x` FAILS (RED) — the targeted collector behavior is unimplemented.
    - Source assertion: each of the five selectors (`llm_turn`, `tool_buffer`, `session_end_once`, `ollama_metadata`, `remote_degrade`) matches at least one test (collection is non-empty for each `-k`).
    - Behavior assertion: pre-existing `tests/test_collector.py` tests for `on_turn_complete`/`on_session_start` still PASS (no regression from scaffolding).
    - Source assertion: a `session_end_once` test asserts a file COUNT of exactly 1 in the staging dir (not just "a file exists").
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_collector.py -k "llm_turn or tool_buffer or session_end_once or ollama_metadata or remote_degrade" -x; if [ $? -eq 0 ]; then echo "ERROR: expected RED"; exit 1; else echo "RED confirmed"; fi</automated>
  </verify>
  <done>Five capture test groups exist with the exact validation-map selectors and are RED; back-compat tests still green.</done>
</task>

<task type="auto">
  <name>Task 3: RED hook-dispatch scaffolds in test_plugin.py</name>
  <read_first>
    - tests/test_plugin.py (existing `StubCtx` + register/kwargs/debug tests; the patterns for installing a fake collector)
    - src/kajiba/plugin/hooks.py lines 87-114 (the `on_post_llm_call`/`on_post_tool_call` debug-only stubs to promote) and lines 64-84 (`on_session_start` dispatch shell to mirror)
    - 06-HOOK-KWARGS.md Hook 2 + Hook 3 kwarg tables (`user_message`, `assistant_response`, `turn_id`; `tool_name`, `args`, `result`, `tool_call_id`, `status`)
  </read_first>
  <action>
    Add failing tests asserting that the promoted `on_post_llm_call` and `on_post_tool_call` hooks
    DISPATCH to the collector (currently they only `_log_kwargs`). Install a spy/stub collector via
    `set_collector` and assert: calling `on_post_llm_call(**post_llm_kwargs)` invokes the collector's
    new paired-turn method with `user_message`/`assistant_response`/`turn_id` extracted from kwargs;
    calling `on_post_tool_call(**post_tool_kwargs)` invokes the collector's new tool-ingest method with
    `tool_name`/`args`/`result`/`tool_call_id`/`turn_id`/`status`. Also assert the fault-tolerant
    contract survives: a collector method that raises does NOT propagate out of the hook (Hermes is
    never disrupted). Use the live kwarg names from 06-HOOK-KWARGS verbatim. These tests reference
    collector methods that do not yet exist → RED. Do not modify `hooks.py` in this task.
  </action>
  <acceptance_criteria>
    - Behavior assertion: `python -m pytest tests/test_plugin.py -k "post_llm or post_tool or dispatch or fault" -x` FAILS (RED) — promoted dispatch unimplemented.
    - Source assertion: a test asserts the collector paired-turn method is called with kwargs sourced from `user_message`/`assistant_response`/`turn_id`.
    - Source assertion: a test asserts a raising collector method does NOT propagate out of `on_post_llm_call`/`on_post_tool_call` (try/except contract).
    - Behavior assertion: the three pre-existing test_plugin.py tests (register/kwargs/debug) still PASS.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_plugin.py -k "post_llm or post_tool or dispatch or fault" -x; if [ $? -eq 0 ]; then echo "ERROR: expected RED"; exit 1; else echo "RED confirmed"; fi</automated>
  </verify>
  <done>Hook-dispatch scaffolds assert collector invocation + fault tolerance and are RED; existing plugin tests green.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- **pyproject.toml**: populated `[llm-scrub]` extra (`gliner`, `torch`, `transformers`, `ollama`).
- **tests/test_collector.py** (extended): RED test groups `llm_turn`, `tool_buffer`, `session_end_once`, `ollama_metadata`, `remote_degrade`.
- **tests/test_plugin.py** (extended): RED hook-dispatch + fault-tolerance scaffolds for promoted `on_post_llm_call`/`on_post_tool_call`.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supply chain → dev/CI env | New ML packages (gliner/torch/transformers/ollama) declared as installable extra |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | core import surface | mitigate | ML deps go in `[llm-scrub]` extra only, never core `dependencies`; import-clean test (Task 1 verify) proves core stays offline. |
| T-07-SC | Tampering | pip installs (gliner/torch/transformers/ollama) | mitigate | RESEARCH §Package Legitimacy Audit ran slopcheck (4 OK); pinned to verified PyPI versions. No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages → no blocking-human legitimacy checkpoint required. Declaration-only task installs nothing. |
</threat_model>

<verification>
- `python -m pytest tests/test_collector.py tests/test_plugin.py -k "llm_turn or tool_buffer or session_end_once or ollama_metadata or remote_degrade or post_llm or post_tool or dispatch or fault"` is RED (expected pre-implementation).
- Full suite excluding these new RED selectors stays green: `python -m pytest -k "not (llm_turn or tool_buffer or session_end_once or ollama_metadata or remote_degrade or post_llm or post_tool or dispatch or fault)"` passes.
- `python -c "import tomllib; tomllib.load(open('pyproject.toml','rb'))"` parses.
</verification>

<success_criteria>
- `[llm-scrub]` extra populated with the four verified packages; core import-clean.
- Capture + hook RED scaffolds exist with the exact 07-VALIDATION selectors and fail on unimplemented behavior.
- No regression in pre-existing collector/plugin tests.
</success_criteria>

<output>
Create `.planning/phases/07-turn-capture-semantic-pii-scrubbing/07-01-SUMMARY.md` when done
</output>
