---
phase: 07-turn-capture-semantic-pii-scrubbing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/code_content_pii.json
  - tests/test_scrubber_semantic.py
autonomous: true
requirements: [PRIV-01, PRIV-02, PRIV-03]
must_haves:
  truths:
    - "A code-content calibration fixture of known-safe identifiers exists for the D-06 hard gate"
    - "Semantic-scrub test scaffolds (bands, asymmetric, calibration, soft-import) exist and are RED"
    - "GLiNER-dependent tests skip gracefully without [llm-scrub] installed"
  artifacts:
    - path: "tests/fixtures/code_content_pii.json"
      provides: "D-06 calibration fixture: known-safe code identifiers + a few real names for true-positive proof"
      contains: "pandas"
    - path: "tests/test_scrubber_semantic.py"
      provides: "RED scaffolds for band logic, asymmetric coverage, calibration gate, soft-import fallback"
      contains: "calibration"
  key_links:
    - from: "tests/test_scrubber_semantic.py"
      to: "tests/fixtures/code_content_pii.json"
      via: "fixture load for calibration gate"
      pattern: "code_content_pii"
---

<objective>
Lay the Wave 0 foundation for the semantic-scrubbing half of Phase 7: build the D-06
calibration fixture and the RED test scaffolds for `scrubber_semantic` (band bucketing,
asymmetric coverage, the calibration hard gate, and soft-import fallback). These tests
encode the locked confidence bands (D-05), asymmetric coverage (D-07), and the
zero-auto-redact calibration gate (D-06) before the module exists, so the implementation
plan (07-04) builds to a pinned contract.

Purpose: Nyquist compliance for the privacy layer; the calibration gate is a HARD GATE
(zero auto-redacts on code identifiers) and must be expressed as a test that fails until
the asymmetric + band logic is correct.
Output: `tests/fixtures/code_content_pii.json`; failing `tests/test_scrubber_semantic.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-turn-capture-semantic-pii-scrubbing/07-RESEARCH.md
@.planning/phases/07-turn-capture-semantic-pii-scrubbing/07-VALIDATION.md
@.planning/phases/07-turn-capture-semantic-pii-scrubbing/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the D-06 code-content calibration fixture</name>
  <read_first>
    - tests/fixtures/gold_trajectory.json and tests/fixtures/pii_trajectory.json (existing fixture shape — a KajibaRecord JSON with `trajectory.conversations[].value` and tool_calls)
    - 07-RESEARCH.md § Validation Architecture "Calibration-Gate Test Shape (D-06)" and § Wave 0 Gaps
    - 07-CONTEXT.md D-06 (the hard gate: zero auto-redact ≥0.7 on known-safe identifiers; flag-band hits allowed; record FP rate)
    - src/kajiba/schema.py lines 125-163 (ToolCall, ConversationTurn, Trajectory required fields so the fixture validates)
  </read_first>
  <action>
    Create `tests/fixtures/code_content_pii.json` as a valid KajibaRecord JSON whose conversation
    turns embed KNOWN-SAFE code identifiers that a generalist NER tends to over-fire on: library
    names (`pandas`, `React`, `numpy`), framework tokens, capitalized class/function names
    (`def compute_quality_score(record):`, `const App = () => <React.Fragment/>`, `import pandas as pd`),
    and ordinary variable names. Include BOTH a `ConversationTurn.value` block of such code AND a
    `tool_input`/`tool_output` block of code (so the asymmetric-coverage test in 07-04 has tool-field
    code to assert flag-only on). ALSO seed a small number of genuine person/company names in prose
    (e.g. "reviewed by <a real-looking name> at <a company>") so the true-positive detection test can
    prove GLiNER still fires on actual PII. Keep the file minimal but schema-valid (it must load via
    `KajibaRecord.model_validate`/`validate_record`). Record (in a fixture comment field or sibling
    constant in the test) the explicit list of KNOWN_SAFE_TOKENS used for the FP-rate denominator.
  </action>
  <acceptance_criteria>
    - Source assertion: `tests/fixtures/code_content_pii.json` parses as JSON and loads via `kajiba.schema.validate_record` (or `KajibaRecord.model_validate`) without error.
    - Source assertion: the fixture contains at least one `ConversationTurn.value` with code identifiers AND at least one tool_call with code in `tool_input`/`tool_output`.
    - Source assertion: the fixture contains at least one genuine person/company name in prose (true-positive seed).
    - Behavior assertion: `python -c "import json,kajiba.schema as s; s.validate_record(json.load(open('tests/fixtures/code_content_pii.json')))"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python -c "import json,kajiba.schema as s; r=s.validate_record(json.load(open('tests/fixtures/code_content_pii.json'))); assert r.trajectory.conversations; print('ok')"</automated>
  </verify>
  <done>Schema-valid calibration fixture exists with code identifiers in both turn-value and tool fields plus true-positive name seeds.</done>
</task>

<task type="auto">
  <name>Task 2: RED scaffolds in test_scrubber_semantic.py (PRIV-01/02/03, D-05/06/07)</name>
  <read_first>
    - src/kajiba/scrubber.py lines 150-159 (`FlaggedItem` dataclass to reuse) and lines 314-370 (`scrub_record` deep-copy discipline the new module mirrors)
    - src/kajiba/scrubber_llm.py (the stub being replaced; obsolete string-confidence shape)
    - 07-RESEARCH.md § Pattern 5 (GLiNER load/inference), § Pattern 6 (asymmetric composition), § Code Examples (band bucketing), § Validation Architecture (calibration test shape, `importorskip` guard)
    - 07-VALIDATION.md Per-Requirement Verification Map (exact `-k` selectors: detect, bands, calibration, asymmetric, soft_import)
    - tests/fixtures/code_content_pii.json (Task 1 output)
  </read_first>
  <action>
    Add failing tests to a new `tests/test_scrubber_semantic.py` using the EXACT `-k` selectors from
    07-VALIDATION.md. Split into two lanes:
    LANE A — pure-logic tests that DO NOT need the model (no `[llm-scrub]`): feed synthetic span dicts
    (`{"start","end","text","label","score"}`) to the band-classification + composition helpers and assert
    (`bands`) ≥0.7 → redact, 0.4≤score<0.7 → flag, <0.4 → ignore (D-05); (`asymmetric`) given identical
    spans in a `ConversationTurn.value` vs in `tool_input`/`tool_output`, the turn-value path redacts ≥0.7
    AND flags 0.4–0.7 while the tool-field path produces FLAGS ONLY and NEVER mutates text (D-07);
    (`soft_import`) when gliner is unavailable the module raises `SemanticScrubUnavailable` (a defined
    exception) rather than `ModuleNotFoundError`, and `import kajiba.scrubber_semantic` itself succeeds
    without the extra. These reference the new module's not-yet-existing public surface → RED.
    LANE B — model-dependent integration tests guarded by `pytest.importorskip("gliner")`: (`detect`)
    loads `nvidia/gliner-PII` (CAPITAL PII — Correction 1) and detects person/company/project/location on
    the true-positive prose seeds; (`calibration`) the D-06 HARD GATE — on `code_content_pii.json` code
    text, the set of spans with `score>=0.7` is EMPTY (zero auto-redact), flag-band hits are allowed, and
    the observed FP rate is recorded (e.g. via a printed/recorded artifact value). LANE B skips cleanly
    when the extra is absent so the core suite stays green.
    Do NOT create `scrubber_semantic.py` here — that is 07-04. Reference `FlaggedItem` from
    `kajiba.scrubber` (do not redefine).
  </action>
  <acceptance_criteria>
    - Behavior assertion: `python -m pytest tests/test_scrubber_semantic.py -k "bands or asymmetric or soft_import" -x` FAILS (RED) — `kajiba.scrubber_semantic` public surface unimplemented.
    - Source assertion: each selector `detect`, `bands`, `calibration`, `asymmetric`, `soft_import` matches at least one test.
    - Source assertion: LANE B tests (`detect`, `calibration`) are guarded by `pytest.importorskip("gliner")` so they SKIP (not fail) without `[llm-scrub]`.
    - Source assertion: the calibration test asserts the `score>=0.7` span set is empty on the code fixture (hard gate) and records an FP-rate value.
    - Source assertion: the model id string used in LANE B is exactly `nvidia/gliner-PII` (capital PII), not lowercase.
    - Behavior assertion: `python -m pytest tests/test_scrubber_semantic.py` does NOT error on collection when gliner is absent (LANE B skipped, LANE A RED).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_scrubber_semantic.py -k "bands or asymmetric or soft_import" -x; if [ $? -eq 0 ]; then echo "ERROR: expected RED"; exit 1; else echo "RED confirmed"; fi</automated>
  </verify>
  <done>Semantic-scrub RED scaffolds exist with exact selectors; LANE B importorskip-guarded; calibration gate asserts zero ≥0.7 on code; uses capital `nvidia/gliner-PII`.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- **tests/fixtures/code_content_pii.json**: D-06 calibration fixture (code identifiers in turn-value + tool fields, plus true-positive name seeds).
- **tests/test_scrubber_semantic.py** (new): RED scaffolds — LANE A pure-logic (`bands`, `asymmetric`, `soft_import`) and LANE B model-gated (`detect`, `calibration`) with `importorskip` guard.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted session content → scrubber | Code/prose text (from fixtures here; from live capture in 07-03) flows into the calibration tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Tampering | code training data | mitigate | Calibration gate test asserts ZERO auto-redact (≥0.7) on known-safe code identifiers (D-06), preventing silent corruption of code in training data. |
| T-07-03 | Information Disclosure | true-positive PII detection | mitigate | `detect` test proves GLiNER still fires on genuine person/company names so the privacy property (PII leak prevention) is not lost while limiting false positives. |
</threat_model>

<verification>
- `python -m pytest tests/test_scrubber_semantic.py -k "bands or asymmetric or soft_import"` is RED (expected pre-implementation).
- `python -m pytest tests/test_scrubber_semantic.py` collects without error when gliner is absent (LANE B skipped).
- Calibration fixture loads via `validate_record`.
- Full suite excluding the new semantic selectors stays green.
</verification>

<success_criteria>
- D-06 calibration fixture exists and is schema-valid.
- Semantic RED scaffolds exist with the exact 07-VALIDATION selectors; model-dependent lane skips gracefully without the extra.
- Calibration gate, band logic, and asymmetric coverage are all expressed as tests.
</success_criteria>

<output>
Create `.planning/phases/07-turn-capture-semantic-pii-scrubbing/07-02-SUMMARY.md` when done
</output>
