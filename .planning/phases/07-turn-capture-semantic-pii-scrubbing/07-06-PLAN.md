---
phase: 07-turn-capture-semantic-pii-scrubbing
plan: 06
type: execute
wave: 4
depends_on: ["07-03", "07-04", "07-05"]
files_modified:
  - .planning/phases/07-turn-capture-semantic-pii-scrubbing/07-LIVE-CAPTURE.md
  - docs/hermes-setup.md
autonomous: false
requirements: [CAPT-04, PRIV-01]
user_setup:
  - service: ollama
    why: "Local model metadata capture (CAPT-04) and the D-02 live-capture proof on real data"
    env_vars: []
    dashboard_config:
      - task: "Install Ollama for Windows and pull the Hermes 3 8B Q4 model"
        location: "https://ollama.com/download — then `ollama pull` the Hermes 3 8B Q4 tag"
      - task: "Configure Hermes v0.15.1 to use the local Ollama backend for one session"
        location: "Hermes config / endpoint setup per docs/hermes-setup.md optional Ollama appendix"
must_haves:
  truths:
    - "A real local-Ollama Hermes session (Hermes 3 8B Q4) is captured into exactly one staging record"
    - "The captured staging record carries non-null parameter_count, quantization, model_family, context_window from ollama.show()"
    - "kajiba preview on the live record shows GLiNER redactions/flags (Layer C runs on real data)"
    - "The staging JSON is archived as the phase artifact proving SC#3 on real data"
  artifacts:
    - path: ".planning/phases/07-turn-capture-semantic-pii-scrubbing/07-LIVE-CAPTURE.md"
      provides: "Documented walkthrough + the archived live staging JSON + observed metadata/GLiNER results (D-02 proof)"
      contains: "parameter_count"
  key_links:
    - from: "live Hermes session (Ollama backend)"
      to: "session_{id}.json staging record"
      via: "promoted plugin hooks → collector finalize-once"
      pattern: "session_.*\\.json"
    - from: "ollama.show()"
      to: "ModelMetadata.parameter_count/quantization"
      via: "_enrich_from_ollama on a real local model"
      pattern: "parameter_count"
---

<objective>
Prove Success Criterion #3 on REAL data (D-02 hard gate): install Ollama, pull Hermes 3
8B Q4, run one live Hermes v0.15.1 session through the promoted Kajiba plugin, and confirm
the captured staging record carries real `parameter_count`/`quantization`/`model_family`/
`context_window` from `ollama.show()` — not just the mocked path proven in 07-03. Then run
`kajiba preview` on the live record to confirm Layer C (GLiNER) runs on real captured prose
(redactions + flags). Archive the staging JSON as the phase artifact. The dev machine has
NO Ollama today, so this requires human setup and is necessarily a checkpoint-gated,
non-autonomous plan.

Purpose: SC#3/CAPT-04 must be demonstrably TRUE on live data (D-02), and PRIV-01 GLiNER
must be shown loading the real `nvidia/gliner-PII` model end-to-end at least once. This is
the phase gate before `/gsd-verify-work`.
Output: `07-LIVE-CAPTURE.md` walkthrough + archived live staging JSON; optional Ollama
appendix touch-ups in `docs/hermes-setup.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-environment-plugin-foundation/06-HOOK-KWARGS.md
@.planning/phases/07-turn-capture-semantic-pii-scrubbing/07-RESEARCH.md
@docs/hermes-setup.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install the [llm-scrub] extra into the Hermes venv + verify GLiNER + ollama client resolve</name>
  <files>pyproject.toml, tests/test_scrubber_semantic.py</files>
  <read_first>
    - 07-RESEARCH.md § Standard Stack (verified versions), § Runtime State Inventory (plugin installed editable in the Hermes venv `%LOCALAPPDATA%\hermes\hermes-agent\venv`, finding 8), § Environment Availability
    - 06-HOOK-KWARGS.md findings 7 (native-Windows COPY workflow) and 8 (plugin must be importable in the Hermes venv)
    - pyproject.toml (the populated `[llm-scrub]` extra from 07-01)
  </read_first>
  <action>
    In the Hermes venv where the Kajiba plugin is installed (per 06-HOOK-KWARGS finding 8,
    `%LOCALAPPDATA%\hermes\hermes-agent\venv`), reinstall the Kajiba editable package WITH the extra
    (`pip install -e .[llm-scrub]` / the uv equivalent) so `gliner`, `torch`, `transformers`, and the
    `ollama` client resolve there AND in the dev `.venv` used for the GLiNER LANE B tests. These four
    packages passed slopcheck in RESEARCH (4 OK) — no legitimacy checkpoint needed. Confirm the GLiNER
    model id resolves by loading `nvidia/gliner-PII` (CAPITAL PII — Correction 1) once (this triggers the
    one-time ~1.5-2.4GB HF weight download). Run the LANE B semantic tests
    (`python -m pytest tests/test_scrubber_semantic.py -k "detect or calibration" -x`) now that the extra
    is installed — the D-06 calibration HARD GATE (zero auto-redact ≥0.7 on the code fixture) must pass on
    the real model. If the 570M model OOMs on the 8GB GPU, fall GLiNER back to CPU (the device check
    already handles this) — do NOT substitute a different model (PRIV-01 names the NVIDIA model; a swap
    needs user approval).
  </action>
  <acceptance_criteria>
    - Behavior assertion: `python -c "import gliner, torch, transformers, ollama; print('extra ok')"` exits 0 in the target venv.
    - Behavior assertion: `nvidia/gliner-PII` loads via `GLiNER.from_pretrained` without a 404 (capital PII resolves).
    - Behavior assertion: `python -m pytest tests/test_scrubber_semantic.py -k "detect or calibration" -x` PASSES on the real model (D-06 hard gate green: zero ≥0.7 auto-redacts on the code fixture; FP rate recorded).
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_scrubber_semantic.py -k "detect or calibration" -x</automated>
  </verify>
  <done>`[llm-scrub]` installed in the target venv; `nvidia/gliner-PII` loads; LANE B detect + calibration gate pass on the real model.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Live local-Ollama Hermes capture run (D-02 human action)</name>
  <files>(no repo files — human runs Ollama + a live Hermes session; evidence reported back)</files>
  <action>
    Human-action checkpoint (see what-built / how-to-verify / resume-signal). Claude cannot install
    Ollama, pull the Hermes 3 8B Q4 model, or drive a live Hermes session on this machine — the human
    performs the run and reports the captured staging-record evidence (the real `model` metadata block,
    the one-staging-file confirmation, and the live `kajiba preview` GLiNER result).
  </action>
  <what-built>
    Promoted capture (07-03), GLiNER Layer C (07-04), and CLI wiring (07-05) are complete and unit-green.
    `[llm-scrub]` is installed (Task 1). What remains REQUIRES a human: the dev machine has no Ollama, so
    a person must install Ollama, pull Hermes 3 8B Q4, point Hermes v0.15.1 at the local Ollama backend,
    and run one real session — none of which Claude can do via CLI/API on this machine.
  </what-built>
  <how-to-verify>
    1. Install Ollama for Windows (https://ollama.com/download) and confirm `ollama --version` works.
    2. Pull the model: `ollama pull` the Hermes 3 8B Q4 tag; confirm `ollama list` shows it.
    3. Confirm `ollama show <model>` returns details (parameter_size, quantization_level, family) and a digest.
    4. Per `docs/hermes-setup.md` (optional Ollama appendix), point Hermes v0.15.1 at the local Ollama
       backend; ensure the Kajiba plugin is enabled (`hermes plugins enable kajiba`) and re-copied into the
       discovery dir if edited (native-Windows COPY workflow, 06-HOOK-KWARGS finding 7).
    5. Run ONE short Hermes session (a few turns, at least one tool call) using throwaway, non-sensitive
       prompts.
    6. After the session, confirm EXACTLY ONE staging file `session_<id>.json` exists under
       `<HERMES_HOME>/kajiba/staging/` (finalize-once — Correction 3), and that its `model` block has
       non-null `parameter_count`/`quantization`/`model_family`/`context_window` from `ollama.show()`.
    7. Run `kajiba preview` on that record and confirm Layer C runs (GLiNER redactions and/or a flagged
       panel appear; at minimum the command completes with the GLiNER path active, not degraded).
  </how-to-verify>
  <resume-signal>
    Paste the relevant excerpt of the staging JSON `model` block (showing the real parameter_count /
    quantization) and a one-line note on the `kajiba preview` GLiNER result, OR describe what failed
    (e.g. ollama not detected as local, metadata null, multiple staging files). Type "approved" with the
    excerpt to continue, or describe the issue for a gap-closure replan.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the D-02 live-capture artifact (07-LIVE-CAPTURE.md)</name>
  <files>.planning/phases/07-turn-capture-semantic-pii-scrubbing/07-LIVE-CAPTURE.md, docs/hermes-setup.md</files>
  <read_first>
    - 07-VALIDATION.md § Manual-Only Verifications (the D-02 row defines the artifact: archived staging JSON with non-null param/quant/family/context_window)
    - 07-RESEARCH.md § Assumptions A3 (idempotent-overwrite accumulates full trajectory) and A4 (interrupted-turn capture) and Open Questions 1-2
    - The human-provided staging JSON excerpt from the checkpoint
    - 06-HOOK-KWARGS.md (the established living-doc format to mirror for provenance)
  </read_first>
  <action>
    Write `.planning/phases/07-turn-capture-semantic-pii-scrubbing/07-LIVE-CAPTURE.md` documenting the
    D-02 live run: session id, Hermes/Ollama versions, the model tag, the captured `model` block (real
    `parameter_count`/`quantization`/`model_family`/`context_window`/`model_hash`), confirmation that
    exactly ONE staging file was produced (finalize-once verified live — closes Assumption A3), the
    `kajiba preview` GLiNER result on real data (redaction/flag counts), and the observed answers to the
    two open questions if encountered (A4 interrupted-turn capture; final-vs-turn session-end behavior).
    Archive the (PII-scrubbed/synthetic-where-needed) staging JSON excerpt inline or as a sibling file,
    following the T-06-11 PII discipline from 06-HOOK-KWARGS (record shapes/metadata, not real prompt
    content). If the live run surfaced any deviation from the mocked path (e.g. ollama return-object shape
    differing from Assumption A1), note it and whether a code fix is needed (route to gap closure if so).
    Add any necessary touch-ups to `docs/hermes-setup.md`'s Ollama appendix so the local-backend steps are
    reproducible. This artifact is the phase-gate evidence consumed by `/gsd-verify-work`.
  </action>
  <acceptance_criteria>
    - Source assertion: `07-LIVE-CAPTURE.md` exists and contains the captured `model` block with non-null `parameter_count` and `quantization` from the live Ollama run.
    - Source assertion: the doc states EXACTLY ONE staging file was produced for the session (finalize-once confirmed live).
    - Source assertion: the doc records the `kajiba preview` GLiNER result (redaction and/or flag counts) on the live record.
    - Source assertion: PII discipline honored — no real prompt/response/file content transcribed (synthetic/placeholder where needed, per T-06-11).
    - Behavior assertion: `git diff --quiet src/kajiba/schema.py` — no schema change snuck in via the live run.
  </acceptance_criteria>
  <verify>
    <automated>python -c "import os; p='.planning/phases/07-turn-capture-semantic-pii-scrubbing/07-LIVE-CAPTURE.md'; assert os.path.exists(p); t=open(p,encoding='utf-8').read(); assert 'parameter_count' in t and 'quantization' in t, 'missing live metadata evidence'; print('artifact ok')"</automated>
  </verify>
  <done>D-02 live-capture artifact records real Ollama metadata, finalize-once confirmation, and the live GLiNER preview result; setup doc reproducible; PII discipline honored.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan)
- **.planning/phases/07-turn-capture-semantic-pii-scrubbing/07-LIVE-CAPTURE.md** (NEW): D-02 live-capture proof — real `ollama.show()` metadata, finalize-once confirmation, live `kajiba preview` GLiNER result, archived (PII-safe) staging JSON.
- **docs/hermes-setup.md**: Ollama local-backend appendix touch-ups for reproducibility.
- `[llm-scrub]` extra installed in the Hermes + dev venvs; `nvidia/gliner-PII` weights cached; LANE B GLiNER tests (detect/calibration) proven GREEN on the real model.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local Ollama service → collector | Real `ollama.show()` + local inference for the live session |
| HuggingFace download → local cache | One-time `nvidia/gliner-PII` weight fetch |
| live session content → staging | Real (throwaway) prompts captured to local staging |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-15 | Information Disclosure | live capture artifact | mitigate | Live run uses throwaway non-sensitive prompts; the recorded artifact follows T-06-11 PII discipline (shapes/metadata only, no real content); staging stays local-only under HERMES_HOME. |
| T-07-16 | Tampering | model supply chain (download) | mitigate | `nvidia/gliner-PII` + Hermes 3 8B Q4 pulled from official sources; gliner/torch/ollama slopcheck-clean (RESEARCH); capital-PII repo id prevents resolving a typo-squatted lowercase repo. |
| T-07-17 | Denial of Service | live Hermes session | mitigate | Capture hooks are fault-tolerant (07-03); scrubbing is a CLI step run AFTER the session so GLiNER never contends with the resident 8B model in the hook path (Pitfall 6 — CPU fallback if VRAM-constrained). |
| T-07-SC | Tampering | pip install (gliner/torch/transformers/ollama) | mitigate | Verified-version installs of slopcheck-OK packages (RESEARCH §Package Legitimacy Audit); no `[ASSUMED]`/`[SUS]`/`[SLOP]` → no blocking legitimacy checkpoint required. |
</threat_model>

<verification>
- `python -m pytest tests/test_scrubber_semantic.py -k "detect or calibration" -x` GREEN on the real model (D-06 gate live).
- `07-LIVE-CAPTURE.md` exists with real `parameter_count`/`quantization` evidence and finalize-once confirmation.
- Exactly one `session_<id>.json` produced for the live session (human-verified at the checkpoint).
- `git diff --quiet src/kajiba/schema.py` — schema untouched.
- Full suite `python -m pytest` green with the extra installed (LANE B now runs, not skipped).
</verification>

<success_criteria>
- SC#3 on REAL data: model metadata captured from `ollama.show()` (real param/quant/family/context_window) into ModelMetadata via a live Hermes 3 8B Q4 session (D-02).
- PRIV-01: `nvidia/gliner-PII` loads end-to-end and Layer C runs on real captured prose.
- D-02 phase-gate artifact archived; finalize-once proven live; no schema change.
</success_criteria>

<output>
Create `.planning/phases/07-turn-capture-semantic-pii-scrubbing/07-06-SUMMARY.md` when done
</output>
