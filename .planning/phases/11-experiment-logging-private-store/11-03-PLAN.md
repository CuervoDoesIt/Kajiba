---
phase: 11-experiment-logging-private-store
plan: 03
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - src/kajiba/cli.py
  - tests/test_experiment_exclusion.py
autonomous: true
requirements: [ELOG-03]
must_haves:
  truths:
    - "publish skips any outbox record whose record_kind == model_experiment and prints a skip notice"
    - "submit carries a defensive assertion that it never processes a model_experiment record"
    - "An experiment written by log_experiment never appears in publish/browse/download output"
    - "The structural separation (experiments dir excluded from the outbox glob) holds without network access"
  artifacts:
    - path: "src/kajiba/cli.py"
      provides: "record_kind == model_experiment skip guard in the publish consent loop"
      contains: "model_experiment"
    - path: "tests/test_experiment_exclusion.py"
      provides: "ELOG-03 active-guard + D-14 regression tests"
      contains: "def test_experiment_absent_from_community_paths"
  key_links:
    - from: "src/kajiba/cli.py publish loop"
      to: "data.get('record_kind')"
      via: "raw-dict discriminator check before validate_record"
      pattern: "record_kind"
    - from: "tests/test_experiment_exclusion.py"
      to: "kajiba.experiment_store.log_experiment"
      via: "writes a real experiment then asserts community-path absence"
      pattern: "log_experiment\\("
---

<objective>
Close the active half of ELOG-03: add the `record_kind == "model_experiment"` skip guard inside the existing `publish` consent-verification loop (defense in depth on top of the structural dir separation from 11-01), add a defensive assertion in `submit`, and add the D-14 regression test proving a logged experiment never appears in any community path.

Purpose: Satisfy ELOG-03 fully — experiments are structurally separate AND actively refused by the publish path. Depends on 11-01 (log_experiment, EXPERIMENTS_DIR) and 11-02 (the experiment group, so the regression can exercise the real write path).
Output: publish guard + submit assertion in `src/kajiba/cli.py`; `tests/test_experiment_exclusion.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-experiment-logging-private-store/11-CONTEXT.md
@.planning/phases/11-experiment-logging-private-store/11-RESEARCH.md
@.planning/phases/11-experiment-logging-private-store/11-PATTERNS.md
@.planning/phases/11-experiment-logging-private-store/11-VALIDATION.md
@.planning/phases/11-experiment-logging-private-store/11-01-SUMMARY.md
@.planning/phases/11-experiment-logging-private-store/11-02-SUMMARY.md
@src/kajiba/cli.py
</context>

<artifacts_this_phase_produces>
This plan creates the following NEW symbols (downstream drift checks must treat these as newly-created):

- `record_kind == "model_experiment"` skip guard inside the `publish` Step 4 loop — `src/kajiba/cli.py`
- defensive assertion in `submit` that no model_experiment record reaches it — `src/kajiba/cli.py`
- `tests/test_experiment_exclusion.py` — NEW test file

Symbols this plan CONSUMES (already created, not drift): `log_experiment`, `EXPERIMENTS_DIR` (11-01); `experiment` group / `experiment log` (11-02); `_load_outbox_records`, `publish`, `submit`, `validate_record` (pre-existing); `GitHubOps` (pre-existing, monkeypatched in tests).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 exclusion test stubs (ELOG-03 active guard + D-14 regression)</name>
  <files>tests/test_experiment_exclusion.py</files>
  <read_first>
    - tests/test_cli.py (publish-style consent flow tests + CliRunner + monkeypatch idiom; the GitHubOps monkeypatch pattern used by existing publish tests)
    - src/kajiba/cli.py (publish at 1429; _load_outbox_records at 101-115; the Step 4 loop at 1472-1502; OUTBOX_DIR glob at 109)
    - .planning/phases/11-experiment-logging-private-store/11-VALIDATION.md (exact names: test_publish_skips_experiment, test_experiment_absent_from_community_paths; D-14 detail note at lines 533-535)
    - .planning/phases/11-experiment-logging-private-store/11-PATTERNS.md (exclusion regression guidance lines 154-179)
    - .planning/phases/11-experiment-logging-private-store/11-RESEARCH.md (Pattern 4 active guard 311-324; D-14 regression detail 535 — avoid real network, monkeypatch GitHubOps/catalog or assert on the structural outbox-glob path)
  </read_first>
  <behavior>
    - test_publish_skips_experiment: with a model_experiment dict placed (deliberately, as a misplaced file) into the monkeypatched OUTBOX_DIR, invoking ["publish","--dry-run"] does NOT include that record's record_id in output and the skip notice appears; no network is hit (GitHubOps / catalog fetch monkeypatched).
    - test_experiment_absent_from_community_paths: write a real experiment via log_experiment to the monkeypatched EXPERIMENTS_DIR, then assert (a) OUTBOX_DIR.glob("*.jsonl") does not contain the experiment file, (b) the experiment file is untouched on disk, (c) the experiment record_id never appears in publish output.
  </behavior>
  <action>
    Create tests/test_experiment_exclusion.py with a CliRunner runner fixture and the two functions named exactly per 11-VALIDATION.md. Monkeypatch dir constants (`kajiba.cli.OUTBOX_DIR`, `kajiba.cli.EXPERIMENTS_DIR`, `kajiba.cli.KAJIBA_BASE`) to tmp_path subdirs (Pitfall 2). To avoid real network in the publish test, monkeypatch `kajiba.cli.GitHubOps` (and/or the catalog fetch) or drive the assertion through `publish --dry-run` so no PR is opened; prefer asserting on the structural outbox-glob exclusion path which needs no network at all (RESEARCH line 535). For the active-guard test, write a minimal model_experiment dict directly into OUTBOX_DIR as a .jsonl line (simulating a misplaced file) and assert the publish guard skips it. For the regression test, use kajiba.experiment_store.log_experiment to write a real ExperimentRecord into EXPERIMENTS_DIR and assert it never crosses into the community path. These stubs WILL FAIL until Task 2 adds the guard (RED). Write the file directly — no fenced implementation in this action.
  </action>
  <verify>
    <automated>python -m pytest tests/test_experiment_exclusion.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_experiment_exclusion.py contains def test_publish_skips_experiment and def test_experiment_absent_from_community_paths
    - tests/test_experiment_exclusion.py contains `monkeypatch.setattr("kajiba.cli.OUTBOX_DIR"` and `log_experiment(`
    - tests/test_experiment_exclusion.py does NOT make real network calls (GitHubOps/catalog monkeypatched OR only the structural outbox-glob asserted)
    - `python -m pytest tests/test_experiment_exclusion.py --collect-only -q` lists both tests without collection errors
  </acceptance_criteria>
  <done>Both exclusion test functions exist, collect cleanly, isolate dirs, and avoid real network.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: publish record_kind skip guard + submit defensive assertion (ELOG-03, D-13 active)</name>
  <files>src/kajiba/cli.py</files>
  <read_first>
    - src/kajiba/cli.py (publish Step 4 loop at 1472-1502 — the `for path, data in outbox_records:` insertion point at 1476; existing skip style `logger.warning(...); console.print("[yellow]  Skipping invalid record: ...")` at 1496-1497 to mirror; submit at 476 + _load_latest_staging at 77-98; _load_outbox_records returns raw dicts at 113)
    - .planning/phases/11-experiment-logging-private-store/11-RESEARCH.md (Pattern 4 active guard 311-324; Assumption A2 — submit only reads STAGING via _load_latest_staging so it needs a defensive assertion, not dead code)
    - .planning/phases/11-experiment-logging-private-store/11-PATTERNS.md (publish active guard section 118-138)
    - tests/test_experiment_exclusion.py (the two behaviors to satisfy — created in Task 1)
  </read_first>
  <action>
    In src/kajiba/cli.py inside the `publish` Step 4 loop (`for path, data in outbox_records:` at line 1476), as the FIRST statement inside the loop body, before the existing `try:`/`validate_record(data)`, add the active guard reading the raw-dict discriminator: if `data.get("record_kind") == "model_experiment"`, call `logger.warning("Refusing to publish experiment record: %s", path)`, `console.print(f"[yellow]  Skipping experiment record (never published): {path.name}[/yellow]")`, and `continue`. Reading record_kind on the raw dict (matches how _load_outbox_records returns raw dicts at line 113) avoids feeding an experiment into the KajibaRecord validator (Pitfall 1). Mirror the existing skip phrasing/style at lines 1496-1497.
    In `submit`: after `record = _load_latest_staging()` and the None check, add a defensive assertion that the loaded record is not a model_experiment — e.g. `if getattr(record, "record_kind", "coding_session") == "model_experiment": raise click.ClickException("Experiment records are never submitted to the community path.")`. Per Assumption A2 submit only reads STAGING_DIR via _load_latest_staging and only log_experiment writes experiments (only to EXPERIMENTS_DIR), so this is defensive, not a live path — keep it a single guard, not dead branching. Do not alter any other publish/submit behavior.
  </action>
  <verify>
    <automated>python -m pytest tests/test_experiment_exclusion.py -x</automated>
  </verify>
  <acceptance_criteria>
    - src/kajiba/cli.py publish loop contains `data.get("record_kind") == "model_experiment"` followed by a `continue`
    - src/kajiba/cli.py contains a "Skipping experiment record (never published)" console message
    - src/kajiba/cli.py submit contains a defensive `model_experiment` guard raising a click.ClickException
    - `python -m pytest tests/test_experiment_exclusion.py -x` exits 0
  </acceptance_criteria>
  <done>publish skips model_experiment records with a notice and never validates them as KajibaRecord; submit has a defensive experiment-refusal guard; both exclusion tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression gate (ELOG-01/02/03 end to end)</name>
  <files>tests/test_experiment_store.py, tests/test_cli_experiment.py, tests/test_experiment_exclusion.py</files>
  <read_first>
    - .planning/phases/11-experiment-logging-private-store/11-VALIDATION.md (Phase gate: full suite green before verify-work; baseline 264 passed + 2 yaml-soft-dep skips)
    - .planning/STATE.md (Phase 10 completion note: 264 passed, 2 pre-existing skips)
  </read_first>
  <action>
    Run the full test suite and confirm no regressions: the three new test files (test_experiment_store.py, test_cli_experiment.py, test_experiment_exclusion.py) are all green and the pre-existing 264 passing tests still pass, with the 2 pre-existing yaml-soft-dep skips remaining (expected). If any pre-existing test now fails, it indicates a regression introduced by the publish-guard insertion or an import-time side effect — fix the cause (do not delete or xfail existing tests). This task adds no production code; it is the phase gate that proves ELOG-01/02/03 are satisfied together and nothing else broke. If the full suite is green, the phase is ready for /gsd-verify-work.
  </action>
  <verify>
    <automated>python -m pytest</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest` reports 0 failed
    - `python -m pytest` reports at least 264 + the new experiment tests passed (new tests strictly add to the passed count)
    - The only skips are the 2 pre-existing yaml-soft-dep skips (no new skips/xfails introduced)
    - `python -m pytest tests/test_experiment_store.py tests/test_cli_experiment.py tests/test_experiment_exclusion.py -x` exits 0
  </acceptance_criteria>
  <done>Full suite green with no regressions; all three new experiment test files pass; only the 2 expected pre-existing skips remain; phase ready for verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| outbox file → publish | A misplaced or hostile model_experiment file could sit in the outbox; the publish loop must refuse it before it reaches the community PR |
| disk store → community paths | The private experiment store must never be read by browse/download (remote-only) or globbed by publish |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Information Disclosure | experiment record reaching the community publish path | mitigate | Defense in depth (D-13): structural dir separation (publish globs OUTBOX_DIR only — primary, from 11-01) PLUS an active `record_kind == "model_experiment"` skip+notice in the publish Step 4 loop (backstop). Proven by tests/test_experiment_exclusion.py (active guard + D-14 regression) |
| T-11-09 | Information Disclosure | experiment routed into submit/staging | mitigate | Defensive ClickException in submit if a loaded record is a model_experiment (Assumption A2: structurally unreachable today, guarded for future paths) |
| T-11-10 | Tampering | feeding an experiment dict into the KajibaRecord validator | mitigate | Guard reads `data.get("record_kind")` on the raw dict BEFORE validate_record, so an experiment is skipped, never mis-validated (Pitfall 1) |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed (stdlib + pinned deps). Package legitimacy gate N/A (RESEARCH) |
</threat_model>

<verification>
- `python -m pytest tests/test_experiment_exclusion.py -x` green (active guard + D-14 regression).
- `python -m pytest` full suite green: 264 pre-existing + new experiment tests passed, only the 2 pre-existing yaml-soft-dep skips remain.
- No real network in the exclusion tests (GitHubOps/catalog monkeypatched or structural-only assertion).
- Manual smoke (11-VALIDATION Manual-Only): after `kajiba experiment log --from <fixture>`, `kajiba experiment list` shows the run and `kajiba browse` does not.
</verification>

<success_criteria>
- ELOG-03 fully satisfied: structural separation (11-01) + active publish refusal (this plan) + D-14 regression all in place.
- publish skips model_experiment records with a notice; submit defends against them.
- Full test suite green with no regressions — phase ready for /gsd-verify-work.
</success_criteria>

<output>
Create `.planning/phases/11-experiment-logging-private-store/11-03-SUMMARY.md` when done.
</output>
