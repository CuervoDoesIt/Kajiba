---
phase: 11-experiment-logging-private-store
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/kajiba/experiment_store.py
  - src/kajiba/cli.py
  - src/kajiba/__init__.py
  - tests/test_experiment_store.py
  - tests/fixtures/experiment_run.example.json
autonomous: true
requirements: [ELOG-02, ELOG-03]
must_haves:
  truths:
    - "An external script can import build_experiment_record and log_experiment from the kajiba package"
    - "build_experiment_record(**fields) returns a validated ExperimentRecord with record_kind == model_experiment"
    - "log_experiment(record, store_dir) writes exp_<record_id>.json into the experiments store and returns its path"
    - "Re-logging identical content is skipped with a notice and returns the existing path"
    - "log_experiment refuses to write into any directory whose name is not experiments (structural privacy guard, D-13)"
    - "One JSON file per run, written as exp_<record_id>.json in a flat store (D-01)"
    - "Experiment records land directly in the store on log — no staging→outbox promotion gate (D-02)"
    - "Persistence lives in the new single-responsibility module src/kajiba/experiment_store.py (D-05)"
  artifacts:
    - path: "src/kajiba/experiment_store.py"
      provides: "log_experiment + build_experiment_record persistence module"
      contains: "def log_experiment"
      min_lines: 60
    - path: "src/kajiba/__init__.py"
      provides: "Public re-export of the two functions (D-07)"
      contains: "from kajiba.experiment_store import"
    - path: "tests/fixtures/experiment_run.example.json"
      provides: "Canonical --from / run.json example fixture"
      contains: "model_experiment"
  key_links:
    - from: "src/kajiba/experiment_store.py"
      to: "kajiba.schema.ExperimentRecord"
      via: "import + compute_record_id/compute_submission_hash calls"
      pattern: "compute_record_id"
    - from: "src/kajiba/__init__.py"
      to: "src/kajiba/experiment_store.py"
      via: "re-export"
      pattern: "from kajiba.experiment_store import"
    - from: "src/kajiba/cli.py"
      to: "EXPERIMENTS_DIR"
      via: "module constant derived from KAJIBA_BASE + _ensure_dirs()"
      pattern: "EXPERIMENTS_DIR = KAJIBA_BASE"
---

<objective>
Build the persistence foundation for private experiment logging: a new `experiment_store.py` module owning the single write path, the `EXPERIMENTS_DIR` store constant in `cli.py`, the package-level re-exports, and the canonical `run.json` fixture. This is the foundation every downstream plan (CLI surface, publish guard) depends on.

Purpose: Satisfy ELOG-02 (programmatic logging entry point) and the structural half of ELOG-03 (private namespace + write guard). The CLI plan (11-02) and the exclusion plan (11-03) both call `log_experiment` and reference `EXPERIMENTS_DIR`, so this must land first.
Output: `src/kajiba/experiment_store.py`, `EXPERIMENTS_DIR` constant in `cli.py` (added to `_ensure_dirs()`), extended `src/kajiba/__init__.py`, `tests/test_experiment_store.py`, `tests/fixtures/experiment_run.example.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-experiment-logging-private-store/11-CONTEXT.md
@.planning/phases/11-experiment-logging-private-store/11-RESEARCH.md
@.planning/phases/11-experiment-logging-private-store/11-PATTERNS.md
@.planning/phases/11-experiment-logging-private-store/11-VALIDATION.md
@src/kajiba/schema.py
@src/kajiba/cli.py
@src/kajiba/__init__.py
</context>

<artifacts_this_phase_produces>
This phase creates the following NEW symbols (downstream drift checks must treat these as newly-created, not pre-existing):

- `EXPERIMENTS_DIR` — module constant in `src/kajiba/cli.py` (`KAJIBA_BASE / "experiments"`)
- `src/kajiba/experiment_store.py` — NEW module
- `log_experiment(record, store_dir) -> Path` — function in `experiment_store.py`
- `build_experiment_record(**fields) -> ExperimentRecord` — function in `experiment_store.py`
- `experiment` Click group + `log` / `list` subcommands — in `cli.py` (created in 11-02)
- New CLI flags `--from`, `--score`, `--type`, `--task-category`, `--local-model` (created in 11-02)
- `record_kind == "model_experiment"` skip guard in `publish` (created in 11-03)
- `tests/test_experiment_store.py`, `tests/test_cli_experiment.py`, `tests/test_experiment_exclusion.py` — NEW test files
- `tests/fixtures/experiment_run.example.json` — NEW fixture
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 test stubs + canonical run.json fixture</name>
  <files>tests/test_experiment_store.py, tests/fixtures/experiment_run.example.json</files>
  <read_first>
    - tests/test_cli.py (CliRunner + monkeypatch + tmp_path dir-isolation idiom — lines 1-30 imports/runner fixture)
    - tests/test_schema_experiment.py (round-trip + vocab + eval_score bound assertions for ExperimentRecord — the construction shape to mirror)
    - src/kajiba/schema.py (ExperimentRecord / ExperimentMetadata / ExperimentOutcome / ModelMetadata field sets at lines 166-177, 408-442; load_record at 515; EXPERIMENT_TYPES/RECOMMENDED_ACTIONS at 114/117)
    - .planning/phases/11-experiment-logging-private-store/11-VALIDATION.md (Per-Task Verification Map — the exact test function names to create)
    - .planning/phases/11-experiment-logging-private-store/11-RESEARCH.md (canonical run.json shape at lines 433-461)
  </read_first>
  <behavior>
    - test_build_record: build_experiment_record(**fields) returns an ExperimentRecord; record_kind == "model_experiment"; outcome.eval_score and experiment.experiment_type round-trip.
    - test_log_writes_file: log_experiment(rec, tmp_path/"experiments") creates exp_<record_id>.json under that dir and returns the path; record_id matches the kajiba_exp_ prefix.
    - test_atomic_write: after log_experiment, no file ending in .tmp remains in the store dir and the written file parses as valid JSON via json.loads.
    - test_dedup_skip: calling log_experiment twice with the same record returns the same path and leaves exactly one exp_*.json file.
    - test_public_exports: `from kajiba import log_experiment, build_experiment_record` succeeds.
    - test_refuses_outbox_dir: log_experiment(rec, tmp_path/"outbox") raises ValueError (structural guard, D-13).
  </behavior>
  <action>
    Create tests/test_experiment_store.py with the six pytest functions named exactly as in 11-VALIDATION.md (test_build_record, test_log_writes_file, test_atomic_write, test_dedup_skip, test_public_exports, test_refuses_outbox_dir). Import from kajiba.experiment_store and kajiba.schema. For store isolation, pass tmp_path / "experiments" directly as the store_dir argument (the store module is Click-free, so no monkeypatch of cli constants is needed here — cleaner than the cli test idiom per RESEARCH Pitfall 2). Build the input ExperimentRecord either via build_experiment_record or by constructing ExperimentMetadata/ExperimentOutcome/ModelMetadata directly mirroring tests/test_schema_experiment.py. These stubs WILL FAIL until Task 2 lands the module (RED state) — that is expected and correct. Also create tests/fixtures/experiment_run.example.json as a serialized ExperimentRecord matching RESEARCH lines 433-461: schema_version "0.2.0", record_kind "model_experiment", a full experiment block (experiment_id, experiment_type "model_evaluation", local_model with model_name/quantization/provider/is_local, reviewer_model, task_category, task_description, started_at, completed_at) and an outcome block (local_model_output, reviewer_critique, eval_score 0.82, drift_flag false, lessons_learned, recommended_action). OMIT record_id and submission_hash (log_experiment computes them). This fixture doubles as the documented --from example for 11-02. Do NOT inline implementation code in this action — write the test file directly.
  </action>
  <verify>
    <automated>python -m pytest tests/test_experiment_store.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_experiment_store.py contains def test_build_record, def test_log_writes_file, def test_atomic_write, def test_dedup_skip, def test_public_exports, and def test_refuses_outbox_dir
    - `python -m pytest tests/test_experiment_store.py --collect-only -q` lists all six tests without collection errors
    - tests/fixtures/experiment_run.example.json parses via json.loads and contains "record_kind": "model_experiment" and does NOT contain a top-level "record_id" key
  </acceptance_criteria>
  <done>All six test functions exist and collect; the canonical fixture exists, is valid JSON, is a model_experiment record, and omits record_id/submission_hash.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: experiment_store.py — log_experiment + build_experiment_record (ELOG-02 + D-13 structural guard)</name>
  <files>src/kajiba/experiment_store.py</files>
  <read_first>
    - src/kajiba/schema.py (ExperimentRecord at 432; compute_record_id at 445 + compute_submission_hash at 469 — the frozen identity methods to CALL, not reimplement; ExperimentMetadata at 408; ExperimentOutcome at 421; ModelMetadata at 166; EXPERIMENT_TYPES at 114)
    - src/kajiba/cli.py (_submit_record at 371-416 — the ID-compute -> model_dump(mode="json", by_alias=True) -> write reference sequence; module-level logger idiom at line 57)
    - .planning/phases/11-experiment-logging-private-store/11-RESEARCH.md (Pattern 1 reference implementation at lines 176-237; Pattern 2 at 239-281; os.replace citation at 339)
    - .planning/phases/11-experiment-logging-private-store/11-PATTERNS.md (experiment_store.py section lines 23-64 — module header, write core, structural guard, dedup)
    - tests/test_experiment_store.py (the six behaviors this module must satisfy — created in Task 1)
  </read_first>
  <action>
    Create src/kajiba/experiment_store.py with the module docstring, stdlib imports (json, logging, os, tempfile), `from pathlib import Path`, `from datetime import UTC, datetime`, `from typing import Optional`, and `from kajiba.schema import ExperimentMetadata, ExperimentOutcome, ExperimentRecord, ModelMetadata`. Add module-level `logger = logging.getLogger(__name__)`. Per D-05 this is the new single-responsibility persistence module. Implement two public functions per D-06/D-08:
    (1) `log_experiment(record: ExperimentRecord, store_dir: Path) -> Path` — D-13 structural guard FIRST: `resolved = store_dir.resolve(); if resolved.name != "experiments": raise ValueError(...)`. Then `store_dir.mkdir(parents=True, exist_ok=True)`, call `record.compute_record_id()` and `record.compute_submission_hash()` (the frozen schema methods — do NOT hand-roll hashing), build `dest = store_dir / f"exp_{record.record_id}.json"` (one flat file per run, D-01). This is the single direct-to-store write — there is no staging→outbox promotion gate for experiments (D-02). Dedup: `if dest.exists(): logger.info("Experiment already logged (identical content): %s", dest); return dest` (skip-with-notice, Claude's discretion). Serialize via `record.model_dump(mode="json", by_alias=True)` then `json.dumps(..., ensure_ascii=False, indent=2) + "\n"` (indent=2 for inspectability per D-04). Atomic write: `fd, tmp_name = tempfile.mkstemp(dir=store_dir, suffix=".tmp")`, write payload through `os.fdopen(fd, "w", encoding="utf-8")`, then `os.replace(tmp_name, dest)`; on `BaseException` clean up via `Path(tmp_name).unlink(missing_ok=True)` and re-raise. Return `dest`.
    (2) `build_experiment_record(**fields) -> ExperimentRecord` — keyword-only convenience constructor (RESEARCH Pattern 2): assemble `ExperimentMetadata` (with nested `ModelMetadata(model_name=...)`), `ExperimentOutcome`, default `started_at` to `datetime.now(UTC)` when absent, and return `ExperimentRecord(experiment=..., outcome=...)`. Pydantic validates on construction (D-08) — no extra validation call. Use Optional[X] typing, double-quote strings, Google-style docstrings with Args/Returns/Raises. Do NOT define EXPERIMENTS_DIR here (it lives in cli.py per D-03) and do NOT import anything from kajiba.cli (keeps the module Click-free for ELOG-02 callers). Do NOT scrub at log time (out of scope — store raw).
  </action>
  <verify>
    <automated>python -m pytest tests/test_experiment_store.py::test_build_record tests/test_experiment_store.py::test_log_writes_file tests/test_experiment_store.py::test_atomic_write tests/test_experiment_store.py::test_dedup_skip tests/test_experiment_store.py::test_refuses_outbox_dir -x</automated>
  </verify>
  <acceptance_criteria>
    - src/kajiba/experiment_store.py contains `def log_experiment(` and `def build_experiment_record(`
    - src/kajiba/experiment_store.py contains `os.replace` and `tempfile.mkstemp` and `record.compute_record_id()` and `record.compute_submission_hash()`
    - src/kajiba/experiment_store.py contains `model_dump(mode="json", by_alias=True)`
    - src/kajiba/experiment_store.py does NOT contain `import` of `kajiba.cli` (grep for `from kajiba.cli` and `import kajiba.cli` returns nothing)
    - `python -m pytest tests/test_experiment_store.py::test_log_writes_file tests/test_experiment_store.py::test_atomic_write tests/test_experiment_store.py::test_dedup_skip tests/test_experiment_store.py::test_refuses_outbox_dir -x` exits 0
  </acceptance_criteria>
  <done>The module exists, the five non-export store/build tests pass, IDs are computed by calling the frozen schema methods, the write is atomic, the structural guard rejects non-experiments dirs, and the module imports nothing from cli.</done>
</task>

<task type="auto">
  <name>Task 3: EXPERIMENTS_DIR constant (D-03) + package re-exports (D-07)</name>
  <files>src/kajiba/cli.py, src/kajiba/__init__.py</files>
  <read_first>
    - src/kajiba/cli.py (dir constants + _ensure_dirs() at lines 64-74 — the exact template; KAJIBA_BASE at 64)
    - src/kajiba/__init__.py (current full file — only __version__)
    - .planning/phases/11-experiment-logging-private-store/11-PATTERNS.md (EXPERIMENTS_DIR section lines 67-83; __init__.py section lines 142-150)
    - .planning/phases/11-experiment-logging-private-store/11-CONTEXT.md (D-03 + D-07 wording)
  </read_first>
  <action>
    In src/kajiba/cli.py: add `EXPERIMENTS_DIR = KAJIBA_BASE / "experiments"` directly beneath the existing `OUTBOX_DIR = KAJIBA_BASE / "outbox"` line (line 66) — derive from `KAJIBA_BASE`, do NOT introduce a second `~/.hermes` literal (D-03). Inside `_ensure_dirs()` add `EXPERIMENTS_DIR.mkdir(parents=True, exist_ok=True)` alongside the existing STAGING_DIR/OUTBOX_DIR mkdir calls. Do not change any other behavior in this task. In src/kajiba/__init__.py: add `from kajiba.experiment_store import build_experiment_record, log_experiment` after the `__version__` line so `from kajiba import log_experiment, build_experiment_record` resolves (D-07). The project has no `__all__` convention — the import itself is the export surface; keep it that way.
  </action>
  <verify>
    <automated>python -m pytest tests/test_experiment_store.py::test_public_exports -x</automated>
  </verify>
  <acceptance_criteria>
    - src/kajiba/cli.py contains `EXPERIMENTS_DIR = KAJIBA_BASE / "experiments"`
    - `EXPERIMENTS_DIR` appears inside the body of `_ensure_dirs()` (an `EXPERIMENTS_DIR.mkdir(parents=True, exist_ok=True)` line exists between the def and the next top-level def)
    - src/kajiba/cli.py contains no new `Path.home() / ".hermes"` literal beyond the two that already existed (KAJIBA_BASE line 64 and DOWNLOADS_DIR line 67)
    - src/kajiba/__init__.py contains `from kajiba.experiment_store import build_experiment_record, log_experiment`
    - `python -c "from kajiba import log_experiment, build_experiment_record"` exits 0
    - `python -m pytest tests/test_experiment_store.py::test_public_exports -x` exits 0
  </acceptance_criteria>
  <done>EXPERIMENTS_DIR derives from KAJIBA_BASE and is created in _ensure_dirs(); both functions import cleanly from the top-level kajiba package; the public-exports test passes; the full test_experiment_store.py suite is now green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → log_experiment | An external eval script or the CLI hands in an already-validated ExperimentRecord plus a target store dir |
| process → filesystem | A JSON file is written under the user-home experiment store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | log_experiment write target (D-13 structural guard) | mitigate | Refuse to write unless `store_dir.resolve().name == "experiments"`; raise ValueError otherwise so an experiment can never land in STAGING_DIR/OUTBOX_DIR. Verified by tests/test_experiment_store.py::test_refuses_outbox_dir |
| T-11-02 | Tampering / Integrity | torn write on crash leaves corrupt JSON | mitigate | Atomic write: tempfile.mkstemp in same dir then os.replace (atomic + overwrites on POSIX and Windows); temp cleaned up on BaseException. Verified by test_atomic_write |
| T-11-03 | Tampering | path traversal via record_id-derived filename | mitigate | record_id is hash-derived `kajiba_exp_<12hex>` from compute_record_id (no user path chars); filename `exp_{record_id}.json` is constructed, never user-supplied |
| T-11-04 | Information Disclosure | raw PII stored at log time (no scrub this phase) | accept | By design out of scope (Phase 12 adds experiment-aware scrub). Store is private/no-publish so PII never leaves the machine — conscious documented gap, not a finding to fix here |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Phase 11 installs NO external packages (all stdlib + already-pinned pydantic/click/rich). No package legitimacy gate required (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `python -m pytest tests/test_experiment_store.py -x` is fully green (all six tests).
- `python -c "from kajiba import log_experiment, build_experiment_record"` exits 0.
- `python -m pytest` full suite remains green (baseline 264 passed + 2 pre-existing yaml-soft-dep skips; this plan adds passing tests, no regressions).
- No new `~/.hermes` literal introduced in cli.py beyond the two pre-existing ones.
</verification>

<success_criteria>
- ELOG-02 satisfied: `from kajiba import build_experiment_record, log_experiment` works; build returns a validated ExperimentRecord; log writes `exp_<id>.json` and returns its path, computing IDs via the frozen schema methods.
- ELOG-03 structural half satisfied: log_experiment refuses any non-`experiments` directory.
- EXPERIMENTS_DIR derives from KAJIBA_BASE (D-03) and is created in _ensure_dirs().
- Canonical run.json fixture exists for downstream 11-02 --from tests.
</success_criteria>

<output>
Create `.planning/phases/11-experiment-logging-private-store/11-01-SUMMARY.md` when done.
</output>
</output>
