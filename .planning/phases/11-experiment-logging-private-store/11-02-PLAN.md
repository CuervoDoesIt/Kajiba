---
phase: 11-experiment-logging-private-store
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/kajiba/cli.py
  - tests/test_cli_experiment.py
autonomous: true
requirements: [ELOG-01]
must_haves:
  truths:
    - "kajiba experiment log --from run.json writes exp_<id>.json into the experiment store and prints the written path"
    - "Scalar flags (--score / --type / --task-category) override or fill fields on top of --from before validation"
    - "When neither --from nor required flags are given, an interactive Rich fallback prompts for essential fields and persists a record"
    - "kajiba experiment list shows the logged runs so the user can confirm they landed"
    - "A coding-session JSON passed to --from is rejected with a clear ClickException"
    - "kajiba experiment log --from run.json loads JSON via load_record()/ExperimentRecord.model_validate then calls log_experiment() — CLI and programmatic paths share one file format (D-10)"
    - "Scalar convenience flags (--score, --type, --task-category, ...) override/fill fields on top of --from, applied to the raw dict before validation (D-11)"
  artifacts:
    - path: "src/kajiba/cli.py"
      provides: "experiment Click group + log/list subcommands"
      contains: "def experiment_log"
    - path: "tests/test_cli_experiment.py"
      provides: "ELOG-01 CLI behavior tests"
      contains: "def test_log_from_file"
  key_links:
    - from: "src/kajiba/cli.py experiment_log"
      to: "kajiba.experiment_store.log_experiment"
      via: "single shared write path (D-08)"
      pattern: "log_experiment\\("
    - from: "src/kajiba/cli.py experiment_log"
      to: "kajiba.schema.load_record"
      via: "--from JSON parse + ExperimentRecord isinstance check"
      pattern: "load_record\\("
    - from: "src/kajiba/cli.py experiment group"
      to: "EXPERIMENTS_DIR"
      via: "log writes here; list globs here"
      pattern: "EXPERIMENTS_DIR"
---

<objective>
Add the `kajiba experiment` Click command group (mirroring the existing `config` group) with the primary `log` subcommand and a minimal `list` read-back subcommand. `log` supports file-first input (`--from`), scalar override flags, and an interactive Rich fallback — all funnelling through `experiment_store.log_experiment` so there is one write path.

Purpose: Satisfy ELOG-01 — a developer records an eval run from the CLI with no live Hermes session. Depends on 11-01 (needs `log_experiment`, `EXPERIMENTS_DIR`, and the canonical fixture).
Output: `experiment` group + `log`/`list` commands in `src/kajiba/cli.py`; `tests/test_cli_experiment.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-experiment-logging-private-store/11-CONTEXT.md
@.planning/phases/11-experiment-logging-private-store/11-RESEARCH.md
@.planning/phases/11-experiment-logging-private-store/11-PATTERNS.md
@.planning/phases/11-experiment-logging-private-store/11-VALIDATION.md
@.planning/phases/11-experiment-logging-private-store/11-01-SUMMARY.md
@src/kajiba/cli.py
@src/kajiba/schema.py
</context>

<artifacts_this_phase_produces>
This plan creates the following NEW symbols (downstream drift checks must treat these as newly-created):

- `experiment` Click group — in `src/kajiba/cli.py` (mirrors the `config` group at line 677)
- `experiment_log` command (`kajiba experiment log`) — in `src/kajiba/cli.py`
- `experiment_list` command (`kajiba experiment list`) — in `src/kajiba/cli.py`
- CLI flags `--from`, `--score`, `--type`, `--task-category`, `--local-model` on `experiment log`
- `tests/test_cli_experiment.py` — NEW test file

Symbols this plan CONSUMES from 11-01 (already created, not drift): `log_experiment`, `EXPERIMENTS_DIR`, `tests/fixtures/experiment_run.example.json`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 CLI test stubs for experiment log/list (ELOG-01)</name>
  <files>tests/test_cli_experiment.py</files>
  <read_first>
    - tests/test_cli.py (CliRunner + runner fixture lines 1-30; monkeypatch.setattr("kajiba.cli.<DIR>", ...) dir-isolation idiom at lines 147/212-214; scripted input= at line 216)
    - .planning/phases/11-experiment-logging-private-store/11-VALIDATION.md (exact CLI test function names: test_log_from_file, test_log_scalar_overrides, test_log_interactive, test_list)
    - .planning/phases/11-experiment-logging-private-store/11-RESEARCH.md (Pitfall 2 monkeypatch isolation; Pitfall 5 scripted input for interactive)
    - tests/fixtures/experiment_run.example.json (the --from fixture created in 11-01)
  </read_first>
  <behavior>
    - test_log_from_file: invoking ["experiment","log","--from",<fixture>] exits 0, prints a path containing "exp_" and ".json", and the file exists under the monkeypatched EXPERIMENTS_DIR (not under staging/outbox).
    - test_log_scalar_overrides: invoking ["experiment","log","--from",<fixture>,"--score","0.5"] persists a record whose written JSON has outcome.eval_score == 0.5 (override applied before validation).
    - test_log_interactive: invoking ["experiment","log"] with scripted input= persists a record (covers the Rich fallback path without hanging).
    - test_list: after logging a record, ["experiment","list"] exits 0 and its output contains the logged record_id (or its type/task), confirming read-back.
  </behavior>
  <action>
    Create tests/test_cli_experiment.py with a runner fixture (CliRunner) and the four functions named exactly per 11-VALIDATION.md. Isolate the store with `monkeypatch.setattr("kajiba.cli.EXPERIMENTS_DIR", tmp_path / "experiments")` (and KAJIBA_BASE where _ensure_dirs runs) per RESEARCH Pitfall 2 — never write to the real ~/.hermes. Drive test_log_from_file and test_log_scalar_overrides via flags only (no input=). Cover test_log_interactive with scripted `input="...\n...\n"` matching the prompt order the log command will use (Pitfall 5). Assert via result.exit_code == 0 and result.output substring checks plus reading the written exp_*.json back with json.loads. Use the 11-01 fixture path for --from. These stubs WILL FAIL until Task 2 adds the experiment group (RED). Write the test file directly — no fenced implementation in this action.
  </action>
  <verify>
    <automated>python -m pytest tests/test_cli_experiment.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_cli_experiment.py contains def test_log_from_file, def test_log_scalar_overrides, def test_log_interactive, and def test_list
    - tests/test_cli_experiment.py contains `monkeypatch.setattr("kajiba.cli.EXPERIMENTS_DIR"`
    - `python -m pytest tests/test_cli_experiment.py --collect-only -q` lists all four tests without collection errors
  </acceptance_criteria>
  <done>All four CLI test functions exist, collect cleanly, and use monkeypatched store isolation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: experiment group + log + list subcommands (ELOG-01, D-09..D-12)</name>
  <files>src/kajiba/cli.py</files>
  <read_first>
    - src/kajiba/cli.py (config group at 677-682 — exact group template; config_show Table at 703-719 — the list table model; module-level console at 58; existing schema import block at 49-53 to extend)
    - src/kajiba/schema.py (load_record at 515; ExperimentRecord at 432; EXPERIMENT_TYPES at 114 for --type click.Choice; ExperimentMetadata/ExperimentOutcome/ModelMetadata field sets)
    - src/kajiba/experiment_store.py (log_experiment signature + build_experiment_record signature — created in 11-01; the single write path the CLI MUST call, D-08)
    - .planning/phases/11-experiment-logging-private-store/11-RESEARCH.md (Pattern 3 group/log skeleton 283-309; scalar-override-before-validation note 391-408; minimal list example 410-429; Pitfall 1 load_record-not-validate_record; Pitfall 3 nested ModelMetadata via --local-model)
    - .planning/phases/11-experiment-logging-private-store/11-PATTERNS.md (experiment group section 87-114)
    - tests/test_cli_experiment.py (the four behaviors to satisfy — created in Task 1)
  </read_first>
  <action>
    Extend the kajiba.schema import in cli.py to also import `EXPERIMENT_TYPES, ExperimentRecord, load_record`, and import `build_experiment_record, log_experiment` from kajiba.experiment_store. Declare `@cli.group()` `def experiment()` with docstring "Log and inspect private model-experiment runs." (mirror the config group at line 677, but a plain group without invoke_without_command is fine).
    Add `@experiment.command("log")` `def experiment_log(...)` with options: `--from` (dest from_path, click.Path(exists=True), default None, help references tests/fixtures/experiment_run.example.json as the canonical shape); `--score` (dest eval_score, click.FloatRange(0.0, 1.0), default None); `--type` (dest experiment_type, click.Choice(list(EXPERIMENT_TYPES)), default None); `--task-category` (default None); `--local-model` (dest model_json, click.Path(exists=True), default None, for the nested ModelMetadata JSON snippet, D-12). Body logic:
    (1) D-10 — file-first shared format: If --from given, `data = json.loads(Path(from_path).read_text(encoding="utf-8"))`. D-11 — apply scalar overrides to the RAW dict BEFORE validation (RESEARCH note: these models have no validate_assignment, so mutate-then-validate) — set data["outcome"]["eval_score"] when eval_score is not None, data["experiment"]["experiment_type"] when experiment_type given, data["experiment"]["task_category"] when task-category given; merge the --local-model snippet into data["experiment"]["local_model"] when provided. Then `rec = load_record(data)` (NOT validate_record — Pitfall 1; load_record routes to ExperimentRecord.model_validate so the CLI consumes the SAME file format programmatic callers produce, D-10) and `if not isinstance(rec, ExperimentRecord): raise click.ClickException("--from file is not a model_experiment record.")`.
    (2) Else if the essential scalar flags are present (at minimum --score and --type and --task-category, plus --local-model or a prompted model name): build via build_experiment_record(**fields) applying the flags (D-11 scalar convenience path).
    (3) Else (interactive fallback, D-12): use click.prompt for the essential scalars (experiment_id, task_category, task_description, eval_score, experiment_type) and the single required local_model.model_name; accept the optional --local-model snippet for the rest of ModelMetadata. Prompt only the minimal required nested field (Pitfall 3) — do NOT field-by-field prompt all nine ModelMetadata fields.
    (4) `path = log_experiment(rec, EXPERIMENTS_DIR)` (the single shared write path, D-08 — the CLI must NOT write the file itself) and `console.print` a success panel/line showing the written `path` (must include the exp_<id>.json filename in output).
    Add `@experiment.command("list")` `def experiment_list()`: `EXPERIMENTS_DIR.mkdir(parents=True, exist_ok=True)`, glob `EXPERIMENTS_DIR.glob("exp_*.json")` sorted by st_mtime descending; if empty print "[yellow]No experiments logged.[/yellow]"; else build a Rich Table (columns Record ID / Type / Task / Score) reading each file via json.loads and add a row per file; `console.print(table)`. Use Optional[X], double quotes, Google-style docstrings, lazy `%s` logging. Do NOT scrub at log time. Do NOT duplicate persistence logic — call log_experiment.
  </action>
  <verify>
    <automated>python -m pytest tests/test_cli_experiment.py -x</automated>
  </verify>
  <acceptance_criteria>
    - src/kajiba/cli.py contains `def experiment(` (the group) and `def experiment_log(` and `def experiment_list(`
    - src/kajiba/cli.py contains `log_experiment(` called inside experiment_log (CLI uses the shared write path, not its own write_text)
    - src/kajiba/cli.py contains `load_record(` (not validate_record) for the --from path and a `click.ClickException` rejecting non-experiment records
    - src/kajiba/cli.py contains `EXPERIMENT_TYPES` used in a `click.Choice`
    - `kajiba experiment log --from tests/fixtures/experiment_run.example.json` prints output containing `exp_` and `.json` (verified via the CliRunner test)
    - `python -m pytest tests/test_cli_experiment.py -x` exits 0
  </acceptance_criteria>
  <done>The experiment group with log + list exists; log handles --from, scalar overrides applied pre-validation, and interactive fallback; it rejects coding-session files; it writes exclusively through log_experiment to EXPERIMENTS_DIR; list reads runs back; all four CLI tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user CLI → experiment_log | A user supplies a --from JSON file path and/or scalar flags; untrusted file content crosses here |
| experiment_log → experiment_store | Validated ExperimentRecord handed to the single write path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Tampering | malformed/hostile --from JSON | mitigate | `json.loads` (no eval) + Pydantic validation via `load_record`; non-model_experiment records rejected with a clear `click.ClickException`. Scalar overrides applied to the raw dict BEFORE validation so Pydantic field validators (eval_score bound [0,1], EXPERIMENT_TYPES vocab) catch bad values |
| T-11-06 | Information Disclosure | CLI writing an experiment outside the private store | mitigate | experiment_log calls log_experiment(rec, EXPERIMENTS_DIR) only; the store's structural guard (T-11-01) refuses any non-experiments dir. CLI never writes the file itself |
| T-11-07 | Denial of Service | interactive prompt hangs automated callers | accept | Documented test pattern uses scripted input=; flag-driven and --from paths are non-interactive. Interactive mode is an intentional HITL convenience, not an automation surface |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed this plan (stdlib + pinned click/rich/pydantic). Package legitimacy gate N/A (RESEARCH) |
</threat_model>

<verification>
- `python -m pytest tests/test_cli_experiment.py -x` fully green (all four tests).
- `kajiba experiment --help` lists `log` and `list` subcommands.
- `kajiba experiment log --from tests/fixtures/experiment_run.example.json` writes one exp_*.json and prints its path (manual smoke against real ~/.hermes, see 11-VALIDATION Manual-Only section).
- `python -m pytest` full suite remains green (no regressions to existing CLI tests).
</verification>

<success_criteria>
- ELOG-01 satisfied: `kajiba experiment log` records an ExperimentRecord with no live Hermes session via --from, via scalar flags, and via interactive fallback.
- Scalar overrides apply before validation; coding-session files are rejected.
- `kajiba experiment list` confirms logged runs (read-back, Claude's discretion).
- The CLI funnels every write through experiment_store.log_experiment (D-08, no duplicate persistence).
</success_criteria>

<output>
Create `.planning/phases/11-experiment-logging-private-store/11-02-SUMMARY.md` when done.
</output>
