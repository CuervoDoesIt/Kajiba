---
phase: 12-eval-scoring-scrub-tuning
plan: 03
type: tdd
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/kajiba/experiment_scrub.py
autonomous: true
requirements: [EEVAL-02]

must_haves:
  truths:
    - "scrub_experiment redacts PII from the four free-text fields (local_model_output, reviewer_critique, task_description, lessons_learned) via the shared scrub_text engine"
    - "model_hash, model identity, and the full HardwareProfile survive scrubbing BYTE-IDENTICAL (D-05/D-06)"
    - "lessons_learned stays a list (per-element scrub, not stringified) — Pitfall 1"
    - "scrub_experiment NEVER imports or calls privacy.anonymize_hardware / apply_consent_level / generalize_gpu_name / round_to_tier (D-05)"
    - "A ScrubLog is returned with per-category counts matching the redactions (D-10)"
    - "Implemented as a new single-responsibility module src/kajiba/experiment_scrub.py that reuses scrub_text/SCRUB_PATTERNS rather than forking the regex engine — shared-core / divergent-tail stance (D-09)"
  artifacts:
    - path: "src/kajiba/experiment_scrub.py"
      provides: "Field-allowlist experiment scrub reusing scrub_text; returns (ExperimentRecord, ScrubLog); preserves model/hardware exactly"
      exports: ["scrub_experiment"]
      min_lines: 40
      contains: "scrub_experiment"
  key_links:
    - from: "src/kajiba/experiment_scrub.py"
      to: "kajiba.scrubber.scrub_text"
      via: "import + call on each allowlist field (NOT a forked regex engine)"
      pattern: "from kajiba.scrubber import scrub_text"
    - from: "src/kajiba/experiment_scrub.py"
      to: "kajiba.schema.ScrubLog"
      via: "import + construct redaction-accounting log"
      pattern: "ScrubLog"
    - from: "tests/test_experiment_scrub.py"
      to: "kajiba.experiment_scrub.scrub_experiment"
      via: "import + call"
      pattern: "scrub_experiment"
---

<objective>
Create `src/kajiba/experiment_scrub.py` — an experiment-aware PII scrub (EEVAL-02) that
redacts the four caller-supplied free-text fields while PRESERVING model identity and
hardware EXACTLY. It REUSES `scrub_text`/`SCRUB_PATTERNS` verbatim (D-10, no fork), applies
them only to a field allowlist (D-06/D-07), and explicitly BYPASSES `privacy.py`'s hardware
anonymization (D-05) — the deliberate inverse of the community pipeline. Returns
`(scrubbed ExperimentRecord, ScrubLog)`.

Purpose: Experiments must be shareable-safe without losing the model/hardware context that
makes them analytically useful. This is the scrub primitive; the actual share-boundary
write fires in Phase 15 (D-08 — store raw, scrub at export).
Output: One new module turning Plan 01's `test_experiment_scrub.py` GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-eval-scoring-scrub-tuning/12-RESEARCH.md
@.planning/phases/12-eval-scoring-scrub-tuning/12-PATTERNS.md
@src/kajiba/scrubber.py
</context>

<host_environment>
Executed on Windows 11 / PowerShell (Bash tool also available). All `<automated>` and
acceptance checks below are portable single `python -c "..."` invocations or `python -m pytest`
calls — no POSIX `grep`/`if [ $? -eq 0 ]`. The "no privacy import" assertion is a Python
source scan, not `grep -c`.
</host_environment>

<artifacts_this_phase_produces>
This plan (12-03) produces:
- `src/kajiba/experiment_scrub.py` — module
- `scrub_experiment(record: ExperimentRecord) -> tuple[ExperimentRecord, ScrubLog]` — public entrypoint
- (optional internal) `_apply`-style closure folding `scrub_text` stats into a counts dict
Reuses (does NOT create): `scrubber.scrub_text`, `scrubber.SCRUB_PATTERNS`, `schema.ScrubLog`, `schema.ExperimentRecord`.
Allowlist surfaces scrubbed: `experiment.task_description`, `outcome.local_model_output`, `outcome.reviewer_critique`, each element of `outcome.lessons_learned`.
Preserved exactly: `model`, `hardware`, `experiment.local_model`, `experiment.reviewer_model`, `model_hash`, `eval_score`, `drift_flag`, `recommended_action`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement experiment_scrub.py (RED→GREEN against test_experiment_scrub.py)</name>
  <files>src/kajiba/experiment_scrub.py</files>
  <read_first>
    - src/kajiba/scrubber.py (scrub_text contract lines 229-281 — returns ScrubResult with .scrubbed_text/.stats/.flagged, early-returns on falsy input; the count-fold idiom in _scrub_string_fields_in_turn lines 290-309; the scrub_record model_dump→mutate→ScrubLog→model_validate ENVELOPE lines 314-370 incl. the api_keys+hex_tokens fold lines 357-366 — mirror the envelope, NOT the deep-walk)
    - src/kajiba/schema.py (ExperimentRecord lines 432-492, ExperimentMetadata lines 408-418, ExperimentOutcome lines 421-429 — exact allowlist field names + Optional reviewer_critique; ScrubLog lines 222-233 — exact field names; ModelMetadata model_hash line ~177; HardwareProfile lines 180-190)
    - src/kajiba/privacy.py (anonymize_hardware line 115, generalize_gpu_name line 73, round_to_tier line 89, apply_consent_level line 189 — the SKIP boundary; this module must NEVER import these, D-05)
    - tests/test_experiment_scrub.py (the four contract tests this module must satisfy — created in Plan 01)
    - .planning/phases/12-eval-scoring-scrub-tuning/12-RESEARCH.md (Pattern 2: the allowlist orchestration; Pitfall 1 lessons_learned list shape; Pitfall 2 Optional reviewer_critique; Pitfall 5 model_hash survival = the EEVAL-02 acceptance signal; Open Q2 (RESOLVED) potential_names_redacted=0)
    - .planning/phases/12-eval-scoring-scrub-tuning/12-PATTERNS.md (experiment_scrub.py section — envelope-mirror + allowlist + anti-analog deep-walk)
  </read_first>
  <behavior>
    - test_free_text_redacted: PII (email/path) removed from local_model_output, task_description, and the PII-bearing lessons_learned element
    - test_model_and_hardware_preserved: local_model.model_hash, local_model.model_name, full hardware object, reviewer_model byte-identical pre/post scrub
    - test_scrublog_and_outcome_fields: ScrubLog counts non-zero & matching categories; eval_score/drift_flag/recommended_action unchanged
    - test_lessons_list_shape: lessons_learned still a list, same length, per-element scrubbed
  </behavior>
  <action>
    Create `src/kajiba/experiment_scrub.py`. Module docstring stating it is the experiment-aware divergent-tail scrub: reuses the shared `scrub_text` engine on a free-text allowlist, preserves model/hardware EXACTLY, and deliberately bypasses `privacy.py` (D-05/D-06/D-07/D-10). Imports: `import logging`, `from kajiba.scrubber import scrub_text`, `from kajiba.schema import ExperimentRecord, ScrubLog`, module-level `logger = logging.getLogger(__name__)`. Do NOT import anything from `kajiba.privacy`.
    `scrub_experiment(record: ExperimentRecord) -> tuple[ExperimentRecord, ScrubLog]`:
      - `data = record.model_dump(mode="json", by_alias=True)` (deep copy; never mutate the original — mirror scrub_record envelope).
      - Accumulate `counts: dict[str, int] = {}` and `flagged = 0`. Define a local `_apply(text)` closure that calls `scrub_text(text)`, folds `res.stats` into `counts`, adds `len(res.flagged)` to `flagged`, and returns `res.scrubbed_text`.
      - ALLOWLIST ONLY (D-07): `data["experiment"]["task_description"] = _apply(...)`; `data["outcome"]["local_model_output"] = _apply(...)`; guard `if data["outcome"].get("reviewer_critique"): data["outcome"]["reviewer_critique"] = _apply(...)` (Optional, Pitfall 2); `data["outcome"]["lessons_learned"] = [_apply(s) for s in data["outcome"].get("lessons_learned", [])]` (per-element, preserve list shape — Pitfall 1).
      - DO NOT iterate or touch `data["model"]`, `data["hardware"]`, `experiment.local_model`, `experiment.reviewer_model`, `model_hash`, `eval_score`, `drift_flag`, `recommended_action` (D-05/D-06). NEVER call any `privacy.*` function.
      - Build `ScrubLog` folding `api_keys` + `hex_tokens` into `api_keys_redacted` (mirror scrub_record lines 357-366); map `file_paths`/`emails`/`network`/`phone`/`crypto`/`connection_strings` to their fields; `items_flagged=flagged`; leave `potential_names_redacted=0` (no regex source — Open Q2 RESOLVED).
      - Rebuild via `ExperimentRecord.model_validate(data)` and return `(scrubbed_record, scrub_log)`.
    Use Google-style docstrings, double quotes, modern generics (`dict[str,int]`, `tuple[...]`), `%s` lazy logging, never log raw PII.
  </action>
  <acceptance_criteria>
    - Source: `experiment_scrub.py` contains `from kajiba.scrubber import scrub_text` and `def scrub_experiment(` and constructs a `ScrubLog`.
    - Source (no privacy coupling, portable / Windows-safe): `python -c "import re,pathlib; src=pathlib.Path('src/kajiba/experiment_scrub.py').read_text(encoding='utf-8'); assert 'kajiba.privacy' not in src and 'import privacy' not in src; assert not re.search(r'anonymize_hardware|generalize_gpu_name|round_to_tier|apply_consent_level',src); print('no privacy.* coupling')"` (D-05 SKIP boundary honored).
    - Behavior (redaction): scrubbing experiment_pii.json removes the `@`-email and `/`-path from `outcome.local_model_output` and `experiment.task_description` and the PII lessons element.
    - Behavior (preservation, the EEVAL-02 acceptance signal — Pitfall 5): scrubbed `local_model.model_hash`, `local_model.model_name`, full `hardware`, and `reviewer_model` are byte-identical to pre-scrub (`model_dump` equality).
    - Behavior (shape): `outcome.lessons_learned` is still a `list` of the same length after scrub.
    - Behavior: `python -m pytest tests/test_experiment_scrub.py -x -q` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_experiment_scrub.py -x -q</automated>
  </verify>
  <done>scrub_experiment redacts the four free-text surfaces, preserves model_hash/model identity/full hardware/reviewer_model byte-identical, keeps lessons_learned as a same-length list, returns a populated ScrubLog, never imports privacy.*; test_experiment_scrub.py fully GREEN; full suite no regressions (`python -m pytest -q`).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw ExperimentRecord (private store) → scrub_experiment → shareable record | The scrub is the share-boundary transform; untrusted free text crosses out, model/hardware identity must be deliberately preserved (not leaked accidentally, not destroyed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Information Disclosure | free-text allowlist | mitigate | ALL four free-text surfaces (local_model_output, reviewer_critique, task_description, lessons_learned[]) routed through scrub_text (D-07); test_free_text_redacted asserts redaction on each — a missing surface fails the test (regression catch) |
| T-12-06 | Tampering (over-scrub destroys analysis fields) | model_hash / hardware preservation | mitigate | Field allowlist NOT pattern denylist (D-06); byte-identical preservation test (test_model_and_hardware_preserved, Pitfall 5); portable source scan asserts NO privacy.* import (D-05) |
| T-12-08 | Information Disclosure (raw PII at rest) | store-raw invariant | accept | AR-11-01 accepted at rest in the private no-publish store (D-08); this plan provides the scrub PRIMITIVE only — it returns a scrubbed copy and never overwrites the raw store; the actual export-write gate is Phase 15 |
| T-12-09 | Repudiation/Logging | logger usage | mitigate | `%s` lazy logging; never log raw `local_model_output`/`reviewer_critique` at INFO (CLAUDE.md never-log-PII) |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Phase installs ZERO packages (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `python -m pytest tests/test_experiment_scrub.py -x -q` — all four EEVAL-02 contract tests GREEN.
- `python -m pytest -q` — full suite green; golden-ID tripwire confirms schema un-mutated (D-03/D-08 — scrub returns a copy, never persists/overwrites).
- Portable no-privacy-coupling scan (Windows/PowerShell-safe): `python -c "import re,pathlib; src=pathlib.Path('src/kajiba/experiment_scrub.py').read_text(encoding='utf-8'); assert 'kajiba.privacy' not in src and not re.search(r'anonymize_hardware|generalize_gpu_name|round_to_tier|apply_consent_level',src); print('no privacy coupling')"` (D-05 SKIP boundary honored).
</verification>

<success_criteria>
- scrub_experiment exists, reuses scrub_text (no forked regex), returns (ExperimentRecord, ScrubLog).
- Four free-text surfaces redacted; model_hash/identity/hardware/reviewer_model byte-identical.
- lessons_learned list shape preserved; outcome scalar fields unchanged.
- No privacy.* import; no schema mutation; full suite green.
</success_criteria>

<output>
Create `.planning/phases/12-eval-scoring-scrub-tuning/12-03-SUMMARY.md` when done.
</output>
