---
phase: 12-eval-scoring-scrub-tuning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/experiment_complete.json
  - tests/fixtures/experiment_thin.json
  - tests/fixtures/experiment_pii.json
  - tests/test_eval_scorer.py
  - tests/test_experiment_scrub.py
autonomous: true
requirements: [EEVAL-01, EEVAL-02]

must_haves:
  truths:
    - "Three experiment fixtures exist and load via load_record() as ExperimentRecord without error"
    - "experiment_thin.json contains ONLY schema-required fields (no reviewer_model/critique, no hardware, no lessons_learned, no recommended_action, no completed_at, no model_hash) so the locked thin-band scoring contract holds (composite < 0.50)"
    - "experiment_pii.json carries real PII in free-text fields AND a real hex model_hash + GPU name (the preservation proof inputs)"
    - "tests/test_eval_scorer.py and tests/test_experiment_scrub.py exist with named test functions that fail (RED) pending Wave 2 implementation"
  artifacts:
    - path: "tests/fixtures/experiment_complete.json"
      provides: "Fully-populated ExperimentRecord (reviewer_model, lessons_learned, recommended_action, model_hash, hardware) — complete-band input"
      contains: "model_experiment"
    - path: "tests/fixtures/experiment_thin.json"
      provides: "Minimal valid ExperimentRecord (required fields only) — thin-band input; provably scores < PARTIAL_THRESHOLD under Plan 02's locked scoring contract"
      contains: "model_experiment"
    - path: "tests/fixtures/experiment_pii.json"
      provides: "PII in local_model_output/task_description/lessons_learned + hex model_hash + gpu_name — scrub redaction + preservation proof input"
      contains: "model_experiment"
    - path: "tests/test_eval_scorer.py"
      provides: "EEVAL-01 RED test scaffold (complete/thin bands, vocabulary-distinct, experiment-only guard)"
      contains: "_load_fixture"
    - path: "tests/test_experiment_scrub.py"
      provides: "EEVAL-02 RED test scaffold (free-text redacted, model/hardware byte-identical, ScrubLog counts, lessons list shape)"
      contains: "_load_fixture"
  key_links:
    - from: "tests/test_eval_scorer.py"
      to: "kajiba.schema.load_record"
      via: "import + fixture load"
      pattern: "load_record"
    - from: "tests/test_experiment_scrub.py"
      to: "kajiba.schema.load_record"
      via: "import + fixture load"
      pattern: "load_record"
---

<objective>
Establish the Wave 0 test foundation for Phase 12: three experiment fixtures and two
RED test scaffolds that the eval scorer (Plan 02) and experiment scrub (Plan 03) make
pass. This satisfies the Nyquist contract — implementation tasks in later waves have an
automated verify available the moment they run.

Purpose: Without committed fixtures and named failing tests, the scorer/scrub plans
would have nothing to verify against. This plan defines the contracts (fixture shapes +
test names) that Plans 02/03 implement to. The thin fixture is pinned to required-fields-only
because Plan 02's locked scoring contract relies on it to land in band `thin`.
Output: 3 fixtures + 2 test files (RED), no production code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-eval-scoring-scrub-tuning/12-RESEARCH.md
@.planning/phases/12-eval-scoring-scrub-tuning/12-PATTERNS.md
@.planning/phases/12-eval-scoring-scrub-tuning/12-VALIDATION.md
</context>

<host_environment>
This phase is executed on Windows 11 with PowerShell as the default shell (the Bash tool
is also available). Every `<automated>` command in this phase is a single portable
`python -c "..."` invocation or a `python -m pytest ...` call — both run identically under
PowerShell and bash. The RED-state check below uses a Python wrapper that inverts pytest's
exit code rather than a POSIX `if [ $? -eq 0 ]` test, so it needs no shell-specific syntax.
</host_environment>

<artifacts_this_phase_produces>
This plan (12-01) produces ONLY test infrastructure — no production symbols:
- `tests/fixtures/experiment_complete.json`
- `tests/fixtures/experiment_thin.json`
- `tests/fixtures/experiment_pii.json`
- `tests/test_eval_scorer.py` (RED scaffold; targets `kajiba.eval_scorer.compute_eval_confidence` + `EvalConfidenceResult`, created in Plan 02)
- `tests/test_experiment_scrub.py` (RED scaffold; targets `kajiba.experiment_scrub.scrub_experiment`, created in Plan 03)

Symbols produced by LATER plans in this phase (do NOT exist yet, listed for downstream convergence exclusion):
- `src/kajiba/eval_scorer.py` → `compute_eval_confidence`, `EvalConfidenceResult`, `WEIGHTS`, `COMPLETE_THRESHOLD`, `PARTIAL_THRESHOLD` (Plan 02)
- `src/kajiba/experiment_scrub.py` → `scrub_experiment` (Plan 03)
- `kajiba experiment score`, `kajiba experiment scrub` CLI subcommands + `list` Confidence column (Plan 04)
- `__init__.py` re-exports of `compute_eval_confidence`, `scrub_experiment` (Plan 04)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author three experiment fixtures (complete / thin / pii)</name>
  <files>tests/fixtures/experiment_complete.json, tests/fixtures/experiment_thin.json, tests/fixtures/experiment_pii.json</files>
  <read_first>
    - tests/fixtures/experiment_run.example.json (the only existing ExperimentRecord fixture — copy its shape exactly)
    - src/kajiba/schema.py (ExperimentRecord lines 432-492, ExperimentMetadata lines 408-418, ExperimentOutcome lines 421-429, ModelMetadata lines 166-177, HardwareProfile lines 180-190, EXPERIMENT_TYPES line 114, RECOMMENDED_ACTIONS line 117 — exact required/optional fields)
    - .planning/phases/12-eval-scoring-scrub-tuning/12-RESEARCH.md (Verified Schema Surface tables — which fields are required vs optional)
    - .planning/phases/12-eval-scoring-scrub-tuning/12-02-PLAN.md (`<scoring_contract>` — the thin fixture MUST be required-fields-only so it scores band `thin`)
  </read_first>
  <action>
    Create three `model_experiment` fixtures (each `"schema_version": "0.2.0"`, `"record_kind": "model_experiment"`), mirroring the structure of `experiment_run.example.json`.
    `experiment_complete.json` (complete-band input per D-01): populate ALL completeness signals — `experiment.local_model` with `model_name`/`quantization`/`provider`/`is_local` AND `model_hash` set to a real 64-hex string; `experiment.reviewer_model` present; `experiment.completed_at` present; a populated top-level `hardware` HardwareProfile (`gpu_name`, `gpu_vram_gb`, `gpu_count`, `cpu_name`, `ram_gb`, `os`, `inference_backend`, `cuda_version`); `outcome.local_model_output` non-trivial (>= 20 chars); `outcome.reviewer_critique` present; `outcome.lessons_learned` with >=2 elements; `outcome.recommended_action` from RECOMMENDED_ACTIONS; `outcome.eval_score` in [0,1].
    `experiment_thin.json` (thin-band input — REQUIRED FIELDS ONLY, this is load-bearing for Plan 02's thin-band contract): include ONLY schema-required fields — `experiment.experiment_id`/`experiment_type`/`local_model` (with ONLY `model_name`, NO `quantization`/`provider`)/`task_category`/`task_description`/`started_at`, and `outcome.local_model_output`/`eval_score`. OMIT every optional field: NO `reviewer_model`, NO `reviewer_critique`, NO top-level `hardware`, NO `lessons_learned` (omit the key or `[]`), NO `recommended_action`, NO `completed_at`, NO `model_hash`. Do NOT add `quantization` or `provider` to `local_model` — only `model_name`. (Under Plan 02's locked WEIGHTS this yields composite ≈ 0.367 < 0.50 → band `thin`; adding any optional field risks pushing it to `partial` and breaking `test_thin_experiment_scores_thin`.)
    `experiment_pii.json` (scrub proof input per D-05/D-06/D-07): put PII into the four free-text allowlist surfaces — an email + a unix file path in `outcome.local_model_output`; an email or API-key-like token in `task_description`; PII in at least ONE element of a multi-element `lessons_learned` list (Pitfall 1 coverage); AND set a real hex `model_hash` (64 hex chars) on `local_model`, a recognizable `hardware.gpu_name` (e.g. "NVIDIA GeForce RTX 4070"), `gpu_vram_gb`, `os`, `cuda_version` — these are the BYTE-IDENTICAL preservation targets (Pitfall 5).
    Keep all `started_at`/`completed_at` as ISO-8601 strings (e.g. `"2026-06-03T12:00:00Z"`). Use double-quoted JSON keys/values.
  </action>
  <acceptance_criteria>
    - All three files parse as JSON and round-trip through `kajiba.schema.load_record(data)` to an `ExperimentRecord` instance (no `ValidationError`).
    - `experiment_pii.json` `local_model.model_hash` is a 64-char hex string; `outcome.local_model_output` contains an `@`-bearing email substring and a `/`-bearing path; `outcome.lessons_learned` has >= 2 elements with PII in at least one.
    - `experiment_thin.json` is required-fields-only: it has NO `reviewer_model`, NO `reviewer_critique`, NO `hardware`, NO `lessons_learned` (or empty), NO `recommended_action`, NO `completed_at`, NO `model_hash` keys, and `local_model` carries ONLY `model_name` (no `quantization`/`provider`). Portable check: `python -c "import json; d=json.load(open('tests/fixtures/experiment_thin.json',encoding='utf-8')); exp=d['experiment']; lm=exp['local_model']; out=d['outcome']; assert 'reviewer_model' not in exp and 'completed_at' not in exp; assert 'reviewer_critique' not in out and 'recommended_action' not in out and not out.get('lessons_learned'); assert 'hardware' not in d or not d['hardware']; assert 'model_hash' not in lm and 'quantization' not in lm and 'provider' not in lm; print('thin fixture is required-fields-only')"`.
    - Behavior: each fixture loads as ExperimentRecord (verify command below). Equivalent assertion lives in Task 2's tests.
  </acceptance_criteria>
  <verify>
    <automated>python -c "import json; from kajiba.schema import load_record, ExperimentRecord; [print(p, isinstance(load_record(json.load(open('tests/fixtures/'+p, encoding='utf-8'))), ExperimentRecord)) for p in ('experiment_complete.json','experiment_thin.json','experiment_pii.json')]"</automated>
  </verify>
  <done>All three fixtures load via load_record() as ExperimentRecord; pii fixture has PII in the four allowlist surfaces + a 64-hex model_hash and a GPU name; thin fixture is required-fields-only (only model_name on local_model; all optional completeness fields omitted) so Plan 02's thin-band contract holds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author RED test scaffolds for eval scorer and experiment scrub</name>
  <files>tests/test_eval_scorer.py, tests/test_experiment_scrub.py</files>
  <read_first>
    - tests/test_scorer.py (the `_load_fixture` helper + import-public-API structure to mirror)
    - tests/test_scrubber.py (scrub assertion idioms — redaction present, counts)
    - tests/test_cli_experiment.py (lines 1-31 — fixture-load + path patterns)
    - .planning/phases/12-eval-scoring-scrub-tuning/12-RESEARCH.md (Phase Requirements → Test Map: exact test function names required)
    - .planning/phases/12-eval-scoring-scrub-tuning/12-PATTERNS.md (Tests section — analog mapping)
  </read_first>
  <action>
    Create `tests/test_eval_scorer.py` mirroring `test_scorer.py`'s `FIXTURES = Path(__file__).parent / "fixtures"` + `_load_fixture(name)` helper. Import from the NOT-YET-EXISTING module: `from kajiba.eval_scorer import compute_eval_confidence, EvalConfidenceResult` and `from kajiba.schema import load_record, KajibaRecord`. Define these test functions (names are the locked contract from RESEARCH Test Map):
      - `test_complete_experiment_scores_complete` — load `experiment_complete.json` → load_record → `compute_eval_confidence` → assert `result.confidence_band == "complete"` and `result.composite_score >= 0.80`.
      - `test_thin_experiment_scores_thin` — load `experiment_thin.json` → assert `result.confidence_band == "thin"` AND `result.composite_score < 0.50` (the required-fields-only fixture must stay below PARTIAL_THRESHOLD).
      - `test_band_vocabulary_distinct` — across all three fixtures, assert each `confidence_band` is in `{"complete","partial","thin"}` and NEVER in `{"gold","silver","bronze","review_needed"}` (D-02).
      - `test_experiment_only` — build/load a `KajibaRecord` (reuse an existing `*_trajectory.json` fixture) and assert `compute_eval_confidence` rejects it (raises `TypeError`/`ValueError` or a documented guard).
    Create `tests/test_experiment_scrub.py` mirroring `test_scrubber.py`/`test_scorer.py`. Import `from kajiba.experiment_scrub import scrub_experiment` and schema types. Define (locked names):
      - `test_free_text_redacted` — scrub `experiment_pii.json` record → assert the email/path PII is GONE from `outcome.local_model_output` and `experiment.task_description` and the PII-bearing `lessons_learned` element (substring assertions on `@`/path no longer present).
      - `test_model_and_hardware_preserved` — assert scrubbed record's `local_model.model_hash`, `local_model.model_name`, the full `hardware` object, and `experiment.reviewer_model` are EQUAL to the pre-scrub values (byte-identical via `model_dump` comparison) — Pitfall 5 / D-05/D-06.
      - `test_scrublog_and_outcome_fields` — assert returned `ScrubLog` has non-zero counts matching the injected PII categories, and `outcome.eval_score`/`drift_flag`/`recommended_action` are unchanged.
      - `test_lessons_list_shape` — assert scrubbed `outcome.lessons_learned` is still a `list` with the same length (Pitfall 1 — not stringified).
    Each test should FAIL at import/collection time (modules don't exist yet) — that IS the RED state. Do NOT add `@pytest.mark.skip`; a collection error / ImportError is the expected RED signal. Use Google-style one-line docstrings, double quotes, modern generics.
  </action>
  <acceptance_criteria>
    - `tests/test_eval_scorer.py` defines exactly the four named functions above; `tests/test_experiment_scrub.py` defines exactly the four named functions above. Portable check (Windows-safe): `python -c "import re,pathlib; need={'test_complete_experiment_scores_complete','test_thin_experiment_scores_thin','test_band_vocabulary_distinct','test_experiment_only'}; src=pathlib.Path('tests/test_eval_scorer.py').read_text(encoding='utf-8'); got=set(re.findall(r'def (test_\w+)',src)); assert need<=got, need-got; need2={'test_free_text_redacted','test_model_and_hardware_preserved','test_scrublog_and_outcome_fields','test_lessons_list_shape'}; src2=pathlib.Path('tests/test_experiment_scrub.py').read_text(encoding='utf-8'); got2=set(re.findall(r'def (test_\w+)',src2)); assert need2<=got2, need2-got2; print('all 8 contract tests present')"`.
    - Behavior (RED): the suite collects the new tests and exits NON-ZERO due to `ModuleNotFoundError: kajiba.eval_scorer` / `kajiba.experiment_scrub` (proves tests target the right modules and are not vacuously passing). Verified by the inverted-exit-code wrapper in `<verify>`.
    - Source: both files contain `_load_fixture` and reference fixture filenames `experiment_complete.json`, `experiment_thin.json`, `experiment_pii.json`.
    - Source: `tests/test_eval_scorer.py` asserts band membership against `complete/partial/thin` and explicitly NOT against `gold`/`silver`/`bronze`.
  </acceptance_criteria>
  <verify>
    <automated>python -c "import subprocess,sys; r=subprocess.run([sys.executable,'-m','pytest','tests/test_eval_scorer.py','tests/test_experiment_scrub.py','-q']); sys.exit(0 if r.returncode!=0 else 1)"</automated>
  </verify>
  <done>Both test files exist with the eight contract-named test functions; the suite collects them and fails ONLY because kajiba.eval_scorer / kajiba.experiment_scrub do not yet exist (RED — proven by the inverted-exit-code wrapper). Full existing suite still passes: `python -m pytest -q --ignore=tests/test_eval_scorer.py --ignore=tests/test_experiment_scrub.py`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture JSON → load_record() | Test fixtures are untrusted-shaped input parsed by Pydantic; malformed fixtures must fail at validation, not silently mis-load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | experiment_pii.json | mitigate | Fixture must carry a REAL hex model_hash + GPU name so the Plan 03 preservation test (T-12-06) cannot pass vacuously; acceptance asserts 64-hex model_hash present |
| T-12-02 | Information Disclosure | RED test design | accept | Tests reference PII strings inline in fixtures (private test data, never published; experiments are no-publish per D-08 / ELOG-03). No mitigation needed beyond keeping fixtures out of community paths |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Phase installs ZERO packages (RESEARCH Package Legitimacy Audit: not applicable); no install task in this plan |
</threat_model>

<verification>
- `python -m pytest -q --ignore=tests/test_eval_scorer.py --ignore=tests/test_experiment_scrub.py` — full pre-existing suite stays green (no regression from new fixtures).
- New scaffolds are RED (ModuleNotFoundError) by design — confirmed via the inverted-exit-code Python wrapper in Task 2 (portable; no POSIX `if [ $? -eq 0 ]`).
</verification>

<success_criteria>
- Three fixtures load as ExperimentRecord via load_record().
- thin fixture is required-fields-only (provably scores < 0.50 under Plan 02's contract).
- pii fixture has PII in all four allowlist surfaces + hex model_hash + GPU name.
- Eight contract-named tests exist and fail only on missing Plan 02/03 modules.
- No production code touched; existing suite unaffected.
</success_criteria>

<output>
Create `.planning/phases/12-eval-scoring-scrub-tuning/12-01-SUMMARY.md` when done.
</output>
