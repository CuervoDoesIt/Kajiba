---
phase: 06-environment-plugin-foundation
plan: 04
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - docs/hermes-setup.md
  - src/kajiba/plugin/plugin.yaml
  - src/kajiba/plugin/__init__.py
  - src/kajiba/plugin/hooks.py
requirements: [ENV-01, ENV-02, ENV-03]
autonomous: true
must_haves:
  truths:
    - "A developer can follow docs/hermes-setup.md on native Windows (no WSL2, no GPU, no Ollama) to install Hermes Agent v0.15.x, editable-install Kajiba, symlink the plugin, enable it, and see it load (D-11, D-13)"
    - "The primary path checkpoints collapse to: install Hermes (native) -> pip install -e . Kajiba -> symlink plugin -> hermes plugins enable kajiba -> plugin loads -> hooks fire (D-13)"
    - "The guide documents hermes plugins enable kajiba as a REQUIRED step (a discovered-but-disabled plugin will not load) and HERMES_PLUGINS_DEBUG=1 as Hermes's own discovery debug, alongside the retained KAJIBA_DEBUG=1 PII-in-logs security note (D-14)"
    - "WSL2 + NVIDIA GPU passthrough + Ollama (and the MP-8/MP-5/MP-9 troubleshooting) survive verbatim but are relocated into an OPTIONAL appendix for the dashboard terminal / local inference only (D-11, D-04 revised)"
    - "All v0.6.0 references are bumped to v0.15.x across docs/hermes-setup.md, plugin.yaml header comment, and plugin docstrings; no v0.6.0 strings remain outside the optional WSL2 appendix changelog/context (D-12)"
    - "The guide flags (does NOT edit) that ROADMAP Success Criterion #1 and ENV-01/ENV-02 wording still name WSL2/GPU/Ollama/v0.6.0 as required and need a later /gsd-phase reconciliation (D-20)"
  artifacts:
    - path: "docs/hermes-setup.md"
      provides: "Native-Windows-primary v0.15.x Hermes + Kajiba plugin setup guide; WSL2/GPU/Ollama demoted to optional appendix; enable + HERMES_PLUGINS_DEBUG steps folded in"
      contains: "hermes plugins enable kajiba"
      min_lines: 200
    - path: "src/kajiba/plugin/plugin.yaml"
      provides: "Plugin manifest with header comment version-reconciled to v0.15.x"
      contains: "v0.15.x"
    - path: "src/kajiba/plugin/__init__.py"
      provides: "register(ctx) entry point with docstring version-reconciled to v0.15.x"
      contains: "v0.15.x"
    - path: "src/kajiba/plugin/hooks.py"
      provides: "Hook handlers with docstrings version-reconciled to v0.15.x"
      contains: "v0.15.x"
  key_links:
    - from: "docs/hermes-setup.md"
      to: "src/kajiba/plugin/"
      via: "symlink command into the resolved Hermes plugins/ discovery dir, then hermes plugins enable kajiba"
      pattern: "hermes plugins enable kajiba"
    - from: "docs/hermes-setup.md primary path"
      to: "hermes plugins enable + HERMES_PLUGINS_DEBUG + KAJIBA_DEBUG"
      via: "documented v0.15.x operational steps"
      pattern: "HERMES_PLUGINS_DEBUG"
---

<objective>
Corrective in-place rewrite of `docs/hermes-setup.md` (D-13): make **native Windows + Hermes Agent v0.15.x + remote backends** the documented primary path, demote WSL2 + NVIDIA GPU passthrough + Ollama (and their MP-8/MP-5/MP-9 troubleshooting) to an **optional appendix**, fold in the new v0.15.x operational steps (`hermes plugins enable kajiba`, `HERMES_PLUGINS_DEBUG=1`), and reconcile every `v0.6.0` reference to `v0.15.x` across the guide and the plugin source (`plugin.yaml` header comment + plugin docstrings) (D-11, D-12, D-14, D-04 revised). Preserve the file path and git history of `docs/hermes-setup.md` (edit in place; do NOT create a separate superseding file).

Purpose: The original guide (06-04 first execution) is stale — WSL2/CUDA/Ollama/v0.6.0-centric — because the ecosystem moved to v0.15.x and the dev machine moved to native Windows. This rewrite is the developer-facing deliverable for ENV-01/02/03 on the *current* contract and the runbook the human checkpoint in 06-05 executes. Because the live env cannot be unit-tested, the guide's per-stage verification checkpoints are how a developer self-confirms each layer before the live hook-kwargs session.
Output: rewritten `docs/hermes-setup.md`; version-reconciled `src/kajiba/plugin/plugin.yaml`, `src/kajiba/plugin/__init__.py`, `src/kajiba/plugin/hooks.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-environment-plugin-foundation/06-CONTEXT.md
@.planning/phases/06-environment-plugin-foundation/06-REPLAN-RESEARCH.md
@.planning/phases/06-environment-plugin-foundation/06-PATTERNS.md
@docs/hermes-setup.md
@src/kajiba/plugin/plugin.yaml
@src/kajiba/plugin/__init__.py
@src/kajiba/plugin/hooks.py
</context>

<artifacts_this_phase_produces>
- `docs/hermes-setup.md` — REWRITTEN in place (native-Windows-primary v0.15.x; WSL2/GPU/Ollama moved to an optional appendix; `hermes plugins enable` + `HERMES_PLUGINS_DEBUG` steps folded in; `KAJIBA_DEBUG` PII security note retained; D-20 reconciliation flag note added).
- `src/kajiba/plugin/plugin.yaml` — header comment version string bumped `v0.6.0` -> `v0.15.x` (D-12). Manifest fields unchanged (the `[ASSUMED]` -> `[CONFIRMED]` promotion is 06-05's D-18 step, not here).
- `src/kajiba/plugin/__init__.py` — module/`register` docstring version references bumped to `v0.15.x` (D-12).
- `src/kajiba/plugin/hooks.py` — module/handler docstring version references bumped to `v0.15.x` (D-12).
Drift verification: one docs file rewritten + three source files with docstring/comment-only edits (no behavior change, tests stay green).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Version-reconcile plugin source docstrings + manifest comment to v0.15.x (D-12)</name>
  <files>src/kajiba/plugin/plugin.yaml, src/kajiba/plugin/__init__.py, src/kajiba/plugin/hooks.py</files>
  <read_first>
    - src/kajiba/plugin/plugin.yaml (the header comment block referencing the Hermes plugin contract; manifest fields name/version/description/provides_hooks/provides_tools)
    - src/kajiba/plugin/__init__.py (module docstring + register(ctx) docstring; mentions of the Hermes discovery path and the register_hook contract)
    - src/kajiba/plugin/hooks.py (module docstring + handler docstrings)
    - .planning/phases/06-environment-plugin-foundation/06-CONTEXT.md (D-12 version target; D-18 scope boundary — the [ASSUMED]->[CONFIRMED] manifest-field promotion belongs to 06-05, NOT here)
    - .planning/phases/06-environment-plugin-foundation/06-REPLAN-RESEARCH.md (section 3 — documented v0.15.x plugin contract; confirms register(ctx)/register_hook still hold so only version strings change)
  </read_first>
  <action>
    Edit the three plugin source files to replace every `v0.6.0` / `0.6.0` Hermes-version reference in comments and docstrings with `v0.15.x`, leaving all code behavior, the package layout, and the `plugin.yaml` `version: "0.2.0"` (Kajiba schema family version — NOT a Hermes version) untouched. Specifically: in `src/kajiba/plugin/plugin.yaml` update the header comment so it references the Hermes `v0.15.x` plugin contract; keep the manifest `[ASSUMED]` wording as-is (the confidence promotion to `[CONFIRMED v0.15.x]` is 06-05's D-18 step, gated on the live load + source check — do not promote it here). In `src/kajiba/plugin/__init__.py` and `src/kajiba/plugin/hooks.py`, update any docstring text that names a Hermes version or implies v0.6.0 framing so it reads `v0.15.x`; this is the discretionary docstring refresh allowed by CONTEXT.md "Claude's Discretion" (refresh `[ASSUMED]`/v0.6.0 wording in docstrings beyond plugin.yaml). Do NOT alter handler signatures, the `register_hook` event-name strings, or the `KAJIBA_DEBUG` logic. Do not place fenced code in this action — make targeted text edits in the named files.
  </action>
  <verify>
    <automated>python -c "import pathlib,sys; files=['src/kajiba/plugin/plugin.yaml','src/kajiba/plugin/__init__.py','src/kajiba/plugin/hooks.py']; bad=[f for f in files if 'v0.6.0' in pathlib.Path(f).read_text(encoding='utf-8') or '0.6.0' in pathlib.Path(f).read_text(encoding='utf-8').replace('0.2.0','')]; print('STILL HAS v0.6.0:', bad); has015=[f for f in files if 'v0.15.x' in pathlib.Path(f).read_text(encoding='utf-8')]; print('HAS v0.15.x:', has015); sys.exit(0 if not bad and len(has015)==3 else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/kajiba/plugin/plugin.yaml`, `src/kajiba/plugin/__init__.py`, and `src/kajiba/plugin/hooks.py` each contain the string `v0.15.x` in a comment/docstring.
    - None of the three files contains a residual `v0.6.0` string (the Kajiba `version: "0.2.0"` manifest value is unrelated and must remain).
    - `plugin.yaml` manifest field tags remain `[ASSUMED]` (the `[CONFIRMED v0.15.x]` promotion is deferred to 06-05 per D-18).
    - `python -m pytest tests/test_plugin.py -x` still passes (docstring/comment edits do not change behavior).
  </acceptance_criteria>
  <done>The three plugin source files reference `v0.15.x` in their comments/docstrings, carry no residual `v0.6.0`, keep `plugin.yaml` manifest tags as `[ASSUMED]`, and the plugin test suite still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite docs/hermes-setup.md as native-Windows-primary v0.15.x; demote WSL2/GPU/Ollama to optional appendix (D-11, D-13, D-14, D-04 revised, D-20)</name>
  <files>docs/hermes-setup.md</files>
  <read_first>
    - docs/hermes-setup.md (the EXISTING stale guide — rewrite IN PLACE; reuse the prose for WSL2/CUDA/Ollama/MP-8/MP-5/MP-9 verbatim when relocating it into the appendix; preserve the checkpoint-gated structure and the KAJIBA_DEBUG security note)
    - .planning/phases/06-environment-plugin-foundation/06-CONTEXT.md (D-11 native primary / WSL2 appendix; D-13 in-place rewrite + collapsed primary checkpoints; D-14 hermes plugins enable + HERMES_PLUGINS_DEBUG + retain KAJIBA_DEBUG note; D-04 revised relocate MP-8/MP-5/MP-9; D-20 flag ROADMAP/REQUIREMENTS wording but DO NOT edit them; Specific Ideas — enable is easiest step to forget, call it out)
    - .planning/phases/06-environment-plugin-foundation/06-REPLAN-RESEARCH.md (section 2 installed reality: HERMES_HOME=%LOCALAPPDATA%\hermes, v0.15.1, remote OpenAI/Anthropic, no Ollama; section 3 plugin contract + enable + HERMES_PLUGINS_DEBUG; section 4 native Windows install methods + the dashboard-terminal-only WSL2 limitation)
    - src/kajiba/plugin/ (the symlink source-of-truth target: plugin.yaml + __init__.py register(ctx) + hooks.py; the "Kajiba registered hooks: ..." log line the Plugin-loads checkpoint observes)
    - .planning/research/PITFALLS.md (MP-8 CUDA stub overwrite, MP-5 Ollama num_ctx, MP-9 Ollama WSL2 binding — the canonical detail for the appendix; cite by ID)
  </read_first>
  <action>
    Rewrite `docs/hermes-setup.md` in place (same path, edit the existing file — do NOT create a new file). Restructure into a PRIMARY native-Windows path followed by an OPTIONAL WSL2/GPU/Ollama appendix. Keep the no-emoji Markdown house style (## / ### headers, fenced powershell/bash/yaml blocks) per CLAUDE.md, and keep the "Verification checkpoint at the end of each stage" feel.

    Update the header block: target environment becomes "Windows 11 (native) + Hermes Agent v0.15.x + remote backend (OpenAI / Anthropic)"; state that the plugin and hooks need NO GPU and NO Ollama and fire on any backend (D-11). Note this machine runs `HERMES_HOME=%LOCALAPPDATA%\hermes`.

    PRIMARY path — collapse the stages to exactly these checkpoint-gated steps (D-13):
    1. Install Hermes Agent v0.15.x (native Windows) — document the install options from REPLAN-RESEARCH section 4 (the PowerShell install one-liner, or `pip install hermes-agent && hermes`, or Hermes Desktop). Verification checkpoint: `hermes --version` reports v0.15.x; record the active `HERMES_HOME` (this machine: `%LOCALAPPDATA%\hermes`).
    2. Editable-install Kajiba — `pip install -e .` from the repo root so the plugin's `from kajiba.collector import KajibaCollector` resolves when Hermes loads it. Verification checkpoint: `python -c "from kajiba.plugin import register"` succeeds.
    3. Symlink the plugin into the Hermes discovery `plugins/` dir — give the native-Windows symlink command (`mklink /D` in an elevated cmd, or `New-Item -ItemType SymbolicLink` in PowerShell) pointing the discovery `plugins\kajiba` dir at `src\kajiba\plugin`. State that the EXACT discovery directory (`<HERMES_HOME>\plugins\` vs `%USERPROFILE%\.hermes\plugins\`) is confirmed empirically in 06-05 (D-17) and that the reader should use the directory 06-05 resolves; document a copy fallback if symlink discovery is not followed. Note `plugin.yaml` lives inside `src\kajiba\plugin\` so the link exposes the manifest.
    4. Enable the plugin — `hermes plugins enable kajiba`. Call this out as REQUIRED and the easiest step to forget: a discovered-but-disabled plugin will NOT load (D-14, D-19, Specific Ideas).
    5. Plugin loads -> hooks fire — start a Hermes session with `KAJIBA_DEBUG=1` (and optionally `HERMES_PLUGINS_DEBUG=1` for Hermes's own verbose discovery logging on stderr); observe the `Kajiba registered hooks: on_session_start, post_llm_call, post_tool_call, on_session_end` line at startup and `KAJIBA_DEBUG ...` lines as hooks fire. Verification checkpoint: registration line present + at least one hook debug line on a short interaction.
    6. Edit-reload cycle — edit under `src\kajiba\plugin\`, restart Hermes to reload (no hot-reload); re-run `pip install -e .` only on package-structure/dependency changes.

    Retain (do not delete) the KAJIBA_DEBUG "PII in logs" Security note in the primary path: debug logs may contain unscrubbed (truncated) session content, must not be shared, and debug stays off for normal use (T-06-09). Add a one-line mention that `HERMES_PLUGINS_DEBUG=1` is Hermes's own discovery logging and complements `KAJIBA_DEBUG=1` (D-14).

    OPTIONAL APPENDIX — "Appendix: WSL2 + NVIDIA GPU + Ollama (local inference / dashboard terminal only)". RELOCATE the existing WSL2 install, NVIDIA GPU passthrough, Ollama install + config (num_ctx, hermes3:8b pull, custom endpoint), and the three troubleshooting subsections — CUDA driver stub overwrite (MP-8), Ollama num_ctx truncation (MP-5), Ollama WSL2 network binding (MP-9) — into this appendix essentially verbatim (D-04 revised: keep the content, move it). Open the appendix with a sentence stating it is OPTIONAL and only needed for local GPU inference or the dashboard `/chat` embedded terminal (which needs a POSIX PTY), and that plugin load + hook verification do NOT require any of it.

    D-20 reconciliation flag — add a short "Note on ROADMAP/REQUIREMENTS wording" callout (in the guide or as an HTML comment) stating that ROADMAP Phase 6 Success Criterion #1 and ENV-01/ENV-02 still name WSL2/GPU/Ollama/v0.6.0 as the *required* path and are stale under native-Windows-primary; flag that this needs a later `/gsd-phase` edit. Do NOT write to `.planning/ROADMAP.md` or `.planning/REQUIREMENTS.md` from this plan.

    Update the closing summary checklist table to list the SIX primary native-Windows checkpoints (install Hermes / editable-install / symlink / enable / plugin loads / edit-reload), with the WSL2/GPU/Ollama checkpoints shown under the appendix.

    Do not paste full file contents into this action; make the edits in `docs/hermes-setup.md`.
  </action>
  <verify>
    <automated>python -c "import pathlib,sys; t=pathlib.Path('docs/hermes-setup.md').read_text(encoding='utf-8'); req=['v0.15.x','hermes plugins enable kajiba','HERMES_PLUGINS_DEBUG','KAJIBA_DEBUG','HERMES_HOME','register']; missing=[k for k in req if k not in t]; print('MISSING:',missing); appendix_tokens=['WSL2','nvidia-smi','num_ctx','OLLAMA_HOST']; missing_app=[k for k in appendix_tokens if k not in t]; print('MISSING APPENDIX:',missing_app); idx=t.find('Appendix'); wsl=t.find('WSL2'); print('appendix_before_wsl2:', idx!=-1 and idx<=wsl); print('LINES:',len(t.splitlines())); sys.exit(0 if not missing and not missing_app and len(t.splitlines())>=200 and idx!=-1 and idx<=wsl else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/hermes-setup.md` exists at the same path (rewritten in place), is at least 200 lines, and contains `v0.15.x` (target version) with no claim that v0.6.0 is the install target.
    - Primary path contains `hermes plugins enable kajiba` (REQUIRED enable step), `HERMES_PLUGINS_DEBUG`, the retained `KAJIBA_DEBUG` security note, `HERMES_HOME`, and a reference to the plugin `register`/hooks load checkpoint.
    - The six primary native-Windows checkpoints are present: install Hermes v0.15.x, editable-install Kajiba, symlink plugin, enable plugin, plugin loads (registration line + hooks fire), edit-reload.
    - An "Appendix" section exists and appears BEFORE the WSL2 content (the WSL2/GPU/Ollama material — `WSL2`, `nvidia-smi`, `num_ctx`, `OLLAMA_HOST`, and the MP-8/MP-5/MP-9 troubleshooting — is relocated under it, not in the primary path).
    - A D-20 note flags that ROADMAP SC-#1 + ENV-01/ENV-02 wording is stale and needs a later `/gsd-phase` edit; the plan does NOT modify ROADMAP.md or REQUIREMENTS.md.
    - No emojis in the doc (CLAUDE.md); checkpoint gates use the word "Verification checkpoint".
  </acceptance_criteria>
  <done>`docs/hermes-setup.md` rewritten in place: native-Windows v0.15.x primary path with six checkpoints (install/editable-install/symlink/enable/load/edit-reload), `hermes plugins enable kajiba` and `HERMES_PLUGINS_DEBUG` folded in, the KAJIBA_DEBUG PII note retained, WSL2/GPU/Ollama + MP-8/MP-5/MP-9 relocated to an optional appendix, and a D-20 reconciliation flag added without touching ROADMAP/REQUIREMENTS; the keyword/length/appendix-ordering check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> developer actions | The guide instructs system-level installs and a plugin symlink/enable; no code executes from the doc itself |
| KAJIBA_DEBUG / HERMES_PLUGINS_DEBUG logs -> developer eyes/files | Debug logging may surface truncated, unscrubbed session content (PII) on stderr/log during verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Information Disclosure | guide could omit the KAJIBA_DEBUG / HERMES_PLUGINS_DEBUG PII-in-logs warning | mitigate | Retain the explicit Security note in the primary path: debug logs may contain unscrubbed (truncated) session content, must not be shared, and debug stays off for normal use; note HERMES_PLUGINS_DEBUG is discovery logging (less PII-bearing) but still keep logs local |
| T-06-13 | Tampering / Elevation | native-Windows symlink (mklink /D) + `hermes plugins enable kajiba` pointing Hermes at developer-owned plugin code | accept | Local-first single-operator dev workflow: the symlink/enable targets the developer's own trusted repo at `src/kajiba/plugin/`; documented as a dev-only workflow, no third-party code path |
| T-06-10 | Tampering | CUDA meta-package overwriting the WSL2 driver stub (MP-8) — appendix only | mitigate | The relocated appendix CUDA troubleshooting still instructs installing only `cuda-toolkit-12-x` and verifying libcuda.so stays a symlink; not on the primary native path |
| T-06-SC | Tampering | system-component installs (Hermes native, optional WSL2/Ollama/CUDA) | accept | Installed via official vendor channels documented in the guide; not registry packages — slopcheck N/A (06-RESEARCH Package Legitimacy Audit). The plugin source-docstring edits add no dependencies |
</threat_model>

<verification>
- Task 1 automated check: all three plugin source files contain `v0.15.x` and none retains `v0.6.0`; `pytest tests/test_plugin.py -x` stays green.
- Task 2 automated check: required primary-path tokens present (`v0.15.x`, `hermes plugins enable kajiba`, `HERMES_PLUGINS_DEBUG`, `KAJIBA_DEBUG`, `HERMES_HOME`, `register`), appendix tokens present and ordered after an "Appendix" heading, >= 200 lines.
- A human reading the guide can install Hermes v0.15.x natively, editable-install Kajiba, symlink + enable the plugin, and self-verify at each checkpoint (validated live in 06-05).
</verification>

<success_criteria>
- `docs/hermes-setup.md` makes native Windows + Hermes v0.15.x the documented primary path with per-stage verification checkpoints, plugin load needing no GPU/Ollama (ENV-01, D-11/D-13).
- `hermes plugins enable kajiba` (REQUIRED) and `HERMES_PLUGINS_DEBUG=1` folded into the primary path; KAJIBA_DEBUG PII note retained (D-14, ENV-01).
- Ollama configuration (num_ctx override, hermes3:8b pull, custom endpoint) preserved in the optional appendix (ENV-02, D-04 revised).
- Symlink dev workflow documented for native Windows targeting the resolved Hermes plugins/ discovery dir, with copy fallback (ENV-03, D-13).
- All v0.6.0 references reconciled to v0.15.x across the guide and plugin source comments/docstrings (D-12).
- D-20 stale-wording reconciliation flagged for a later /gsd-phase edit; ROADMAP/REQUIREMENTS not modified here.
</success_criteria>

<output>
Create `.planning/phases/06-environment-plugin-foundation/06-04-SUMMARY.md` when done
</output>
