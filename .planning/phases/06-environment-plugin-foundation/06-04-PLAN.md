---
phase: 06-environment-plugin-foundation
plan: 04
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - docs/hermes-setup.md
requirements: [ENV-01, ENV-02, ENV-03]
autonomous: true
must_haves:
  truths:
    - "A developer can follow docs/hermes-setup.md to install and verify WSL2 + NVIDIA GPU passthrough + Hermes Agent v0.6.0 + Ollama"
    - "The guide has explicit per-stage verification checkpoints (WSL2, GPU, Ollama, Hermes, Plugin loads)"
    - "The guide documents Ollama configuration (num_ctx override, Hermes 3 8B pull, custom endpoint)"
    - "The guide gives a 2-3 command symlink dev workflow linking src/kajiba/plugin into ~/.hermes/plugins/kajiba"
    - "The guide includes troubleshooting for CUDA driver stub overwrite, Ollama num_ctx truncation, and Ollama WSL2 network binding"
  artifacts:
    - path: "docs/hermes-setup.md"
      provides: "End-to-end WSL2/GPU/Ollama/Hermes/plugin setup guide with checkpoints and troubleshooting"
      contains: "HERMES_HOME"
      min_lines: 120
  key_links:
    - from: "docs/hermes-setup.md"
      to: "src/kajiba/plugin/"
      via: "symlink command (ln -s) into ~/.hermes/plugins/kajiba"
      pattern: "ln -s"
---

<objective>
Write `docs/hermes-setup.md` (D-03): the standalone, checkpoint-driven setup guide that takes a developer from a bare Windows machine to a working WSL2 + NVIDIA GPU + Ollama + Hermes Agent v0.6.0 environment with the Kajiba plugin loaded (ENV-01, ENV-02), and documents the symlink dev workflow for rapid edit-reload (ENV-03). The guide bakes in the known pitfalls as troubleshooting sections (D-04): CUDA driver stub overwrite, Ollama num_ctx truncation, Ollama WSL2 network binding.

Purpose: All the env prerequisites for live hook verification (Plan 05) are installed/verified by following this guide. The guide is the developer-facing deliverable for ENV-01/02/03 and the script the human checkpoint in Plan 05 executes. Because the live env cannot be unit-tested, the guide's per-stage verification checkpoints are how a developer self-confirms each layer works before moving on.
Output: docs/hermes-setup.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-environment-plugin-foundation/06-RESEARCH.md
@.planning/phases/06-environment-plugin-foundation/06-PATTERNS.md
@.planning/phases/06-environment-plugin-foundation/06-CONTEXT.md
@.planning/research/PITFALLS.md
</context>

<artifacts_this_phase_produces>
This plan creates `docs/hermes-setup.md`. No code symbols. Drift verification: a new docs file only.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/hermes-setup.md with checkpoint sections and troubleshooting</name>
  <read_first>
    - 06-RESEARCH.md § Symlink Dev Workflow (the 2-3 command workflow lines 250-261), § Common Pitfalls MP-8/MP-9/MP-5 (CUDA stub, Ollama binding, num_ctx — lines 325-332), § Environment Availability (the dependency table lines 427-436), § Standard Stack Installation note (lines 84) — system components install via vendor channels not pip
    - .planning/research/PITFALLS.md (MP-8 CUDA stub overwrite, MP-9 Ollama network binding, MP-5 num_ctx — for the canonical troubleshooting detail; cite by ID)
    - 06-PATTERNS.md § docs/hermes-setup.md (style: ## / ### headers with checkpoint sections + fenced bash/yaml blocks, lines 271-273)
    - 06-CONTEXT.md § Specific Ideas (explicit "checkpoint" sections WSL2 ✓ / GPU ✓ / Ollama ✓ / Hermes ✓ / Plugin loads ✓; symlink in 2-3 commands — lines 88-91)
    - src/kajiba/plugin/ (Plan 03 output — the symlink source-of-truth target the guide links into ~/.hermes/plugins/kajiba; plugin.yaml lives here)
  </read_first>
  <action>
    Create docs/hermes-setup.md following the existing docs/*.md style (Markdown ## / ### headers, fenced bash/yaml blocks, no emojis per CLAUDE.md). Structure it as ordered, checkpoint-gated stages, each ending with a "Verification checkpoint" the developer runs before proceeding:

    1. Prerequisites — Windows 11, RTX 4070 (8GB), Python 3.11+, repo cloned with `pip install -e .` active (note: the editable install is required so the plugin's `from kajiba.collector import ...` resolves when Hermes loads it).
    2. WSL2 install — enable WSL2, install a distro. Verification checkpoint: `wsl --status` / distro shell opens.
    3. NVIDIA GPU passthrough — install the NVIDIA Windows driver (NOT a Linux driver inside WSL2). Verification checkpoint: `nvidia-smi` inside WSL2 shows the RTX 4070 and VRAM. Troubleshooting subsection "CUDA driver stub overwrite" (D-04, MP-8): install ONLY `cuda-toolkit-12-x`, never the `cuda` / `cuda-drivers` meta-package; verify `libcuda.so` remains a symlink to the WSL2 stub; keep models in the WSL2 native FS (not /mnt/c/). Cite MP-8.
    4. Ollama install + config (ENV-02) — install Ollama inside WSL2; pull the Hermes 3 8B model; document the custom endpoint setup Hermes uses. Verification checkpoint: `ollama run` produces output AND `nvidia-smi` shows VRAM in use during inference (confirms GPU, not CPU fallback). Troubleshooting "num_ctx truncation" (D-04, MP-5): Ollama defaults effective context to 2048 regardless of the model's reported length; show how to set `num_ctx` explicitly (Modelfile / API param). Troubleshooting "Ollama WSL2 network binding" (D-04, MP-9): Ollama binds 127.0.0.1 by default; run Hermes + Ollama both inside WSL2, or set `OLLAMA_HOST=0.0.0.0:11434` for Windows-host access; describe the topology.
    5. Hermes Agent v0.6.0 install — install via the Hermes install script (not pip). Note HERMES_HOME profile isolation (v0.6.0) and that Kajiba resolves all paths through it (forward-link to the plugin section). Verification checkpoint: Hermes starts; `echo $HERMES_HOME` (or default `~/.hermes`).
    6. Kajiba plugin — the symlink dev workflow (ENV-03, D-02): from the repo root in WSL2, `mkdir -p ~/.hermes/plugins` then `ln -s "$(pwd)/src/kajiba/plugin" ~/.hermes/plugins/kajiba` (2-3 commands total). Note plugin.yaml lives inside src/kajiba/plugin so the symlink exposes it. Verification checkpoint "Plugin loads ✓": start a Hermes session with `KAJIBA_DEBUG=1` and observe `Kajiba registered hooks: ...` in the log and `KAJIBA_DEBUG ...` lines when hooks fire. Note that if Hermes does not follow symlinks during discovery (RESEARCH Open Question Q3 / A3), fall back to copying the dir; the developer confirms the working method during Plan 05.
    7. Edit-reload cycle — edit files under src/kajiba/plugin/, restart Hermes to reload.

    Add a short "Security note" that `KAJIBA_DEBUG=1` logs (truncated) hook kwargs which may contain unscrubbed session content; do not share debug logs and turn debug off for normal use. Keep all commands copy-pasteable. Honor CLAUDE.md doc conventions (no emojis; the ✓ in checkpoint labels is acceptable as it is part of the user's requested checkpoint-section convention from CONTEXT.md, but prefer the word "Verification checkpoint").
  </action>
  <verify>
    <automated>python -c "import pathlib,sys; t=pathlib.Path('docs/hermes-setup.md').read_text(encoding='utf-8'); req=['WSL2','nvidia-smi','cuda-toolkit-12','num_ctx','OLLAMA_HOST','HERMES_HOME','ln -s','register'] ; missing=[k for k in req if k not in t]; print('MISSING:',missing); sys.exit(1 if missing or len(t.splitlines())<120 else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - docs/hermes-setup.md exists and is at least 120 lines.
    - It contains all of: `WSL2`, `nvidia-smi`, `cuda-toolkit-12` (CUDA stub troubleshooting), `num_ctx` (Ollama truncation), `OLLAMA_HOST` (WSL2 binding), `HERMES_HOME`, `ln -s` (symlink workflow), and a reference to the plugin `register`/hooks load checkpoint.
    - Each of the five stages (WSL2, GPU, Ollama, Hermes, Plugin) has an explicit verification checkpoint.
    - The symlink workflow is 2-3 commands and targets `~/.hermes/plugins/kajiba` → `src/kajiba/plugin`.
    - Troubleshooting sections cover MP-8 (CUDA stub), MP-5 (num_ctx), MP-9 (Ollama binding).
  </acceptance_criteria>
  <done>docs/hermes-setup.md written with five checkpoint-gated stages, Ollama config, the symlink dev workflow, and the three troubleshooting sections; the keyword/length automated check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → developer actions | The guide instructs system-level installs; no code executes from the doc itself |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Information Disclosure | guide could omit the KAJIBA_DEBUG PII-in-logs warning | mitigate | Guide includes an explicit security note that debug logs may contain unscrubbed (truncated) session content and should not be shared; debug off by default |
| T-06-10 | Tampering | CUDA meta-package overwriting the WSL2 driver stub (MP-8) | mitigate | Guide's CUDA troubleshooting instructs installing only `cuda-toolkit-12-x` and verifying libcuda.so stays a symlink (this is an env-integrity, not a code, threat) |
| T-06-SC | Tampering | system-component installs (WSL2, Ollama, Hermes, CUDA toolkit) | accept | Installed via official vendor channels documented in the guide; not registry packages — slopcheck N/A (06-RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- The automated keyword/length check passes (all required tokens present, >= 120 lines).
- A human reading the guide can install each layer and self-verify at each checkpoint (validated live in Plan 05).
</verification>

<success_criteria>
- docs/hermes-setup.md covers WSL2 + GPU passthrough + Hermes v0.6.0 + Ollama with per-stage verification checkpoints (ENV-01).
- Ollama configuration documented: num_ctx override, Hermes 3 8B pull, custom endpoint (ENV-02).
- Symlink dev workflow documented in 2-3 commands linking src/kajiba/plugin into ~/.hermes/plugins/kajiba (ENV-03).
- Troubleshooting for CUDA stub overwrite, num_ctx truncation, Ollama WSL2 binding present (D-04).
</success_criteria>

<output>
Create `.planning/phases/06-environment-plugin-foundation/06-04-SUMMARY.md` when done
</output>
