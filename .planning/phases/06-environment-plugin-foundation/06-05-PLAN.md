---
phase: 06-environment-plugin-foundation
plan: 05
type: execute
wave: 5
depends_on: ["06-03", "06-04"]
files_modified:
  - .planning/phases/06-environment-plugin-foundation/06-HOOK-KWARGS.md
  - src/kajiba/plugin/plugin.yaml
requirements: [ENV-01, ENV-02, ENV-03, PLUG-01, PLUG-02, CAPT-01]
autonomous: false
must_haves:
  truths:
    - "On native Windows, Hermes Agent v0.15.x discovers, loads, and (after hermes plugins enable kajiba) runs the Kajiba plugin's register(ctx) on startup, emitting the Kajiba registered hooks: ... line (D-15, D-19, PLUG-01/PLUG-02)"
    - "A ~15-min live Hermes v0.15.1 session on a REMOTE backend (OpenAI/Anthropic — no GPU, no Ollama) with KAJIBA_DEBUG=1 fires on_session_start, post_llm_call, post_tool_call, on_session_end, each logging its kwargs (D-15, CAPT-01)"
    - "06-HOOK-KWARGS.md records all four hooks' kwargs, each tagged [DOCUMENTED v0.15.x] (from 06-REPLAN-RESEARCH section 3) and [VERIFIED] when confirmed in the live session, permanently closing the old no-published-payload-schema blocker (D-16, D-21)"
    - "The plugin discovery directory is resolved empirically by inspecting the installed Hermes source at %LOCALAPPDATA%\\hermes\\hermes-agent, settling whether discovery scans <HERMES_HOME>/plugins/ or ~/.hermes/plugins/ under an overridden HERMES_HOME — this confirms the D-02 symlink target (D-17)"
    - "plugin.yaml manifest field tags are promoted [ASSUMED] -> [CONFIRMED v0.15.x] once the live load + source check confirm the schema (confidence promotion, not a field change) (D-18)"
    - "hermes plugins enable kajiba is exercised as a REQUIRED load step in the verification checklist (without it the plugin is discovered but inert) (D-19)"
  artifacts:
    - path: ".planning/phases/06-environment-plugin-foundation/06-HOOK-KWARGS.md"
      provides: "Single living record of the real v0.15.x hook contract, dual-tagged [DOCUMENTED v0.15.x] + [VERIFIED], for all four hooks; the payload schema Phase 7 turn capture builds on"
      contains: "on_session_start"
    - path: "src/kajiba/plugin/plugin.yaml"
      provides: "Manifest with field tags promoted to [CONFIRMED v0.15.x] after live load + source check"
      contains: "CONFIRMED v0.15.x"
  key_links:
    - from: "live Hermes v0.15.1 session (remote backend)"
      to: "src/kajiba/plugin (via the resolved plugins/ discovery dir + hermes plugins enable kajiba)"
      via: "Hermes plugin discovery + enable + register(ctx) on startup"
      pattern: "register"
    - from: "KAJIBA_DEBUG=1 / HERMES_PLUGINS_DEBUG=1 logs"
      to: "06-HOOK-KWARGS.md"
      via: "developer records observed kwarg names/types/values and dual-tags them"
      pattern: "KAJIBA_DEBUG"
---

<objective>
Run the native-Windows live verification that cannot be unit-tested (D-15): on Hermes Agent v0.15.1, native Windows, with a REMOTE backend (OpenAI sub / Anthropic key — no GPU, no Ollama), symlink + `hermes plugins enable kajiba` the plugin, run a ~15-min session with `KAJIBA_DEBUG=1`, and confirm all four lifecycle hooks fire and log their kwargs. Record all four hooks' kwargs into `06-HOOK-KWARGS.md`, each dual-tagged `[DOCUMENTED v0.15.x]` (from `06-REPLAN-RESEARCH.md` section 3) + `[VERIFIED]` once confirmed live (D-16, D-21). Resolve the plugin discovery directory empirically from the installed Hermes source (D-17), and promote `plugin.yaml` field tags `[ASSUMED]` -> `[CONFIRMED v0.15.x]` (D-18).

Purpose: This is a CONFIRM-of-documented-kwargs run, not a discover-undocumented-kwargs build (the old WSL2 premise is obsolete). It closes the live half of the phase goal — Hermes loads and runs the plugin and the four hooks fire — and produces `06-HOOK-KWARGS.md`, the single living record of the v0.15.x hook contract that Phase 7 turn assembly depends on. The whole point of the `KAJIBA_DEBUG` mode (built in 06-03) is realized here.
Output: `06-HOOK-KWARGS.md` with dual-tagged per-hook kwargs + the D-17 discovery-dir finding; `plugin.yaml` field tags promoted to `[CONFIRMED v0.15.x]`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-environment-plugin-foundation/06-CONTEXT.md
@.planning/phases/06-environment-plugin-foundation/06-REPLAN-RESEARCH.md
@.planning/phases/06-environment-plugin-foundation/06-VALIDATION.md
@docs/hermes-setup.md
@src/kajiba/plugin/__init__.py
@src/kajiba/plugin/hooks.py
@src/kajiba/plugin/plugin.yaml
</context>

<artifacts_this_phase_produces>
- `.planning/phases/06-environment-plugin-foundation/06-HOOK-KWARGS.md` — NEW. The living v0.15.x hook contract: for each of the four hooks (on_session_start, post_llm_call, post_tool_call, on_session_end) a kwarg name/type/example/notes table, each dual-tagged `[DOCUMENTED v0.15.x]` + `[VERIFIED]`; plus the D-17 discovery-dir finding (which dir Hermes scans under overridden HERMES_HOME) and a Phase 7 implications section.
- `src/kajiba/plugin/plugin.yaml` — manifest field tags promoted from `[ASSUMED]` to `[CONFIRMED v0.15.x]` (D-18; confidence promotion only — no field-name change).
Drift verification: one new planning artifact + a comment-only tag promotion in plugin.yaml (no behavior change).
</artifacts_this_phase_produces>

<live_env_note>
This is a live, human-in-the-loop verification on the developer's native Windows machine (Hermes v0.15.1, remote backend, NO WSL2 / NO GPU / NO Ollama). These behaviors cannot be unit-tested on the host, so all live tasks are `checkpoint:human-verify` (`autonomous: false`). Claude automates everything it can up front — the plugin code, the symlink/enable commands, the [DOCUMENTED v0.15.x] signature table — already exist from 06-03/06-04 and 06-REPLAN-RESEARCH section 3. The developer performs the install/enable/run and reports observations (and the installed-source directory finding); Claude then writes `06-HOOK-KWARGS.md` and promotes the plugin.yaml tags. Each `<verify>` uses `<human-check>`; there is no automated command for the live behaviors.
</live_env_note>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Resolve the discovery dir from installed Hermes source, then symlink + enable + load the plugin (D-17, D-19, ENV-03, PLUG-01/PLUG-02)</name>
  <files>(verification only — findings recorded into 06-HOOK-KWARGS.md in Task 2)</files>
  <read_first>
    - docs/hermes-setup.md (the native-Windows primary path: install -> editable-install -> symlink -> enable -> load checkpoints from 06-04)
    - .planning/phases/06-environment-plugin-foundation/06-CONTEXT.md (D-17 empirical dir resolution; D-19 hermes plugins enable kajiba is REQUIRED; D-02 symlink target set by D-17; Integration Points — HERMES_HOME=%LOCALAPPDATA%\hermes, installed source at %LOCALAPPDATA%\hermes\hermes-agent)
    - .planning/phases/06-environment-plugin-foundation/06-REPLAN-RESEARCH.md (section 2 installed reality + the open dir question; section 3 discovery is directory-based ~/.hermes/plugins/<name>/ with plugin.yaml + __init__.py exposing register(ctx))
    - src/kajiba/plugin/__init__.py (register(ctx) logs "Kajiba registered hooks: on_session_start, post_llm_call, post_tool_call, on_session_end")
  </read_first>
  <action>
    Claude: there is nothing to automate for the live install/enable — present the checklist below and pause for the developer. The developer performs the steps on their native-Windows machine. Two findings must come back for Task 2 to record: (a) the empirically-resolved discovery directory (does Hermes scan `<HERMES_HOME>\plugins\` i.e. `%LOCALAPPDATA%\hermes\plugins\`, or `%USERPROFILE%\.hermes\plugins\`, when `HERMES_HOME` is overridden), found by inspecting the installed source at `%LOCALAPPDATA%\hermes\hermes-agent` (grep/read the plugin-discovery code path) AND/OR by which symlink location actually loads; (b) confirmation that `hermes plugins enable kajiba` was required before the plugin loaded. Do not proceed past this blocking gate until "plugin loaded" with the dir finding reported.
  </action>
  <what-built>
    06-03 built `src/kajiba/plugin/` (register(ctx) + plugin.yaml + four KAJIBA_DEBUG hooks). 06-04 documented the native-Windows symlink + enable workflow and reconciled versions to v0.15.x. This task deploys against the live v0.15.1 install, resolves the real discovery directory, and confirms Hermes discovers, enables, and loads the plugin.
  </what-built>
  <how-to-verify>
    1. Resolve the discovery directory (D-17): open the installed Hermes source at `%LOCALAPPDATA%\hermes\hermes-agent` and inspect the plugin-discovery code path to determine whether it scans `<HERMES_HOME>\plugins\` or always `%USERPROFILE%\.hermes\plugins\` when `HERMES_HOME` is overridden (this machine: `HERMES_HOME=%LOCALAPPDATA%\hermes`). Record the answer.
    2. With `pip install -e .` active, create the plugin link into the resolved discovery dir using a native-Windows symlink: in an elevated cmd `mklink /D "<resolved_plugins_dir>\kajiba" "<repo>\src\kajiba\plugin"`, or in PowerShell `New-Item -ItemType SymbolicLink -Path "<resolved_plugins_dir>\kajiba" -Target "<repo>\src\kajiba\plugin"`. If discovery does not follow the symlink, copy `src\kajiba\plugin` into `<resolved_plugins_dir>\kajiba` as the fallback and note which method worked.
    3. Enable the plugin (REQUIRED, D-19): run `hermes plugins enable kajiba`. Confirm that WITHOUT this step the plugin is discovered but inert, and that AFTER it the plugin loads.
    4. Start Hermes (optionally with `HERMES_PLUGINS_DEBUG=1` for Hermes's own discovery logging). Confirm the `Kajiba registered hooks: on_session_start, post_llm_call, post_tool_call, on_session_end` line appears at startup (proves register(ctx) ran). Report the resolved discovery dir, symlink-vs-copy, and that enable was required.
  </how-to-verify>
  <verify>
    <human-check>Developer confirms: the discovery directory was resolved from the installed Hermes source (states which dir Hermes scans under overridden HERMES_HOME); `hermes plugins enable kajiba` was run and was required for load; the "Kajiba registered hooks: ..." line appears at startup after enabling. Live-only on native Windows — no automated command on the host.</human-check>
  </verify>
  <acceptance_criteria>
    - Developer reports the empirically-resolved discovery directory (e.g. `%LOCALAPPDATA%\hermes\plugins\` vs `%USERPROFILE%\.hermes\plugins\`) determined from the installed source at `%LOCALAPPDATA%\hermes\hermes-agent` and/or which symlink location loaded. (D-17, sets the D-02 target.)
    - Developer confirms `hermes plugins enable kajiba` was run and that the plugin did not load until it was enabled. (D-19, success criterion 2.)
    - Developer confirms the `Kajiba registered hooks: ...` line appears in the Hermes log/stderr at startup after enabling (register(ctx) executed; PLUG-01/PLUG-02 live half).
    - Developer reports whether a symlink worked or a copy was needed (resolves the native-Windows dev-workflow question for ENV-03).
  </acceptance_criteria>
  <resume-signal>Type "plugin loaded" and report: (a) the resolved discovery dir, (b) symlink-vs-copy, (c) that enable was required — or describe the load failure.</resume-signal>
  <done>Developer resolved the discovery dir from the installed source, ran hermes plugins enable kajiba (confirmed required), and saw the registration line; symlink-vs-copy and the dir finding are recorded for Task 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run a ~15-min live KAJIBA_DEBUG=1 session (remote backend), capture the four hooks' kwargs, write 06-HOOK-KWARGS.md, promote plugin.yaml tags (D-15, D-16, D-18, D-21, CAPT-01)</name>
  <files>.planning/phases/06-environment-plugin-foundation/06-HOOK-KWARGS.md, src/kajiba/plugin/plugin.yaml</files>
  <read_first>
    - src/kajiba/plugin/hooks.py (the _log_kwargs format: "KAJIBA_DEBUG <hook> kwarg <name>: type=<type> value=<truncated to 120>"; the four handler names)
    - .planning/phases/06-environment-plugin-foundation/06-REPLAN-RESEARCH.md (section 3 — the DOCUMENTED v0.15.x hook signature table: on_session_start(session_id, model, platform); post_llm_call(session_id, user_message, assistant_response, conversation_history, model, platform); post_tool_call(tool_name, args, result:str, task_id, duration_ms); on_session_end(session_id, completed, interrupted, model, platform). post_tool_call.result is typed str — resolves the old "is result a JSON string?" question.)
    - .planning/phases/06-environment-plugin-foundation/06-CONTEXT.md (D-15 ~15-min remote-backend live run; D-16 dual-tagging [DOCUMENTED v0.15.x] + [VERIFIED]; D-18 promote plugin.yaml [ASSUMED] -> [CONFIRMED v0.15.x]; D-21 CAPT-01 reinterpretation; Specific Ideas — 06-HOOK-KWARGS.md is the single living record)
    - src/kajiba/plugin/plugin.yaml (the [ASSUMED] header comment to promote to [CONFIRMED v0.15.x] after live load + source check)
  </read_first>
  <action>
    Two parts. HUMAN part (live, blocking): the developer runs a short (~15 min) `KAJIBA_DEBUG=1` Hermes v0.15.1 session on a REMOTE backend (OpenAI sub / Anthropic key — no GPU, no Ollama) that exercises all four hooks — start the session (fires on_session_start), send at least one message and get a response (fires post_llm_call), trigger at least one tool call e.g. a file read or shell command (fires post_tool_call), and end the session (fires on_session_end) — then provides the captured `KAJIBA_DEBUG <hook> kwarg ...` lines (paste or a file path), noting any kwargs caught by `**kwargs` beyond the documented names.

    CLAUDE part (after the developer provides the lines): write `.planning/phases/06-environment-plugin-foundation/06-HOOK-KWARGS.md` — for each of the four hooks a table of kwarg name | type | example value | notes, seeded from the `[DOCUMENTED v0.15.x]` signature table in 06-REPLAN-RESEARCH.md section 3 and updated to `[VERIFIED]` for each kwarg confirmed in the live lines; explicitly record the post_tool_call `result` type observed (documented as `str`) and the discovery-dir finding from Task 1 (D-17). Use synthetic/placeholder example values wherever a real kwarg value would contain PII — record TYPES and SHAPES, not real conversation content (the live `repr[:120]` truncation already limits exposure; do not transcribe real prompts/responses into the file). End with a "Phase 7 implications" section mapping the confirmed post_llm_call / post_tool_call kwargs to the collector turn/tool assembly inputs (CAPT-02/CAPT-03). Then promote the `src/kajiba/plugin/plugin.yaml` header-comment tag from `[ASSUMED]` to `[CONFIRMED v0.15.x]` now that the live load (Task 1) + the section-3 documented schema confirm the manifest fields (D-18; comment-only change, no field rename). Do not paste fenced implementations into this action.
  </action>
  <what-built>
    06-03's `KAJIBA_DEBUG=1` mode logs every hook kwarg's name, type, and truncated value. This task runs a real short session on the live v0.15.1 native-Windows install to CONFIRM the documented v0.15.x kwargs for all four hooks and records them dual-tagged — the artifact Phase 7 turn assembly depends on. It also promotes the plugin.yaml manifest from assumed to confirmed.
  </what-built>
  <how-to-verify>
    1. Set `KAJIBA_DEBUG=1` in the Hermes process environment and start a SHORT (~15 min) Hermes v0.15.1 session on a remote backend that fires all four hooks: on_session_start (start), post_llm_call (a message + response), post_tool_call (at least one tool call), on_session_end (end).
    2. Collect the `KAJIBA_DEBUG <hook> kwarg ...` lines for all four hooks. For each hook note every kwarg NAME, its TYPE, and a representative (truncated/synthetic) VALUE — including any kwargs caught by `**kwargs` not named in the documented signatures.
    3. Compare against the [DOCUMENTED v0.15.x] table (06-REPLAN-RESEARCH.md section 3). Confirm in particular that post_tool_call `result` arrives as a `str` (not a nested object), and whether on_session_end carries `completed` / `interrupted`.
    4. Provide the captured lines (or a file path) to Claude so it can write 06-HOOK-KWARGS.md and promote the plugin.yaml tags.
  </how-to-verify>
  <verify>
    <human-check>All four hooks observed firing with their KAJIBA_DEBUG lines captured. After Claude writes the file, the developer confirms 06-HOOK-KWARGS.md contains a per-hook kwarg table for all four hooks, each kwarg dual-tagged [DOCUMENTED v0.15.x] + [VERIFIED], the post_tool_call result-type finding, and the D-17 discovery-dir finding; and that src/kajiba/plugin/plugin.yaml now reads [CONFIRMED v0.15.x]. Live-only on native Windows.</human-check>
  </verify>
  <acceptance_criteria>
    - All four hooks observed firing in the live ~15-min remote-backend session with their KAJIBA_DEBUG lines captured (success criterion 3, CAPT-01/D-21).
    - `06-HOOK-KWARGS.md` exists and contains a per-hook kwarg table for all four hooks (on_session_start, post_llm_call, post_tool_call, on_session_end) with name/type/example, the post_tool_call `result`-as-str finding, and the D-17 discovery-dir finding.
    - Each hook's kwargs are dual-tagged `[DOCUMENTED v0.15.x]` and `[VERIFIED]` (no longer `[ASSUMED]`); placeholder/synthetic values used wherever real values would carry PII.
    - `src/kajiba/plugin/plugin.yaml` header comment is promoted from `[ASSUMED]` to `[CONFIRMED v0.15.x]` (D-18); manifest field names unchanged.
    - `python -m pytest tests/test_plugin.py -x` still passes after the plugin.yaml comment edit.
  </acceptance_criteria>
  <resume-signal>Paste the KAJIBA_DEBUG lines for the four hooks (or confirm Claude may read them from a file you provide), then type "kwargs captured".</resume-signal>
  <done>All four hooks confirmed firing on the live v0.15.1 remote-backend session; 06-HOOK-KWARGS.md written with dual-tagged [DOCUMENTED v0.15.x]+[VERIFIED] per-hook tables, the result-as-str + D-17 dir findings, and a Phase 7 implications section; plugin.yaml tags promoted to [CONFIRMED v0.15.x]; plugin tests still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live Hermes session content -> KAJIBA_DEBUG / HERMES_PLUGINS_DEBUG logs | Real prompts/responses (PII) flow into debug logs during capture |
| Hermes host -> symlinked/enabled plugin code execution | Hermes executes the developer's plugin code from the symlinked discovery dir after enable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Information Disclosure | KAJIBA_DEBUG live logs + 06-HOOK-KWARGS.md capturing real session content/PII | mitigate | _log_kwargs truncates values to repr[:120] (06-03); when writing 06-HOOK-KWARGS.md, use synthetic/placeholder example values where a real value would carry PII — record TYPES and SHAPES, not real conversation content; run the session with throwaway, non-sensitive prompts; keep debug logs local (do not share) per the 06-04 guide security note |
| T-06-13 | Tampering / Elevation | Hermes loading + enabling code from the symlinked plugin dir on native Windows | accept | Local-first single-operator: the symlink/enable targets the developer's own trusted repo at src/kajiba/plugin/; documented dev-only workflow |
| T-06-SC | Tampering | system-component install verified live (Hermes v0.15.1 native) | accept | Installed via official vendor channel per the 06-04 guide; not a registry package — slopcheck N/A. The plugin.yaml tag promotion is a comment-only change adding no dependencies |
</threat_model>

<verification>
- All four hooks confirmed firing in a live Hermes v0.15.1 native-Windows session on a remote backend (success criterion 3).
- Plugin confirmed discovered, enabled (hermes plugins enable kajiba), and loaded; the discovery dir resolved from the installed source (D-17, D-19; success criterion 2).
- 06-HOOK-KWARGS.md written with dual-tagged [DOCUMENTED v0.15.x] + [VERIFIED] per-hook tables (D-16; unblocks Phase 7).
- plugin.yaml manifest tags promoted [ASSUMED] -> [CONFIRMED v0.15.x] (D-18); pytest tests/test_plugin.py -x stays green.
</verification>

<success_criteria>
- Live native-Windows env verified end-to-end on a remote backend with no GPU/Ollama (ENV-01/ENV-02 native-primary intent).
- Plugin discovered, enabled, and loaded by Hermes via the resolved discovery dir + hermes plugins enable kajiba; symlink (or copy-fallback) workflow confirmed (ENV-03, PLUG-01/PLUG-02 live half, D-19).
- All four hooks fire and log kwargs; real kwargs confirmed against the documented v0.15.x table and recorded in 06-HOOK-KWARGS.md dual-tagged [DOCUMENTED v0.15.x] + [VERIFIED] (CAPT-01, D-15/D-16/D-21).
- Discovery dir resolved (D-17, sets D-02 target); plugin.yaml promoted to [CONFIRMED v0.15.x] (D-18); post_tool_call result confirmed as str.
</success_criteria>

<output>
Create `.planning/phases/06-environment-plugin-foundation/06-05-SUMMARY.md` when done
</output>
