---
phase: 06-environment-plugin-foundation
plan: 05
type: execute
wave: 5
depends_on: ["06-03", "06-04"]
files_modified:
  - .planning/phases/06-environment-plugin-foundation/06-HOOK-KWARGS.md
autonomous: false
requirements: [ENV-01, ENV-02, ENV-03, PLUG-01, PLUG-02, CAPT-01]
must_haves:
  truths:
    - "Hermes Agent v0.6.0 discovers and loads the Kajiba plugin from ~/.hermes/plugins/kajiba on startup"
    - "Running a live Hermes session fires on_session_start, post_llm_call, post_tool_call, and on_session_end, each logging its kwargs"
    - "The actual kwargs (names, types, values) for all four hooks are captured and recorded in 06-HOOK-KWARGS.md"
    - "WSL2 + NVIDIA GPU + Ollama + Hermes are verified working with GPU acceleration (VRAM in use during inference)"
    - "The symlink dev workflow loads the plugin (or the documented copy-fallback if symlinks are not followed)"
  artifacts:
    - path: ".planning/phases/06-environment-plugin-foundation/06-HOOK-KWARGS.md"
      provides: "Ground-truth hook kwargs captured from a live Hermes v0.6.0 session (unblocks Phase 7)"
      contains: "on_session_start"
  key_links:
    - from: "live Hermes session"
      to: "src/kajiba/plugin (via ~/.hermes/plugins/kajiba symlink)"
      via: "Hermes plugin discovery + register(ctx) on startup"
      pattern: "register"
    - from: "KAJIBA_DEBUG=1 logs"
      to: "06-HOOK-KWARGS.md"
      via: "developer records observed kwarg names/types/values"
      pattern: "KAJIBA_DEBUG"
---

<objective>
Run the live verification that cannot be unit-tested: stand up the WSL2 + GPU + Ollama + Hermes v0.6.0 environment per docs/hermes-setup.md, load the Kajiba plugin via the symlink workflow, run a short Hermes session with `KAJIBA_DEBUG=1`, and confirm all four lifecycle hooks fire and log their kwargs. Capture the actual kwarg names/types/values for `on_session_start`, `post_llm_call`, `post_tool_call`, `on_session_end` into `06-HOOK-KWARGS.md` (D-06), converting the `[ASSUMED]` signatures into `[VERIFIED]` ground truth for Phase 7.

Purpose: This closes success criteria 1, 2, 3, 5 (the live half) and produces the single most important downstream artifact of the phase — the empirically-confirmed hook kwargs that Phase 7 turn assembly depends on. The whole point of the KAJIBA_DEBUG mode (built in Plan 03) is realized here.
Output: 06-HOOK-KWARGS.md with the live-captured kwargs, plus checkpoint confirmation of the env + plugin load + symlink workflow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-environment-plugin-foundation/06-RESEARCH.md
@.planning/phases/06-environment-plugin-foundation/06-VALIDATION.md
@.planning/phases/06-environment-plugin-foundation/06-CONTEXT.md
@docs/hermes-setup.md
@src/kajiba/plugin/__init__.py
@src/kajiba/plugin/hooks.py
</context>

<artifacts_this_phase_produces>
This plan creates `.planning/phases/06-environment-plugin-foundation/06-HOOK-KWARGS.md` (the live-captured ground-truth kwargs). Drift verification: a new planning artifact only.
</artifacts_this_phase_produces>

<live_env_note>
Every task here REQUIRES a live WSL2 + NVIDIA passthrough + Ollama + Hermes v0.6.0 environment that the developer sets up manually by following docs/hermes-setup.md (Plan 04). These behaviors cannot be unit-tested on the Windows host (06-RESEARCH § Environment Availability, § Validation Architecture Manual-checkpoint split). All tasks are checkpoint:human-verify, gated on the developer completing the env-setup stages. Claude automates everything it can up front (the plugin code + symlink command text are already produced by Plans 03/04); the human performs the install/run and reports observations, then Claude records them into 06-HOOK-KWARGS.md.
</live_env_note>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify WSL2 + GPU + Ollama + Hermes environment (ENV-01/ENV-02)</name>
  <files>(verification only — no files modified)</files>
  <read_first>
    - docs/hermes-setup.md (the stages + verification checkpoints to follow)
    - 06-RESEARCH.md § Environment Availability (dependency table + "missing dependencies expected" note lines 427-436)
  </read_first>
  <action>
    Claude: there is nothing to automate for this task (the env install is human-only — no CLI Claude can run on the Windows host stands up WSL2/GPU/Ollama/Hermes). Present the verification checkpoint below and pause for the developer. After the developer reports results, if any guide step failed, note the correction for a follow-up docs update; do not proceed past this blocking gate until "env verified".
  </action>
  <what-built>
    Plan 04 produced docs/hermes-setup.md with checkpoint-gated stages for WSL2, NVIDIA GPU passthrough, Ollama, and Hermes Agent v0.6.0, including the CUDA-stub / num_ctx / Ollama-binding troubleshooting. There is no Kajiba code to run for this task — it verifies the developer's environment is correctly installed.
  </what-built>
  <how-to-verify>
    Follow docs/hermes-setup.md stages 1-5. Then confirm each verification checkpoint:
    1. WSL2: `wsl --status` shows WSL2; a distro shell opens.
    2. GPU: inside WSL2, `nvidia-smi` lists the RTX 4070 with VRAM. Confirm `libcuda.so` is a symlink (CUDA stub not overwritten — MP-8).
    3. Ollama: `ollama run hermes3` (or the pulled Hermes 3 8B tag) produces output, AND `nvidia-smi` shows VRAM in use during that inference (proves GPU, not CPU fallback). Confirm `num_ctx` was set per the guide.
    4. Hermes: Hermes Agent v0.6.0 starts; note the value of `HERMES_HOME` (or default `~/.hermes`).
    Report any stage that fails and which troubleshooting section you used.
  </how-to-verify>
  <verify>
    <human-check>Developer confirms nvidia-smi shows the GPU inside WSL2, Ollama inference uses VRAM (GPU not CPU), and Hermes v0.6.0 starts (success criterion 1). Live-only — no automated command on the Windows host.</human-check>
  </verify>
  <acceptance_criteria>
    - Developer confirms: nvidia-smi shows the GPU inside WSL2; Ollama inference uses VRAM (GPU acceleration, not CPU); Hermes v0.6.0 starts. (Success criterion 1.)
    - If any layer failed, the developer reports which guide step/troubleshooting resolved it (feeds guide corrections).
  </acceptance_criteria>
  <resume-signal>Type "env verified" (optionally noting GPU VRAM seen during inference and HERMES_HOME value), or describe what failed.</resume-signal>
  <done>Developer confirms the full env (WSL2 + GPU + Ollama + Hermes v0.6.0) works with GPU acceleration.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Symlink + load the plugin in live Hermes; confirm registration (ENV-03, PLUG-01/PLUG-02)</name>
  <files>(verification only — no files modified)</files>
  <read_first>
    - docs/hermes-setup.md § Kajiba plugin / symlink workflow + "Plugin loads" checkpoint
    - src/kajiba/plugin/__init__.py (register(ctx) logs "Kajiba registered hooks: ...")
    - 06-RESEARCH.md § Symlink Dev Workflow (lines 250-261), Open Question Q3 / Assumption A3 (symlink discovery fallback)
  </read_first>
  <action>
    Claude: the symlink commands and the expected "Kajiba registered hooks: ..." log line are already produced by Plans 03/04 — present them in the checkpoint below for the developer to execute in their live WSL2 env. Pause for the developer. Record whether the symlink worked or a copy was needed (resolves A3) and whether plugin.yaml fields were accepted (A2) for the SUMMARY and any guide correction. Do not proceed until "plugin loaded".
  </action>
  <what-built>
    Plan 03 built src/kajiba/plugin/ (register(ctx) + plugin.yaml + KAJIBA_DEBUG hooks). Plan 04 documented the 2-3 command symlink workflow. This task deploys and confirms Hermes actually discovers and loads it.
  </what-built>
  <how-to-verify>
    1. With `pip install -e .` active in WSL2, from the repo root run: `mkdir -p ~/.hermes/plugins` then `ln -s "$(pwd)/src/kajiba/plugin" ~/.hermes/plugins/kajiba`.
    2. Start Hermes. In the Hermes/plugin log, confirm the line `Kajiba registered hooks: on_session_start, post_llm_call, post_tool_call, on_session_end` appears — this proves register(ctx) ran (plugin discovered + loaded).
    3. If the plugin does NOT load (no registration line), test whether Hermes follows the symlink: replace the symlink with a copy of src/kajiba/plugin into ~/.hermes/plugins/kajiba and restart. Report which method worked (this resolves Open Question Q3 / A3 and may require a one-line guide update).
    4. Also note whether `plugin.yaml`'s field names were accepted by Hermes (A2) or whether load complained about manifest fields.
  </how-to-verify>
  <verify>
    <human-check>Developer confirms the "Kajiba registered hooks: ..." line appears in the Hermes log after startup (plugin discovered + register(ctx) executed; success criteria 2 and 5), and reports symlink-vs-copy (A3) + plugin.yaml field acceptance (A2). Live-only.</human-check>
  </verify>
  <acceptance_criteria>
    - Developer confirms the "Kajiba registered hooks: ..." line appears in the Hermes log after startup (plugin discovered + register(ctx) executed). (Success criteria 2 and 5.)
    - Developer reports whether the symlink worked or a copy was needed (A3), and whether plugin.yaml fields were accepted as-is (A2).
  </acceptance_criteria>
  <resume-signal>Type "plugin loaded" (noting symlink-vs-copy and any plugin.yaml field issues), or describe the load failure.</resume-signal>
  <done>Developer confirms Hermes discovered and loaded the plugin (registration log line present); symlink-vs-copy and plugin.yaml findings recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run a live session with KAJIBA_DEBUG=1, capture the 4 hooks kwargs, and record 06-HOOK-KWARGS.md (CAPT-01, D-06)</name>
  <files>.planning/phases/06-environment-plugin-foundation/06-HOOK-KWARGS.md</files>
  <read_first>
    - src/kajiba/plugin/hooks.py (the _log_kwargs format: "KAJIBA_DEBUG <hook> kwarg <name>: type=<type> value=<truncated>")
    - 06-RESEARCH.md § Pattern 3 (debug discovery lines 222-248), § Pattern 1 hook→collector table (the [ASSUMED] kwargs to confirm, lines 192-197), Open Question Q1 (also log os.environ HERMES_HOME / inspect ctx — A4)
    - 06-CONTEXT.md D-06 (first-task discovery deliverable)
  </read_first>
  <action>
    Two parts. HUMAN part (live, blocking): the developer runs a short KAJIBA_DEBUG=1 Hermes session exercising all four hooks and provides the captured KAJIBA_DEBUG log lines back (paste or a file path). CLAUDE part (automated, after the human provides the lines): write `.planning/phases/06-environment-plugin-foundation/06-HOOK-KWARGS.md` — for each of the four hooks a table of kwarg name | type | example value | notes; tag each confirmed signature `[VERIFIED: live Hermes v0.6.0 session <date>]`; record the HERMES_HOME-in-plugin-process finding (resolves A4) and the post_tool_call `result`-type finding (Pitfall 7 — is it a JSON string?); end with a "Phase 7 implications" section mapping confirmed kwargs to the collector turn-assembly inputs (CAPT-02/CAPT-03). Use synthetic/placeholder example values where a real kwarg value would contain PII (record types/shapes, not real conversation content). Present the checkpoint below and pause for the developer's captured lines before writing the file.
  </action>
  <what-built>
    Plan 03's KAJIBA_DEBUG=1 mode logs every hook kwarg's name, type, and truncated value. This task runs a real short Hermes session to capture the ground-truth kwargs for all four hooks — the artifact Phase 7 turn assembly depends on.
  </what-built>
  <how-to-verify>
    1. Set `KAJIBA_DEBUG=1` in the Hermes process environment and start a SHORT Hermes session that: starts (fires on_session_start), sends at least one user message and gets an assistant response (fires post_llm_call), triggers at least one tool call (fires post_tool_call), and ends (fires on_session_end).
    2. Collect the `KAJIBA_DEBUG <hook> kwarg ...` log lines for all four hooks. For each hook, note: every kwarg NAME, its TYPE, and a representative (truncated) VALUE — INCLUDING any kwargs caught by `**kwargs` that the assumed signatures did not name.
    3. Note in particular: does `post_tool_call`'s `result` arrive as a JSON string (Pitfall 7)? Is `HERMES_HOME` present in the plugin process env (Open Question Q1 / A4)?
    4. Provide the captured log lines (or a transcription) back to Claude so it can write 06-HOOK-KWARGS.md.
  </how-to-verify>
  <verify>
    <human-check>All four hooks observed firing with their KAJIBA_DEBUG log lines captured (success criterion 3). After Claude writes the file, confirm 06-HOOK-KWARGS.md contains a per-hook kwarg table for all four hooks with [VERIFIED] tags, the A4 HERMES_HOME finding, and the Pitfall 7 result-type finding.</human-check>
  </verify>
  <acceptance_criteria>
    - All four hooks observed firing with their KAJIBA_DEBUG log lines captured. (Success criterion 3.)
    - 06-HOOK-KWARGS.md exists and contains a per-hook kwarg table for all four hooks with name/type/example, the HERMES_HOME-in-plugin-process finding (A4), and the post_tool_call result-type finding (Pitfall 7).
    - Each hook's confirmed kwargs are tagged [VERIFIED: live Hermes v0.6.0 session ...] (no longer [ASSUMED]).
  </acceptance_criteria>
  <resume-signal>Paste the KAJIBA_DEBUG log lines for the four hooks (or confirm Claude may read them from a file you provide), then type "kwargs captured".</resume-signal>
  <done>All four hooks confirmed firing; 06-HOOK-KWARGS.md written with [VERIFIED] per-hook kwarg tables, the A4 and Pitfall 7 findings, and a Phase 7 implications section.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live Hermes session content → KAJIBA_DEBUG logs | Real prompts/responses (PII) flow into debug logs during capture |
| Hermes host → symlinked plugin code execution | Hermes executes the developer's plugin code from the symlinked dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Information Disclosure | KAJIBA_DEBUG live logs + 06-HOOK-KWARGS.md capturing real session content/PII | mitigate | _log_kwargs truncates values to 120 chars (Plan 03); when recording 06-HOOK-KWARGS.md, use synthetic/placeholder example values where a real kwarg value would contain PII — record TYPES and SHAPES, not real conversation content. Run the discovery session with throwaway, non-sensitive prompts. |
| T-06-12 | Tampering / EoP | Hermes loading code from the symlinked plugin dir | accept | Local-first, single-operator: the symlink points to the developer's own repo code (already trusted). Documented as a dev-only workflow. |
| T-06-SC | Tampering | system-component installs verified live (Ollama/Hermes/CUDA) | accept | Installed via official vendor channels per the guide; not registry packages — slopcheck N/A |
</threat_model>

<verification>
- All four hooks confirmed firing in a live Hermes v0.6.0 session (success criterion 3).
- Plugin confirmed discovered + loaded via the symlink workflow (success criteria 2, 5).
- WSL2 + GPU + Ollama + Hermes confirmed with GPU acceleration (success criterion 1).
- 06-HOOK-KWARGS.md written with [VERIFIED] per-hook kwarg tables (D-06; unblocks Phase 7).
</verification>

<success_criteria>
- Live env verified end-to-end with GPU acceleration (ENV-01/ENV-02).
- Plugin discovered and loaded by Hermes via the documented symlink (or copy-fallback) workflow (ENV-03, PLUG-01/PLUG-02 live half).
- All four hooks fire and log kwargs; real kwargs captured in 06-HOOK-KWARGS.md and tagged [VERIFIED] (CAPT-01, D-06).
- Open Questions Q1 (HERMES_HOME in plugin process / A4), Q3 (symlink discovery / A3), and Pitfall 7 (post_tool_call result type) resolved and recorded.
</success_criteria>

<output>
Create `.planning/phases/06-environment-plugin-foundation/06-05-SUMMARY.md` when done
</output>
