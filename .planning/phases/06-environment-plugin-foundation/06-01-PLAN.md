---
phase: 06-environment-plugin-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_config.py
  - tests/test_plugin.py
  - tests/test_no_hermes_integration.py
autonomous: true
requirements: [PLUG-03, PLUG-02, CAPT-01]
must_haves:
  truths:
    - "Wave 0 test scaffolds exist and are RED (failing) only on not-yet-built symbols, not on import/collection errors in the existing suite"
    - "A static guard test exists that fails if any source file imports hermes_integration"
    - "A stub ctx fixture records register_hook(event, callback) calls for plugin tests"
  artifacts:
    - path: "tests/test_config.py"
      provides: "get_hermes_home() unit cases (env set / unset / temp isolation)"
      contains: "get_hermes_home"
    - path: "tests/test_plugin.py"
      provides: "stub ctx, register(ctx) wiring, **kwargs tolerance, KAJIBA_DEBUG logging tests"
      contains: "register_hook"
    - path: "tests/test_no_hermes_integration.py"
      provides: "static guard that no source imports hermes_integration"
      contains: "hermes_integration"
  key_links:
    - from: "tests/test_plugin.py"
      to: "kajiba.plugin"
      via: "import (RED until Plan 03 creates the package)"
      pattern: "from kajiba.plugin"
    - from: "tests/test_config.py"
      to: "kajiba.config.get_hermes_home"
      via: "import (RED until Plan 02 adds the helper)"
      pattern: "get_hermes_home"
---

<objective>
Create the Wave 0 test scaffolds for Phase 6 so every downstream code task lands against a pre-written, executable test (Nyquist compliance). This plan writes failing tests for the three code surfaces of the phase: the `get_hermes_home()` path helper (Plan 02), the plugin `register(ctx)` + hook handlers (Plan 03), and a static guard that locks the `hermes_integration.py` deletion (Plan 02). It also adds the stub `ctx` fixture the plugin tests depend on.

Purpose: Per 06-VALIDATION.md, no code task in this phase ships without an automated `<automated>` verify that already exists. This plan is that scaffold. The tests are RED on purpose — they fail only because the symbols they import do not exist yet (kajiba.config.get_hermes_home, kajiba.plugin), NOT because they break the existing 322-passing suite.
Output: Three test files (one new module test, one new static guard, one extension of the existing config test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-environment-plugin-foundation/06-VALIDATION.md
@.planning/phases/06-environment-plugin-foundation/06-RESEARCH.md
@.planning/phases/06-environment-plugin-foundation/06-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols/files this phase introduces (drift verification must treat these as newly-created, not missing):
- `kajiba.config.get_hermes_home` (function) — added in Plan 02
- `src/kajiba/plugin/__init__.py` with `register(ctx)` — Plan 03
- `src/kajiba/plugin/hooks.py` with `on_session_start`, `on_post_llm_call`, `on_post_tool_call`, `on_session_end`, `set_collector`, `_log_kwargs` — Plan 03
- `src/kajiba/plugin/plugin.yaml` — Plan 03
- `KAJIBA_DEBUG` env flag (read in hooks.py) — Plan 03
- Collector keyword-only params `model_name`, `platform` on `on_session_start` — Plan 02
- `docs/hermes-setup.md` — Plan 04
- `tests/test_plugin.py`, `tests/test_no_hermes_integration.py`, `06-HOOK-KWARGS.md` — this phase
- DELETED: `src/kajiba/hermes_integration.py`, its `HermesAgent` Protocol, `register_hooks()` — Plan 02
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add get_hermes_home() unit cases to tests/test_config.py</name>
  <read_first>
    - tests/test_config.py (full — reuse the existing `fake_home` fixture at lines 21-25 which monkeypatches `Path.home`; reuse the class-per-feature structure)
    - 06-PATTERNS.md § `tests/test_config.py` (the TestGetHermesHome class skeleton, lines 252-267)
    - 06-RESEARCH.md § Validation Architecture (PLUG-03 test rows) and Code Examples (`get_hermes_home()` contract, lines 339-359)
  </read_first>
  <action>
    Add a new test class `TestGetHermesHome` to tests/test_config.py covering the get_hermes_home() contract (D-10). Required test methods and their exact node names (the verify commands depend on these names): `test_get_hermes_home_env` — `monkeypatch.setenv("HERMES_HOME", str(tmp_path))` then assert `get_hermes_home() == tmp_path`; `test_get_hermes_home_unset` — uses the existing `fake_home` fixture + `monkeypatch.delenv("HERMES_HOME", raising=False)`, assert `get_hermes_home() == fake_home / ".hermes"`. Add at least one path-constant isolation test whose name contains `hermes_home` (so `pytest -k hermes_home` selects it) that sets HERMES_HOME to a temp dir and asserts a derived base resolves under it. Import `get_hermes_home` from `kajiba.config` INSIDE each test method (deferred import) so the file still collects cleanly while the helper does not yet exist. Do NOT modify the `yaml = pytest.importorskip("yaml")` line or existing fixtures. Honor CLAUDE.md: double quotes, `from typing import Optional` style (file already imports it), Google-free test docstrings are fine but keep one-line docstrings per existing convention.
  </action>
  <verify>
    <automated>python -m pytest tests/test_config.py -k hermes_home --co -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_config.py -k hermes_home --co -q` lists at least `test_get_hermes_home_env`, `test_get_hermes_home_unset`, and one isolation test (collection succeeds — file has no syntax/collection error).
    - Running `python -m pytest tests/test_config.py -k hermes_home -x` FAILS with an ImportError/AttributeError on `get_hermes_home` (RED — symbol not built until Plan 02), NOT a collection error.
    - The pre-existing config tests still collect: `python -m pytest tests/test_config.py --co -q` exits 0.
  </acceptance_criteria>
  <done>TestGetHermesHome added with the three named tests; file collects; the new tests are RED only on the missing `get_hermes_home` symbol.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/test_plugin.py with stub ctx fixture, register wiring, **kwargs tolerance, and KAJIBA_DEBUG logging tests</name>
  <read_first>
    - 06-PATTERNS.md § `tests/test_plugin.py` (StubCtx fixture + the three test names: test_register_hooks, test_handlers_accept_extra_kwarg, test_debug_logging, lines 230-248)
    - 06-RESEARCH.md § Pattern 1 (register(ctx) registers 4 hooks: on_session_start, post_llm_call, post_tool_call, on_session_end), § Pattern 3 (KAJIBA_DEBUG=1 logs kwarg name/type/value via logger.warning), § Validation Architecture (PLUG-02/CAPT-01/MP-2 rows)
    - tests/test_collector.py (lines 1-50 — class-per-feature, direct instantiation style)
  </read_first>
  <action>
    Create tests/test_plugin.py (NEW) with a module docstring. Define a `StubCtx` class with `register_hook(self, event, callback)` that records into a `self.hooks: dict` (event -> callback), and a `stub_ctx` pytest fixture returning a fresh StubCtx. Write these named tests (verify commands key off these exact node names): `test_register_hooks` — call `register(stub_ctx)` and assert all four event keys present in `stub_ctx.hooks`: `"on_session_start"`, `"post_llm_call"`, `"post_tool_call"`, `"on_session_end"`; `test_handlers_accept_extra_kwarg` — import each of the four handlers and call each with an UNEXPECTED extra kwarg (e.g. `on_session_start(session_id="x", surprise="z")`) and assert no exception raises (MP-2 tolerance); `test_debug_logging` — set `KAJIBA_DEBUG=1` and assert a handler emits a `logger.warning` record matching `KAJIBA_DEBUG` via `caplog`. CRITICAL for the debug test: `_DEBUG` is read at import time in hooks.py (`_DEBUG = os.environ.get("KAJIBA_DEBUG") == "1"`), so the test MUST either reload the hooks module after `monkeypatch.setenv` (importlib.reload) or patch the module attribute directly (`monkeypatch.setattr("kajiba.plugin.hooks._DEBUG", True)`) — write the test to patch the module attribute (simplest, no reload). Import `register` from `kajiba.plugin` and the handlers from `kajiba.plugin.hooks` INSIDE the test bodies (deferred import) so the file collects while the package does not yet exist. Honor CLAUDE.md (double quotes, typing.Optional if needed, no print).
  </action>
  <verify>
    <automated>python -m pytest tests/test_plugin.py --co -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_plugin.py --co -q` collects `test_register_hooks`, `test_handlers_accept_extra_kwarg`, `test_debug_logging` (no collection/syntax error).
    - `python -m pytest tests/test_plugin.py -x` FAILS with ModuleNotFoundError on `kajiba.plugin` (RED — package built in Plan 03), NOT a collection error.
    - `test_register_hooks` asserts the four exact event-name strings; `test_handlers_accept_extra_kwarg` calls handlers with an extra kwarg; `test_debug_logging` patches `kajiba.plugin.hooks._DEBUG` (or reloads) rather than relying on setenv-after-import.
  </acceptance_criteria>
  <done>tests/test_plugin.py created with StubCtx fixture and the three named RED tests; collects cleanly; fails only on the missing kajiba.plugin package.</done>
</task>

<task type="auto">
  <name>Task 3: Create tests/test_no_hermes_integration.py static guard locking the D-07 deletion</name>
  <read_first>
    - 06-RESEARCH.md § State of the Art / Deprecated (the module + symbols to be gone) and § Validation Architecture (D-07 row: "no import references remain")
    - 06-VALIDATION.md Wave 0 Requirements bullet 4 (grep/static guard)
    - src/kajiba/ directory layout (the guard scans src/kajiba/*.py)
  </read_first>
  <action>
    Create tests/test_no_hermes_integration.py (NEW) with a module docstring. Write `test_no_source_imports_hermes_integration` that walks every `.py` file under `src/kajiba/` (use `pathlib.Path(__file__).resolve().parents[1] / "src" / "kajiba"` then `.rglob("*.py")`) and asserts NONE contain the substring `hermes_integration` in any import statement — read each file's text and assert `"import hermes_integration" not in text` and `"from kajiba.hermes_integration" not in text` and `"from .hermes_integration" not in text`. Add `test_hermes_integration_module_absent` that asserts `import kajiba.hermes_integration` raises ModuleNotFoundError (use `pytest.raises(ModuleNotFoundError)` with `importlib.import_module`). This test is RED NOW (the module still exists until Plan 02 deletes it) and turns GREEN after Plan 02. Honor CLAUDE.md conventions.
  </action>
  <verify>
    <automated>python -m pytest tests/test_no_hermes_integration.py --co -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_no_hermes_integration.py --co -q` collects `test_no_source_imports_hermes_integration` and `test_hermes_integration_module_absent` (no collection error).
    - With hermes_integration.py still present, `python -m pytest tests/test_no_hermes_integration.py -x` FAILS (RED — the module is still importable), confirming the guard is real and will flip GREEN after Plan 02 deletes it.
    - The source scan uses an absolute path derived from the test file location (works regardless of cwd, Windows-safe).
  </acceptance_criteria>
  <done>tests/test_no_hermes_integration.py created with two guard tests; collects; RED while the module exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → filesystem (src/ scan) | The static guard reads source files; read-only, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | test scaffolds | accept | Tests assert on symbol names/types only; no real session payloads or PII handled in Wave 0 |
| T-06-02 | Tampering | (no installs) | N/A | This plan installs no registry packages (06-RESEARCH Package Legitimacy Audit: none new) |
| T-06-SC | Tampering | npm/pip/cargo installs | N/A | No package-manager installs in this phase; slopcheck not applicable |
</threat_model>

<verification>
- `python -m pytest tests/test_config.py --co -q` and `tests/test_plugin.py --co -q` and `tests/test_no_hermes_integration.py --co -q` all collect without error.
- The three new test surfaces are RED only on not-yet-built symbols (get_hermes_home, kajiba.plugin) or the not-yet-deleted module (hermes_integration), NOT on existing-suite breakage.
- Pre-existing suite still collects: `python -m pytest --co -q` exits 0.
</verification>

<success_criteria>
- get_hermes_home() cases added to tests/test_config.py (PLUG-03 scaffold).
- tests/test_plugin.py created with stub ctx fixture + register/kwargs/debug RED tests (PLUG-02, CAPT-01, MP-2 scaffold).
- tests/test_no_hermes_integration.py created with the D-07 static guard (RED until Plan 02).
- No existing test broken (collection clean across the suite).
</success_criteria>

<output>
Create `.planning/phases/06-environment-plugin-foundation/06-01-SUMMARY.md` when done
</output>
