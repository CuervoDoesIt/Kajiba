---
phase: 10-experiment-schema-foundation
plan: 03
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - tests/test_schema_backcompat.py
  - tests/test_schema_experiment.py
autonomous: true
requirements: [ESCH-01, ESCH-02, ESCH-03, ESCH-04, ESCH-05]
must_haves:
  truths:
    - "ESCH-04: a parametrized golden test proves post-refactor record_id + submission_hash are byte-identical to tests/fixtures/golden_ids.json for every fixture, and every legacy dict loads via validate_record() without error."
    - "ESCH-01: a record dict omitting record_kind validates and yields record_kind == 'coding_session'."
    - "ESCH-02: issubclass(KajibaRecord, RecordBase) and issubclass(ExperimentRecord, RecordBase) hold, and base-inherited attrs are present on instances of each."
    - "ESCH-03: a fully populated ExperimentRecord round-trips model_dump(mode='json', by_alias=True) -> model_validate to an equal record; out-of-vocab experiment_type/recommended_action raise ValidationError; recommended_action=None accepted; lessons_learned defaults to []."
    - "ESCH-05: validate_record() returns a KajibaRecord; load_record() returns an ExperimentRecord for a model_experiment dict and a KajibaRecord otherwise."
    - "The full existing suite stays green (no regression)."
  artifacts:
    - path: "tests/test_schema_backcompat.py"
      provides: "Golden-ID stability, legacy-load, record_kind default, base inheritance, and load-dispatch tests (ESCH-01/02/04/05)"
      contains: "test_record_id_and_submission_hash_stable"
    - path: "tests/test_schema_experiment.py"
      provides: "ExperimentRecord round-trip, vocab rejection, recommended_action=None, lessons_learned default (ESCH-03)"
      contains: "ExperimentRecord"
  key_links:
    - from: "tests/test_schema_backcompat.py"
      to: "tests/fixtures/golden_ids.json"
      via: "parametrized read of the committed baseline"
      pattern: "golden_ids.json"
    - from: "tests/test_schema_backcompat.py"
      to: "kajiba.schema.validate_record / load_record / RecordBase / ExperimentRecord"
      via: "import + assertions"
      pattern: "from kajiba.schema import"
---

<objective>
Author the two new test modules that lock every Phase 10 acceptance criterion: `tests/test_schema_backcompat.py` (ESCH-01/02/04/05) and `tests/test_schema_experiment.py` (ESCH-03). These are the executable proof that the refactor in plan 10-02 satisfies the spec — most importantly the ESCH-04 byte-identical-hash guarantee against the golden baseline captured in plan 10-01.

Purpose: Convert the VALIDATION.md test map into runnable pytest, in the house style of `tests/test_scorer.py`. Once these pass alongside the existing suite, the phase's goal-backward truths are all verifiable by command.

Output: Two new test files; the full suite (`python -m pytest -q`) green with no regressions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-experiment-schema-foundation/10-SPEC.md
@.planning/phases/10-experiment-schema-foundation/10-RESEARCH.md
@.planning/phases/10-experiment-schema-foundation/10-PATTERNS.md
@.planning/phases/10-experiment-schema-foundation/10-VALIDATION.md
@src/kajiba/schema.py
@tests/test_scorer.py
@.planning/phases/10-experiment-schema-foundation/10-02-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan's contributions)

- `tests/test_schema_backcompat.py` — NEW. Tests:
  - `test_record_id_and_submission_hash_stable` (parametrized over `golden_ids.json` keys) — ESCH-04
  - `test_legacy_dicts_load` — ESCH-04
  - `test_record_kind_default` — ESCH-01
  - `test_base_inheritance` — ESCH-02
  - `test_load_dispatch` — ESCH-05
- `tests/test_schema_experiment.py` — NEW. Tests: ExperimentRecord JSON round-trip, out-of-vocab `experiment_type`/`recommended_action` rejection, `recommended_action=None` accepted, `lessons_learned` default — ESCH-03.

(Schema symbols are delivered by plan 10-02; the golden baseline `tests/fixtures/golden_ids.json` by plan 10-01. This plan consumes both.)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Write tests/test_schema_backcompat.py (ESCH-01/02/04/05)</name>
  <files>tests/test_schema_backcompat.py</files>
  <read_first>
    - tests/test_scorer.py (lines 1-34) — house style: `FIXTURES = Path(__file__).parent / "fixtures"`, `_load_fixture`, imports from `kajiba.schema`, `@pytest.mark.parametrize`.
    - .planning/phases/10-experiment-schema-foundation/10-PATTERNS.md § "tests/test_schema_backcompat.py + tests/test_schema_experiment.py" — the exact test list and the parametrized golden-ID snippet.
    - .planning/phases/10-experiment-schema-foundation/10-VALIDATION.md § "Per-Task Verification Map" and § "Wave 0 Requirements" — the canonical test names and commands.
    - src/kajiba/schema.py — confirm `RecordBase`, `KajibaRecord`, `ExperimentRecord`, `validate_record`, `load_record` are importable post-10-02.
    - tests/fixtures/golden_ids.json — the baseline these tests assert against.
  </read_first>
  <action>
    Create `tests/test_schema_backcompat.py` in the `tests/test_scorer.py` house style: module docstring; `import json`, `from pathlib import Path`, `import pytest`; `from kajiba.schema import RecordBase, KajibaRecord, ExperimentRecord, validate_record, load_record`; `FIXTURES = Path(__file__).parent / "fixtures"`; reuse a `_load_fixture(name)` helper (`json.loads((FIXTURES / name).read_text(encoding="utf-8"))`); and `GOLDEN = json.loads((FIXTURES / "golden_ids.json").read_text(encoding="utf-8"))`. Implement these tests with one-line docstrings, double-quoted strings, type-annotated signatures (`-> None`): (1) `test_record_id_and_submission_hash_stable` — `@pytest.mark.parametrize("name", list(GOLDEN.keys()))`, load each fixture, `rec = validate_record(data)`, assert `rec.compute_record_id() == GOLDEN[name]["record_id"]` and `rec.compute_submission_hash() == GOLDEN[name]["submission_hash"]` (ESCH-04 tripwire). (2) `test_legacy_dicts_load` — parametrized over `GOLDEN.keys()`, assert `isinstance(validate_record(data), KajibaRecord)` (loads without error). (3) `test_record_kind_default` — load a fixture that omits `record_kind` (e.g. `minimal_trajectory.json`), assert `validate_record(data).record_kind == "coding_session"` (ESCH-01). (4) `test_base_inheritance` — assert `issubclass(KajibaRecord, RecordBase)` and `issubclass(ExperimentRecord, RecordBase)`; load a KajibaRecord fixture and assert the inherited base attrs exist (`hasattr(rec, x)` for `model`, `hardware`, `submission`, `record_id`, `submission_hash`, `created_at`, `schema_version`, `record_kind`) (ESCH-02). (5) `test_load_dispatch` — assert `isinstance(load_record(minimal_data), KajibaRecord)` (no `record_kind` key) and assert `validate_record(minimal_data)` is a `KajibaRecord`; build a minimal `model_experiment` dict (use `ExperimentMetadata`/`ExperimentOutcome` constructed objects `.model_dump(mode="json")` or a literal dict with `record_kind="model_experiment"`, an `experiment` block, and an `outcome` block) and assert `isinstance(load_record(exp_data), ExperimentRecord)` (ESCH-05). Do NOT use watch-mode flags. Do NOT modify `src/kajiba/schema.py`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_schema_backcompat.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_schema_backcompat.py -x -q` passes with all five test functions collected and green.
    - `python -m pytest tests/test_schema_backcompat.py::test_record_id_and_submission_hash_stable -q` is parametrized over the five `golden_ids.json` keys and passes (ESCH-04).
    - `test_record_kind_default` proves a dict without `record_kind` → `"coding_session"` (ESCH-01); `test_base_inheritance` proves both `issubclass` relations and inherited attrs (ESCH-02); `test_load_dispatch` proves the `validate_record`/`load_record` split (ESCH-05).
    - The file imports `RecordBase`, `ExperimentRecord`, and `load_record` from `kajiba.schema` (verifiable in source).
    - `git diff --quiet src/kajiba/schema.py` exits 0 (schema not edited in this plan).
  </acceptance_criteria>
  <done>test_schema_backcompat.py exists in house style and passes, locking ESCH-01/02/04/05 against the golden baseline and the refactored schema.</done>
</task>

<task type="auto">
  <name>Task 2: Write tests/test_schema_experiment.py (ESCH-03) and run the full suite</name>
  <files>tests/test_schema_experiment.py</files>
  <read_first>
    - tests/test_scorer.py — house style (class grouping `TestXxx`, `pytest.raises`, fixtures helper).
    - .planning/phases/10-experiment-schema-foundation/10-PATTERNS.md § "tests/.../test_schema_experiment.py" — round-trip + vocab-rejection requirements.
    - .planning/phases/10-experiment-schema-foundation/10-SPEC.md § Requirements R3 + § "Acceptance Criteria" — the exact ExperimentRecord behaviors to assert.
    - src/kajiba/schema.py — `ExperimentMetadata`, `ExperimentOutcome`, `ExperimentRecord`, `ModelMetadata` constructors and field sets (post-10-02).
  </read_first>
  <action>
    Create `tests/test_schema_experiment.py` in the house style: module docstring; `import pytest`; `from datetime import datetime, UTC`; `import pydantic` (for `pydantic.ValidationError`); `from kajiba.schema import ModelMetadata, ExperimentMetadata, ExperimentOutcome, ExperimentRecord`. Add a small `_make_experiment_record(**overrides)` helper that builds a fully populated `ExperimentRecord` (a `ModelMetadata(model_name=...)` for `local_model`, a valid `experiment_type` such as `"model_evaluation"`, `task_category`, `task_description`, a tz-aware `started_at=datetime(..., tzinfo=UTC)`, and an `ExperimentOutcome` with `local_model_output`, `eval_score` in range, etc.). Implement tests (grouped in a `class TestExperimentRecord:` or as module functions, one-line docstrings, `-> None`): (1) `test_round_trip` — `rec = _make_experiment_record(...)`; `dumped = rec.model_dump(mode="json", by_alias=True)`; `rebuilt = ExperimentRecord.model_validate(dumped)`; assert `rebuilt == rec` (ESCH-03 round-trip). (2) `test_experiment_type_rejects_out_of_vocab` — `with pytest.raises(pydantic.ValidationError):` construct `ExperimentMetadata(... experiment_type="bogus" ...)`. (3) `test_recommended_action_rejects_out_of_vocab` — `with pytest.raises(pydantic.ValidationError):` construct `ExperimentOutcome(... recommended_action="nope" ...)`. (4) `test_recommended_action_none_accepted` — construct an `ExperimentOutcome` with `recommended_action=None` (or default) and assert `.recommended_action is None`. (5) `test_lessons_learned_defaults_empty` — construct an `ExperimentOutcome` without `lessons_learned` and assert it equals `[]`; also assert a `list[str]` is accepted. (6) `test_eval_score_bounds` — `with pytest.raises(pydantic.ValidationError):` construct an `ExperimentOutcome` with `eval_score=1.5` (out of 0.0-1.0). After writing the file, run the FULL suite (`python -m pytest -q`) to confirm no regression in `test_scorer.py` or any other module. Do NOT modify `src/kajiba/schema.py`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_schema_experiment.py -x -q && python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_schema_experiment.py -x -q` passes (all six behaviors green).
    - `test_round_trip` asserts `ExperimentRecord.model_validate(rec.model_dump(mode="json", by_alias=True)) == rec` (ESCH-03 round-trip).
    - Out-of-vocab `experiment_type` and `recommended_action` each raise `pydantic.ValidationError`; `recommended_action=None` is accepted; `lessons_learned` defaults to `[]`; `eval_score` outside `0.0-1.0` raises (ESCH-03 acceptance criteria).
    - `python -m pytest -q` (full suite) passes with NO regressions (existing `test_scorer.py` still green).
    - `git diff --quiet src/kajiba/schema.py` exits 0 (schema not edited in this plan).
  </acceptance_criteria>
  <done>test_schema_experiment.py exists and passes, locking ESCH-03; the full suite is green with no regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → schema models | Test-only code constructing/validating in-memory models from in-repo fixtures. No network, no external input, no secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Tampering | regression detection (the tests themselves are the integrity control) | mitigate | These tests ARE the mitigation for the phase's tampering threats: the parametrized golden test detects any silent change to legacy hashes (ESCH-04), and the vocab-rejection tests detect any controlled-vocabulary bypass (T-10-03). They run on every task commit and every wave merge per VALIDATION.md sampling. |
| T-10-08 | Information Disclosure | test fixtures / golden baseline | accept | Fixtures and `golden_ids.json` are public, PII-free test data already in the repo. |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No package installs — `pyproject.toml` must not grow (hard constraint). pytest is already a declared dev dependency. Nothing to gate. |

Highest realistic severity: **low**. No `high` blockers. Test-only plan; no network, no auth, no untrusted input.
</threat_model>

<verification>
- `python -m pytest tests/test_schema_backcompat.py tests/test_schema_experiment.py -x -q` passes (the VALIDATION.md quick-run command).
- `python -m pytest -q` (full suite) passes with no regressions.
- The parametrized golden test covers all five `golden_ids.json` keys.
- `git diff --quiet src/kajiba/schema.py` exits 0 — tests do not edit the schema.
</verification>

<success_criteria>
- All Phase 10 acceptance criteria are covered by automated tests: ESCH-01, ESCH-02, ESCH-03, ESCH-04, ESCH-05.
- The full existing suite stays green (no regression).
- Both test modules follow the `tests/test_scorer.py` house style; no watch-mode flags; feedback latency under ~10s.
</success_criteria>

<output>
Create `.planning/phases/10-experiment-schema-foundation/10-03-SUMMARY.md` when done. Record: the test functions added, confirmation the golden-ID parametrized test passes for all five fixtures, and confirmation the full suite is green. After this plan, consider updating `10-VALIDATION.md` frontmatter `wave_0_complete: true` / `nyquist_compliant: true` (note it in the summary; the actual flip can happen at verify-work).
</output>
