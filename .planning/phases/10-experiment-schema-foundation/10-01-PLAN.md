---
phase: 10-experiment-schema-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/capture_golden_ids.py
  - tests/fixtures/golden_ids.json
autonomous: true
requirements: [ESCH-04]
must_haves:
  truths:
    - "A golden baseline of record_id + submission_hash exists for every existing *_trajectory.json fixture, captured from the PRE-refactor schema (ESCH-04)."
    - "The baseline is committed to git BEFORE schema.py is edited, so the ESCH-04 back-compat guarantee is falsifiable."
    - "enriched_catalog.json is excluded because it has no trajectory and is not a KajibaRecord."
  artifacts:
    - path: "tests/fixtures/golden_ids.json"
      provides: "Committed golden baseline of record_id + submission_hash for the 5 *_trajectory.json fixtures (ESCH-04 tripwire)"
      contains: "minimal_trajectory.json"
    - path: "tests/capture_golden_ids.py"
      provides: "Reproducible capture script that generates golden_ids.json from the current (pre-refactor) schema"
  key_links:
    - from: "tests/capture_golden_ids.py"
      to: "kajiba.schema.validate_record"
      via: "import + compute_record_id/compute_submission_hash over fixtures"
      pattern: "from kajiba.schema import validate_record"
---

<objective>
Capture the ESCH-04 golden baseline: for every existing `*_trajectory.json` fixture, record the `record_id` and `submission_hash` produced by the CURRENT (pre-refactor) `schema.py`, and commit that baseline to `tests/fixtures/golden_ids.json`.

Purpose: This is the back-compat tripwire for the entire phase. The baseline MUST be generated from the pre-refactor schema. If `schema.py` were edited first, the baseline would capture post-refactor hashes and the ESCH-04 byte-identical guarantee (the dominant risk of this phase) would become unfalsifiable. Every schema-editing plan in this phase depends on this plan completing first.

Output: A reproducible capture script (`tests/capture_golden_ids.py`) and the committed baseline JSON (`tests/fixtures/golden_ids.json`) covering the five `*_trajectory.json` fixtures (NOT `enriched_catalog.json`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-experiment-schema-foundation/10-SPEC.md
@.planning/phases/10-experiment-schema-foundation/10-RESEARCH.md
@.planning/phases/10-experiment-schema-foundation/10-VALIDATION.md
@src/kajiba/schema.py
@tests/test_scorer.py
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan's contributions)

- `tests/capture_golden_ids.py` — NEW reproducible capture script (runs the procedure from `10-RESEARCH.md` § Golden Baseline Capture).
- `tests/fixtures/golden_ids.json` — NEW committed baseline mapping each of the 5 `*_trajectory.json` fixture filenames → `{record_id, submission_hash}` from the pre-refactor schema.

(Full phase symbol inventory — `RecordBase`, `ExperimentRecord`, `ExperimentMetadata`, `ExperimentOutcome`, `record_kind`, `RECORD_KINDS`/`RecordKindType`, `EXPERIMENT_TYPES`/`ExperimentTypeType`, `RECOMMENDED_ACTIONS`/`RecommendedActionType`, `load_record`, bumped `SCHEMA_VERSION`, `tests/test_schema_backcompat.py`, `tests/test_schema_experiment.py` — is delivered by plans 10-02 and 10-03.)
</artifacts_produced>

<critical_constraint>
DO NOT edit `src/kajiba/schema.py` in this plan. Not one character. The whole point of this plan is to snapshot the PRE-refactor hashes. The schema refactor happens in plan 10-02, which depends on this plan. If `schema.py` is touched before this baseline is committed, the ESCH-04 guarantee is destroyed.
</critical_constraint>

<tasks>

<task type="auto">
  <name>Task 1: Write the golden-baseline capture script</name>
  <files>tests/capture_golden_ids.py</files>
  <read_first>
    - src/kajiba/schema.py — confirm `validate_record(data) -> KajibaRecord`, `compute_record_id()`, `compute_submission_hash()` signatures (do NOT edit).
    - .planning/phases/10-experiment-schema-foundation/10-RESEARCH.md § "Golden Baseline Capture Procedure" — the exact procedure to replicate as a committed script.
    - tests/test_scorer.py (lines 1-34) — house style: `FIXTURES = Path(__file__).parent / "fixtures"`, `json.loads(... read_text(encoding="utf-8"))`.
  </read_first>
  <action>
    Create `tests/capture_golden_ids.py` as a standalone, runnable script (a `main()` plus `if __name__ == "__main__": main()`) that reproduces the capture procedure from `10-RESEARCH.md` § Golden Baseline Capture. It MUST: import `validate_record` from `kajiba.schema`; resolve the fixtures dir as `Path(__file__).parent / "fixtures"`; iterate `sorted(fixtures.glob("*_trajectory.json"))` (this naturally selects only the five record fixtures and excludes `enriched_catalog.json`, which lacks `trajectory`); for each, `json.loads(... read_text(encoding="utf-8"))`, call `validate_record(data)`, then build an entry `{"record_id": rec.compute_record_id(), "submission_hash": rec.compute_submission_hash()}` keyed by the fixture filename (`f.name`); add a defensive guard that skips any fixture whose loaded dict has no top-level `"trajectory"` key (belt-and-suspenders against accidentally including a non-KajibaRecord fixture); write the result to `tests/fixtures/golden_ids.json` via `json.dumps(out, indent=2, sort_keys=True)` with `encoding="utf-8"`; and print a one-line summary per fixture. Use double-quoted strings, a module docstring, and `Optional[X]` typing if any annotations are needed (do NOT use `X | None`) per CLAUDE.md. Do NOT add any new dependency. Do NOT import or reference any experiment/RecordBase symbol — none exist yet at this point.
  </action>
  <verify>
    <automated>python -c "import ast,sys; ast.parse(open('tests/capture_golden_ids.py',encoding='utf-8').read()); print('parse-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/capture_golden_ids.py` exists and parses (the AST check above prints `parse-ok`).
    - The script imports `validate_record` from `kajiba.schema` (verifiable: `python -c "import re; assert re.search(r'from kajiba.schema import .*validate_record', open('tests/capture_golden_ids.py',encoding='utf-8').read())"`).
    - The script globs `*_trajectory.json` (NOT a hardcoded list that includes `enriched_catalog.json`) — verify the literal `*_trajectory.json` appears in the file.
    - `src/kajiba/schema.py` is unmodified vs HEAD (`git diff --quiet src/kajiba/schema.py` exits 0).
  </acceptance_criteria>
  <done>The capture script exists, parses, imports the pre-refactor `validate_record`, targets only `*_trajectory.json`, and `schema.py` is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Run the script and commit the pre-refactor golden baseline</name>
  <files>tests/fixtures/golden_ids.json</files>
  <read_first>
    - tests/capture_golden_ids.py — the script created in Task 1.
    - .planning/phases/10-experiment-schema-foundation/10-RESEARCH.md — note the verified expected value for `minimal_trajectory.json`: `record_id == "kajiba_c2eac32fcdc4"` (research proved byte-identical output survives the later refactor).
  </read_first>
  <action>
    Run `python tests/capture_golden_ids.py` from the repo root (ensure the editable install is active; if import fails, `pip install -e .` is already linked per RESEARCH § Runtime State Inventory). This generates `tests/fixtures/golden_ids.json`. Confirm the file contains exactly five top-level keys — `adversarial_trajectory.json`, `gold_trajectory.json`, `minimal_trajectory.json`, `pii_trajectory.json`, `silver_trajectory.json` — and NOT `enriched_catalog.json`. Confirm `minimal_trajectory.json`'s `record_id` is `kajiba_c2eac32fcdc4` and that every `record_id` starts with `kajiba_` (12 hex chars) and every `submission_hash` starts with `sha256:`. This baseline is the ESCH-04 tripwire; it must be committed before any edit to `schema.py` (which happens in plan 10-02). Do NOT edit `src/kajiba/schema.py`.
  </action>
  <verify>
    <automated>python -c "import json; d=json.load(open('tests/fixtures/golden_ids.json',encoding='utf-8')); ks=set(d); assert ks=={'adversarial_trajectory.json','gold_trajectory.json','minimal_trajectory.json','pii_trajectory.json','silver_trajectory.json'}, ks; assert d['minimal_trajectory.json']['record_id']=='kajiba_c2eac32fcdc4', d['minimal_trajectory.json']; assert all(v['record_id'].startswith('kajiba_') and v['submission_hash'].startswith('sha256:') for v in d.values()); print('golden-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/golden_ids.json` exists with exactly the five `*_trajectory.json` keys above and excludes `enriched_catalog.json` (the automated check asserts the exact key set).
    - `minimal_trajectory.json` → `record_id == "kajiba_c2eac32fcdc4"` (matches the value RESEARCH proved survives the refactor).
    - Every entry's `record_id` starts with `kajiba_` and every `submission_hash` starts with `sha256:`.
    - `git diff --quiet src/kajiba/schema.py` still exits 0 (schema untouched).
  </acceptance_criteria>
  <done>`golden_ids.json` is generated from the pre-refactor schema with the exact five fixture keys and the expected `minimal_trajectory.json` id, ready to commit BEFORE the refactor; `schema.py` is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo fixtures → capture script | Trusted, in-repo JSON read by a local script. No network, no external input, no secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | `golden_ids.json` baseline integrity | mitigate | Baseline is generated by a committed, reproducible script (`capture_golden_ids.py`) from the pre-refactor schema and committed before any schema edit; anyone can re-run the script to detect tampering. The downstream parametrized test (plan 10-03) reads this file as ground truth. |
| T-10-02 | Information Disclosure | fixture content | accept | Fixtures are public test data already in the repo; no PII, no secrets. The script only hashes already-committed content. |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan or phase — `pyproject.toml` must not grow (hard constraint). No `[ASSUMED]`/`[SUS]` packages; nothing to gate. |

Highest realistic severity: **low**. No `high` blockers. No network, no auth, no user input beyond in-repo JSON.
</threat_model>

<verification>
- `tests/fixtures/golden_ids.json` exists, has exactly the five `*_trajectory.json` keys, and excludes `enriched_catalog.json`.
- `minimal_trajectory.json` record_id is `kajiba_c2eac32fcdc4`.
- `git diff --quiet src/kajiba/schema.py` exits 0 — the schema is provably untouched in this plan.
- `python tests/capture_golden_ids.py` is reproducible (re-running yields byte-identical JSON).
</verification>

<success_criteria>
- The golden baseline is captured from the pre-refactor schema and committed before any schema edit (ESCH-04 prerequisite satisfied).
- The capture is reproducible via a committed script.
- `schema.py` remains byte-for-byte unchanged in this plan.
</success_criteria>

<output>
Create `.planning/phases/10-experiment-schema-foundation/10-01-SUMMARY.md` when done. Note the captured `record_id`/`submission_hash` for each fixture and confirm `schema.py` was not edited.
</output>
