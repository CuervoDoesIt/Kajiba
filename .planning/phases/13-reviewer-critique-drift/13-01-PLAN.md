---
phase: 13-reviewer-critique-drift
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_experiment_store.py
  - tests/test_cli_experiment.py
  - tests/test_experiment_drift.py
autonomous: true
requirements: [EREV-01, EREV-02, EREV-03]
must_haves:
  truths:
    - "All Phase 13 behaviors have a RED test that fails for the right reason (missing impl, not a syntax error)"
    - "Test files never touch real ~/.hermes (store isolation enforced)"
    - "The store guard tests use the EQUAL predicate: accept store_dir == expected_base, reject store_dir != expected_base (incl. the production default-base path)"
    - "schema.py is provably untouched after this plan"
  artifacts:
    - path: "tests/test_experiment_drift.py"
      provides: "Pure compute_drift unit tests (new file)"
      contains: "compute_drift"
    - path: "tests/test_experiment_store.py"
      provides: "update_experiment overwrite/identity/EQUAL-guard + default-base tests (extended) + 3 migrated log_experiment tests"
      contains: "update_experiment"
    - path: "tests/test_cli_experiment.py"
      provides: "review/lessons/drift CLI + WR error-path tests (extended); _isolate_store also isolates experiment_store.EXPERIMENTS_DIR"
      contains: "experiment"
  key_links:
    - from: "tests/test_experiment_drift.py"
      to: "kajiba.experiment_drift.compute_drift"
      via: "import (RED until 13-03)"
      pattern: "from kajiba.experiment_drift import"
    - from: "tests/test_experiment_store.py"
      to: "kajiba.experiment_store.update_experiment"
      via: "import (RED until 13-02)"
      pattern: "update_experiment"
---

<objective>
Land all Wave 0 test scaffolds (RED) for Phase 13 BEFORE any implementation runs.
This is the Nyquist gate from `13-VALIDATION.md`: every Phase 13 behavior gets a
failing automated test now, so the implementation plans (13-02..13-05) turn them
GREEN.

Purpose: Test-first discipline. The drift algorithm and lesson parse rule are the
only genuinely new logic; the rest is composition. RED scaffolds lock the
contracts (function signatures, CLI flags, behaviors) the implementers must hit.
This plan also locks the store-guard contract to the EQUAL predicate
(`store_dir.resolve() == expected_base.resolve()`) with an `expected_base=None`
default that falls back to `experiment_store.EXPERIMENTS_DIR` read at CALL time —
so the production default path, the CLI isolation path, the new unit tests, and
the migrated pre-existing tests are ALL jointly satisfiable by one predicate.
Output: One new test file (`tests/test_experiment_drift.py`) plus extensions to
two existing test files, all RED for the correct reason (missing
`update_experiment` / `compute_drift` / new subcommands — NOT import or syntax
errors). schema.py untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-reviewer-critique-drift/13-CONTEXT.md
@.planning/phases/13-reviewer-critique-drift/13-RESEARCH.md
@.planning/phases/13-reviewer-critique-drift/13-PATTERNS.md
@.planning/phases/13-reviewer-critique-drift/13-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan creates (test-side only — no production symbols):
- NEW file `tests/test_experiment_drift.py` — pure `compute_drift` unit tests.
- Extensions to `tests/test_experiment_store.py` — `update_experiment` overwrite,
  identity-stable, EQUAL-predicate guard, and a production-shape default-base guard
  test; PLUS migration of the 3 pre-existing `log_experiment` tests to pass
  `expected_base=store` so they stay green under the tightened EQUAL guard.
- Extensions to `tests/test_cli_experiment.py` — `review`/`lessons`/`drift`
  integration + WR-01/02/03 error paths; `_isolate_store` also monkeypatches
  `experiment_store.EXPERIMENTS_DIR` so the store guard's default base is isolated.

Production symbols these tests TARGET (created in later plans, listed for the
executor's contract awareness):
`update_experiment(record, store_dir, *, expected_base=None) -> Path` (13-02; the
EQUAL guard `store_dir.resolve() == expected_base.resolve()`, default base read
from `experiment_store.EXPERIMENTS_DIR` at call time), `compute_drift(records,
threshold=DRIFT_THRESHOLD) -> dict[str, bool]` and `DRIFT_THRESHOLD = 0.15`
(13-03), the `review`/`lessons`/`drift` Click commands and
`_parse_lesson`/`_mutate_experiment`/`_read_critique_input` helpers (13-04/13-05).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extend tests/test_experiment_store.py — update_experiment overwrite, identity-stable, EQUAL expected_base guard + production-shape default-base guard (CR-01, WR-04); migrate the 3 pre-existing log_experiment tests</name>
  <files>tests/test_experiment_store.py</files>
  <read_first>
    - tests/test_experiment_store.py (the file being extended — reuse `_make_record` at :27-53 verbatim; the THREE pre-existing `log_experiment` tests this task MUST migrate are `test_log_writes_file` :75-87, `test_atomic_write` :90-100, `test_dedup_skip` :103-112 — all currently call `log_experiment(rec, tmp_path/"experiments")` with NO expected_base; `test_refuses_outbox_dir` :123-130 is preserved; mirror the atomic/dedup shapes for the new tests)
    - src/kajiba/experiment_store.py:49-105 (`log_experiment` — the analog `update_experiment` will mirror; the `dest.exists()` dedup-skip at :86-88 is the CR-01 bug; the leaf-name guard at :72-76 is the WR-04 defect being replaced by the EQUAL expected_base check; NOTE the module currently has NO `EXPERIMENTS_DIR` constant — 13-02 adds one as the guard default)
    - src/kajiba/schema.py:445-492 (`compute_record_id`/`compute_submission_hash` — identity excludes outcome, the proof in-place mutation keeps the filename byte-stable)
    - .planning/phases/13-reviewer-critique-drift/13-VALIDATION.md (Per-Task map rows for CR-01 + WR-04; the `-k "update or identity_stable"` and `-k guard` automated commands)
  </read_first>
  <action>
    LOCKED CONTRACT (matches 13-02's predicate exactly — the EQUAL predicate, NOT
    parent-of-dir). `update_experiment` and `log_experiment` BOTH take a keyword-only
    `expected_base: Optional[Path] = None` and guard via:
    raise `ValueError` unless `store_dir.resolve() == expected_base.resolve()`.
    When `expected_base is None`, the function falls back to the module constant
    `experiment_store.EXPERIMENTS_DIR`, READ AT CALL TIME (do not bind it as a
    signature default), so a monkeypatch of `experiment_store.EXPERIMENTS_DIR` is
    honored. Net: `store_dir == expected_base` → ACCEPTED; `store_dir != expected_base`
    → REJECTED. `store_dir` is a DIRECTORY and the accepted base IS that same
    directory (not its parent).

    --- (A) Migrate the 3 pre-existing log_experiment tests ---
    Edit `test_log_writes_file`, `test_atomic_write`, `test_dedup_skip` so every
    `log_experiment(...)` call passes `expected_base=store` (the same
    `tmp_path / "experiments"` they already use as `store_dir`). With
    `store_dir == expected_base` the EQUAL guard ACCEPTS them, so they stay GREEN once
    13-02 lands the guard. (Without this migration they would go RED, because the
    default base resolves to the real `~/.hermes/.../experiments`, which is not
    `tmp_path/"experiments"`.) These migrated tests still call only `log_experiment`
    (which gains the `expected_base` param in 13-02); their GREEN state is asserted by
    13-02's full-suite check, not by 13-01's own RED verify. Keep
    `test_refuses_outbox_dir` (:123-130): it calls `log_experiment(_make_record(),
    tmp_path/"outbox")` with NO expected_base → default base != `tmp_path/"outbox"` →
    still raises ValueError (WR-04 reject intent preserved). Optionally also pass
    `expected_base=tmp_path/"experiments"` there to assert reject explicitly.

    --- (B) Three new RED update_experiment tests (EQUAL shape) ---
    Import inside each test (mirroring `test_build_record`'s in-test import at :58).
    Build records via the existing `_make_record` helper. Use the SAME directory for
    both `store_dir` and `expected_base` on accept paths.

    `test_update_overwrites_in_place` (CR-01, T-13-CR01): `store_dir = tmp_path /
    "experiments"`. Call `update_experiment(rec, store_dir, expected_base=store_dir)`
    with `eval_score=0.50`, then again with the SAME identity but `eval_score=0.90`
    (vary via `_make_record(outcome=...)` keeping experiment_id / task_description /
    local_model_name / local_model_output / started_at identical), again
    `expected_base=store_dir`. Assert exactly one `exp_*.json` exists and the on-disk
    `outcome.eval_score == 0.90` (re-read via `json.loads`). Bug-closure assertion:
    dedup-skip would have left 0.50. (`store_dir == expected_base` → EQUAL → ACCEPTED.)

    `test_identity_stable_across_mutation` (CR-01): same accepted `store_dir = tmp_path
    / "experiments"`, `expected_base=store_dir`. Capture `record_id` and the returned
    `Path.name` after first write; mutate `reviewer_critique`, `lessons_learned`, and
    `drift_flag`; call `update_experiment(rec, store_dir, expected_base=store_dir)`;
    assert the returned path name is byte-identical and `record_id` unchanged.

    `test_update_guard_rejects_dir_outside_base` (WR-04, T-13-ERR): create
    `base = tmp_path / "experiments"` and `bad_dir = tmp_path / "elsewhere" /
    "experiments"`; assert `update_experiment(rec, bad_dir, expected_base=base)` raises
    `ValueError`. Rationale (EQUAL predicate): `bad_dir.resolve() != base.resolve()`,
    so it is rejected — while the accept tests pass `store_dir == expected_base` and
    are accepted by the SAME predicate. Jointly satisfiable by one EQUAL predicate.
    Keep the assertion behavior-level (raises `ValueError`); the predicate lives in
    13-02.

    --- (C) New production-shape default-base guard test (regression guard) ---
    `test_update_default_base_guard` (takes `monkeypatch: pytest.MonkeyPatch`): import
    `kajiba.experiment_store as store_mod`; `base = tmp_path / "experiments"`;
    `monkeypatch.setattr(store_mod, "EXPERIMENTS_DIR", base)`. ACCEPT — call
    `store_mod.update_experiment(rec, base)` with NO `expected_base`; assert it writes
    one `exp_*.json` (the default reads the monkeypatched `EXPERIMENTS_DIR` at call
    time → `base == base` → accepted). REJECT — call
    `store_mod.update_experiment(rec, tmp_path / "elsewhere")` with NO `expected_base`;
    assert `ValueError` (default base `tmp_path/"experiments"` != `tmp_path/"elsewhere"`).
    This proves the PRODUCTION default path (`update_experiment(rec, EXPERIMENTS_DIR)`
    with the module constant as fallback base) is accepted exactly as production uses
    it, and pins the default-binding-at-call-time rule so the parent-vs-equal
    regression cannot recur silently.

    All four new tests are RED now (ImportError/AttributeError on `update_experiment`,
    and AttributeError on `store_mod.EXPERIMENTS_DIR` until 13-02 adds it). Do NOT stub
    or add the production function/constant — that is 13-02's job. NO fenced code in
    this plan file; write actual Python in the test file.
  </action>
  <verify>
    <automated>python -m pytest tests/test_experiment_store.py -k "update or identity_stable or guard or default_base" -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_experiment_store.py -k "update or identity_stable or guard or default_base" -q` collects the 4 new tests (`test_update_overwrites_in_place`, `test_identity_stable_across_mutation`, `test_update_guard_rejects_dir_outside_base`, `test_update_default_base_guard`) and they FAIL for the right reason (missing `update_experiment` / `experiment_store.EXPERIMENTS_DIR`), NOT a syntax/collection error.
    - All accept paths use `store_dir == expected_base` (or, in the default-base test, `store_dir == EXPERIMENTS_DIR`); all reject paths use `store_dir != expected_base` — so the SINGLE EQUAL predicate 13-02 implements satisfies every case.
    - The 3 pre-existing tests (`test_log_writes_file`, `test_atomic_write`, `test_dedup_skip`) are migrated to pass `expected_base=store`; their GREEN state under the EQUAL guard is verified by 13-02's full-suite check. `test_refuses_outbox_dir` still asserts ValueError.
    - `git diff --quiet src/kajiba/schema.py` exits 0.
    - No test references real `~/.hermes` — store dirs are all under `tmp_path`; the default-base test monkeypatches `experiment_store.EXPERIMENTS_DIR` to a `tmp_path` subdir.
  </acceptance_criteria>
  <done>Four RED update_experiment tests added using the EQUAL shape (accept store_dir==expected_base; reject store_dir!=expected_base; production-shape default-base accept+reject via monkeypatched EXPERIMENTS_DIR); the 3 pre-existing log_experiment tests migrated to pass expected_base=store (green post-13-02); test_refuses_outbox_dir preserved; schema untouched.</done>
</task>

<task type="auto">
  <name>Task 2: New tests/test_experiment_drift.py — pure compute_drift unit tests (EREV-03)</name>
  <files>tests/test_experiment_drift.py</files>
  <read_first>
    - tests/test_eval_scorer.py:1-45 (the pure-compute-unit-test structure analog — RED-import header note, builder loads, direct `compute_*` assertions)
    - tests/test_experiment_store.py:27-53 (`_make_record` builder pattern — copy or import it to build multi-run records in-test)
    - src/kajiba/schema.py:408-429 (ExperimentMetadata.local_model/task_category, ExperimentOutcome.eval_score/drift_flag — the fields compute_drift groups and reads)
    - .planning/phases/13-reviewer-critique-drift/13-RESEARCH.md (Pattern 3 — leave-one-out prior-mean baseline, both-directions, <2-run never-flag, idempotent verdict-over-all-records)
    - .planning/phases/13-reviewer-critique-drift/13-VALIDATION.md (EREV-03 row + `--threshold`/leave-one-out edge cases)
  </read_first>
  <action>
    Create `tests/test_experiment_drift.py` with a module docstring noting it is
    RED until `kajiba.experiment_drift` exists (13-03). Import
    `from kajiba.experiment_drift import compute_drift, DRIFT_THRESHOLD` at module
    top (this makes the whole file RED via ImportError — acceptable; or import
    inside each test for granular RED). Build `ExperimentRecord` lists via a local
    `_make_record`-style builder that varies
    `experiment.local_model.model_name`, `experiment.task_category`, and
    `outcome.eval_score` (and a distinct `experiment_id`/`started_at` per run so
    `record_id` differs).

    Cover these behaviors as separate tests:
    - `test_flags_regression`: a group of 3 runs in same (model_name, task_category)
      where one run's eval_score deviates from the leave-one-out mean of the
      others by MORE than `DRIFT_THRESHOLD` (0.15) downward → verdict[id] is True
      for that run, False for the consistent ones.
    - `test_flags_improvement`: same but the outlier deviates UPWARD beyond
      threshold → still flagged (both directions, D-14).
    - `test_under_threshold_not_flagged`: all runs within threshold → no flags.
    - `test_group_of_one_never_flagged`: a group with a single run → verdict is
      False, and the call does NOT raise (no `mean([])` crash, <2-run guard).
    - `test_threshold_override`: same data, a tighter `threshold=0.05` flags runs
      the default 0.15 would not; assert the difference.
    - `test_verdict_covers_all_records`: the returned dict has an entry for EVERY
      input record_id (so callers can SET and CLEAR idempotently, D-15).
    - `test_groups_isolated`: two different (model_name, task_category) groups are
      computed independently — a deviation in one group does not flag the other.

    Use the documented leave-one-out baseline (each run compared to the mean of
    the OTHER runs in its group). Assert against `record_id` keys. Pure function —
    NO store, NO CLI, NO file I/O. NO fenced code in this plan file.
  </action>
  <verify>
    <automated>python -m pytest tests/test_experiment_drift.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_experiment_drift.py -q` FAILS with ImportError on `kajiba.experiment_drift` (RED for the right reason).
    - The file collects ≥7 tests (one per behavior above) — confirm via `python -m pytest tests/test_experiment_drift.py --collect-only -q` showing the test ids even though import fails OR (if module-level import) the collection error names `kajiba.experiment_drift` specifically.
    - No real `~/.hermes` access; no file I/O in the tests.
    - `git diff --quiet src/kajiba/schema.py` exits 0.
  </acceptance_criteria>
  <done>New drift unit-test file added, RED on missing `kajiba.experiment_drift`, covering both directions / <2-run / threshold / all-records-verdict / group isolation.</done>
</task>

<task type="auto">
  <name>Task 3: Extend tests/test_cli_experiment.py — review/lessons/drift integration + WR-01/02/03 error paths (EREV-01, EREV-02, WR fixes); isolate experiment_store.EXPERIMENTS_DIR too</name>
  <files>tests/test_cli_experiment.py</files>
  <read_first>
    - tests/test_cli_experiment.py (the file being extended — `_isolate_store` at :28-33 currently patches only `kajiba.cli.EXPERIMENTS_DIR` + `kajiba.cli.KAJIBA_BASE`; mirror `test_log_from_file` :36 and `test_log_scalar_overrides` :54 shapes; `runner` fixture :23-25)
    - src/kajiba/cli.py:82-128 (`_load_experiment` — load-by-id the new commands reuse) and :854-1041 (`experiment` group, `experiment_log` input branches incl. WR sites :917/:921/:935/:939-943, `experiment_list`); cli.py:983 `log_experiment(rec, EXPERIMENTS_DIR)` is the production call (no expected_base — store falls back to its own EXPERIMENTS_DIR default)
    - src/kajiba/schema.py:117-118 (`RECOMMENDED_ACTIONS` — the `--action` Choice vocab) and :408-429 (the outcome/metadata fields the commands write)
    - .planning/phases/13-reviewer-critique-drift/13-RESEARCH.md (Patterns 4/5/6 — critique input modes, lesson parse rule, reviewer identity/verdict; WR-01..04 Fix Locations table; Open Question 2 resolution — `drift --id` writes/clears the WHOLE group)
    - .planning/phases/13-reviewer-critique-drift/13-VALIDATION.md (EREV-01/EREV-02/WR rows + the `-k` automated commands)
  </read_first>
  <action>
    EXTEND `_isolate_store(tmp_path, monkeypatch)` so it ALSO isolates the store
    module's guard-default base: in addition to the existing
    `monkeypatch.setattr("kajiba.cli.EXPERIMENTS_DIR", store)` and
    `monkeypatch.setattr("kajiba.cli.KAJIBA_BASE", tmp_path)`, add
    `monkeypatch.setattr("kajiba.experiment_store.EXPERIMENTS_DIR", store)`. This is
    REQUIRED: the CLI calls `log_experiment(rec, EXPERIMENTS_DIR)` /
    `update_experiment(rec, EXPERIMENTS_DIR)` with NO `expected_base`, so the store's
    EQUAL guard falls back to `experiment_store.EXPERIMENTS_DIR` (read at call time).
    Without this line every CLI write would compare the isolated `tmp_path/"experiments"`
    store_dir against the REAL `~/.hermes/.../experiments` default base → ValueError →
    every CLI test (existing AND new) breaks. With it, store_dir == default base →
    accepted, and tests never touch real `~/.hermes`. Keep the function's return value
    (`store`) unchanged so existing call sites are unaffected.

    First, log a known experiment into the isolated store at the start of each
    test (invoke `["experiment", "log", "--from", str(FIXTURE)]` like the existing
    tests, then read its `record_id` from the written `exp_*.json`) so review/
    lessons/drift have a real target.

    REVIEW (EREV-01) tests:
    - `test_review_sets_critique`: `["experiment", "review", <id>, "--critique", "Good but slow"]`
      → exit 0; on-disk `outcome.reviewer_critique == "Good but slow"`.
    - `test_review_replaces_critique` (D-07): review twice with different
      `--critique`; assert the SECOND value persisted (single string, no append).
    - `test_review_reviewer_model_set_and_omitted` (D-05): with
      `--reviewer-model gpt-4o` → `experiment.reviewer_model.model_name == "gpt-4o"`;
      a separate run WITHOUT the flag leaves `reviewer_model` null.
    - `test_review_action_validated` (D-06): `--action use_as_is` →
      `outcome.recommended_action == "use_as_is"`; `--action bogus` → non-zero exit
      (Click Choice rejection), no traceback.
    - `test_review_from_txt` and `test_review_from_json` (D-04): write a `.txt`
      file (raw critique) and a `.json` fragment (`{"reviewer_critique": "..."}`)
      under `tmp_path`, pass via `--from`; assert each persists the critique.
    - `test_review_interactive_paste` (D-04): invoke with NO critique flags and
      `input="line one\nline two\n"`; assert the multi-line critique persisted.

    LESSONS (EREV-02) tests:
    - `test_lessons_add_with_category`: `["experiment", "lessons", <id>, "--add", "needs explicit output format", "--category", "prompting"]`
      → on-disk `outcome.lessons_learned` contains `"prompting: needs explicit output format"`.
    - `test_lessons_read_mode`: with NO `--add`, `["experiment", "lessons", <id>]`
      prints the record's lessons (assert the lesson text appears in output).
    - `test_lessons_filter_by_category`: add lessons in two categories, then
      `lessons <id> --category prompting` prints only the prompting lesson.
    - `test_lessons_crossrecord` (D-11): log two records, add a "prompting" lesson
      to each, then `["experiment", "lessons", "--category", "prompting"]` with NO
      id prints lessons from BOTH records (assert both record_ids in output).
    - `test_parse_lesson_*` (D-08/D-10): direct unit tests on the `_parse_lesson`
      helper (import from `kajiba.cli`): first-`:` split, lowercase category,
      `uncategorized` fallback for a colon-less lesson, colons in the lesson TEXT
      preserved (e.g. `"http: see https://x"` → category `http`, text keeps the
      `https://` colon).

    DRIFT idempotent (EREV-03 CLI half) tests:
    - `test_drift_persists_and_clears` (D-15): log ≥2 runs same (model,task) with
      one drifting score → `["experiment", "drift"]` sets `drift_flag=true` on the
      outlier on disk; then log/normalize so it no longer drifts and re-run
      `drift` → assert the flag CLEARED (idempotent set-AND-clear). (May be tagged
      `-k drift_idempotent`.)
    - `test_drift_id_writes_whole_group` (D-15, Open Question 2 — LOCKED:
      `--id` writes/clears the WHOLE group, consistent verdict): log ≥3 runs in the
      SAME (model_name, task_category) group, with one clear outlier; run
      `["experiment", "drift", "--id", <any_id_in_the_group>]`. Assert that ALL
      records in that group have their `drift_flag` written to the
      group-consistent verdict on disk (the outlier → true, the consistent ones →
      false), NOT only the `--id` record. Then normalize the outlier and re-run
      `drift --id <same_id>`; assert EVERY record in the group is now false
      (whole-group clear). Records in OTHER groups are untouched. This pins the
      whole-group write semantics so 13-05's executor cannot silently scope the
      write to the single `--id` record. (Tag e.g. `-k drift_id_group`.)

    WR error-path tests:
    - `test_partial_flags_error` (WR-01): `experiment log --score 0.5` (no `--from`,
      missing `--type`/`--task-category`) → non-zero exit with a message naming the
      missing flags; does NOT silently drop into interactive prompts.
    - `test_missing_record_kind_friendly` (WR-02): `--from` a JSON file that is a
      valid experiment fragment EXCEPT it omits `record_kind` → friendly
      ClickException message (no raw `ValidationError` traceback in output). Add a
      SECOND assertion: `--from` a fragment that ALSO omits other required
      experiment fields → STILL a friendly ClickException (no raw `ValidationError`
      leak), pinning the WR-02 "wrap in try/except ValidationError" requirement
      (default-inject record_kind ALONE is insufficient when other fields are
      missing — see 13-04 Task 2).
    - `test_malformed_json_friendly` (WR-03): `--from` a file with invalid JSON →
      message contains "Malformed JSON" (no `JSONDecodeError` traceback).

    All RED now (commands/helpers/fixes do not exist yet). NO fenced code in this
    plan file; write Python in the test file.
  </action>
  <verify>
    <automated>python -m pytest tests/test_cli_experiment.py -k "review or lessons or parse_lesson or drift or partial_flags or missing_record_kind or malformed_json" -q</automated>
  </verify>
  <acceptance_criteria>
    - The selected tests FAIL for the right reason: missing subcommands (Click "No such command 'review'/'lessons'/'drift'") or missing `_parse_lesson` import / unimplemented WR fixes — NOT collection/syntax errors.
    - `_isolate_store` now also monkeypatches `kajiba.experiment_store.EXPERIMENTS_DIR` to the same `tmp_path/"experiments"` store, so CLI writes (which pass NO `expected_base`) pass the store's EQUAL guard by hitting the isolated default base.
    - `test_drift_id_writes_whole_group` is present and asserts whole-group write AND whole-group clear (the locked Open Question 2 semantics), so 13-05 cannot diverge to single-record scope.
    - `test_missing_record_kind_friendly` asserts a friendly error for BOTH a record_kind-only-missing fragment AND a fragment missing other required fields (pins WR-02 try/except ValidationError).
    - Pre-existing CLI tests (`test_log_from_file`, `test_log_scalar_overrides`, list tests, Phase 12 score/scrub tests) still PASS: `python -m pytest tests/test_cli_experiment.py -q` shows only the new tests red.
    - Every test uses `_isolate_store`; grep confirms no real `~/.hermes` path literal in the added tests.
    - `git diff --quiet src/kajiba/schema.py` exits 0.
  </acceptance_criteria>
  <done>CLI integration + drift whole-group + WR error-path + `_parse_lesson` unit tests added, all RED on missing impl; `_isolate_store` extended to isolate experiment_store.EXPERIMENTS_DIR so CLI writes pass the EQUAL guard's default base; existing CLI tests unaffected; schema untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → real filesystem | Tests must NEVER write to the real `~/.hermes`; all store dirs are `tmp_path`-scoped via `_isolate_store` (which patches BOTH `kajiba.cli.EXPERIMENTS_DIR` and `kajiba.experiment_store.EXPERIMENTS_DIR`) / direct `store_dir`+`expected_base` args |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-CR01 | Tampering/DoS (data loss) | `update_experiment` overwrite tests | mitigate | Task 1 asserts corrected score persists + exactly one file + identity-stable using the EQUAL guard shape (accept store_dir==expected_base), locking the CR-01 fix contract before impl |
| T-13-DRIFT | Information disclosure (false verdict) | `compute_drift` unit + drift CLI tests | mitigate | Task 2 asserts both-directions + <2-run-never-flag + all-records verdict; Task 3 pins whole-group `--id` write/clear (Open Question 2 locked), locking idempotent-clear contract |
| T-13-ERR | Tampering (untrusted input) | WR-01/02/03/04 tests | mitigate | Tasks 1+3 assert friendly errors (no traceback) for malformed JSON / missing record_kind (incl. other missing fields) / partial flags / out-of-base store dir (EQUAL guard: reject store_dir != expected_base, incl. the production default-base path) |
| T-13-ENV | Tampering (test pollution) | store isolation | mitigate | All tests scoped to `tmp_path`; `_isolate_store` patches both EXPERIMENTS_DIR symbols; default-base unit test monkeypatches `experiment_store.EXPERIMENTS_DIR`; no real `~/.hermes` literal (grep-verified in acceptance) |
</threat_model>

<verification>
- RED suite for the whole phase exists: `python -m pytest tests/test_experiment_store.py tests/test_cli_experiment.py tests/test_experiment_drift.py -q` shows the NEW Phase-13 tests failing for the right reason (missing impl) while ALL pre-existing tests stay green (the 3 migrated `log_experiment` tests go green once 13-02 adds the `expected_base` param).
- Schema frozen: `git diff --quiet src/kajiba/schema.py` exits 0.
- No test touches real `~/.hermes`.
</verification>

<success_criteria>
- `tests/test_experiment_drift.py` exists (new) with ≥7 RED compute_drift tests.
- `tests/test_experiment_store.py` has 4 new RED tests (update/identity/EQUAL-guard/default-base), all using the EQUAL shape (accept store_dir==expected_base, reject store_dir!=expected_base), jointly satisfiable by 13-02's predicate; the 3 pre-existing `log_experiment` tests migrated to pass `expected_base=store`.
- `tests/test_cli_experiment.py` has the review/lessons/drift (incl. whole-group `--id`)/WR + `_parse_lesson` RED tests; `_isolate_store` also patches `experiment_store.EXPERIMENTS_DIR`.
- All new tests RED on MISSING IMPLEMENTATION (not syntax/collection); all existing tests still pass (migrated store tests green post-13-02).
- `git diff --quiet src/kajiba/schema.py` exits 0.
</success_criteria>

<output>
Create `.planning/phases/13-reviewer-critique-drift/13-01-SUMMARY.md` when done.
</output>
</content>
