---
phase: 13-reviewer-critique-drift
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/kajiba/experiment_store.py
autonomous: true
requirements: [EREV-01, EREV-02, EREV-03]
must_haves:
  truths:
    - "An outcome-mutating write to an existing experiment overwrites exp_<id>.json in place with no data loss (CR-01 closed)"
    - "record_id and the on-disk filename stay byte-stable across any outcome/metadata mutation (D-01)"
    - "The store guard rejects a same-named experiments directory outside the canonical base (WR-04)"
    - "schema.py is provably untouched"
  artifacts:
    - path: "src/kajiba/experiment_store.py"
      provides: "update_experiment() in-place overwrite write path + tightened D-13 guard"
      contains: "def update_experiment"
  key_links:
    - from: "src/kajiba/experiment_store.py"
      to: "ExperimentRecord.model_validate"
      via: "re-validate after mutation before write"
      pattern: "model_validate"
    - from: "src/kajiba/experiment_store.py"
      to: "os.replace"
      via: "atomic temp-file write (no dest.exists() skip)"
      pattern: "os\\.replace"
---

<objective>
Add `update_experiment(record, store_dir) -> Path` to `experiment_store.py`: the
single in-place overwrite write path that all three Phase 13 commands funnel
through (D-03). This closes CR-01 (the `log_experiment` dedup-skip data-loss bug)
by giving outcome-mutating writes a path that intentionally OVERWRITES rather
than early-returning on `dest.exists()`. Also sweep WR-04: tighten the D-13
structural guard from a leaf-name check to a canonical-base check, applied to
BOTH `log_experiment` and the new `update_experiment`.

Purpose: First-class, non-lossy "correct a logged eval" — the structural fix for
CR-01, not a bolt-on `--force` flag. This is the foundation the CLI commands
(13-04/13-05) write through.
Output: `update_experiment()` (new), tightened guard in `log_experiment` and
`update_experiment`. Turns the Task-1 RED store tests from 13-01 GREEN. No schema
change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-reviewer-critique-drift/13-CONTEXT.md
@.planning/phases/13-reviewer-critique-drift/13-RESEARCH.md
@.planning/phases/13-reviewer-critique-drift/13-PATTERNS.md
@src/kajiba/experiment_store.py
</context>

<artifacts_this_phase_produces>
- NEW `update_experiment(record: ExperimentRecord, store_dir: Path) -> Path` in
  `src/kajiba/experiment_store.py` — in-place overwrite write path (D-03).
- MODIFIED store guard (WR-04): tightened canonical-base check applied to both
  `log_experiment` and `update_experiment`.
- (Re-export of `update_experiment` from `__init__.py` is owned by 13-04.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add update_experiment() in-place overwrite write path (CR-01, D-03) + tighten D-13 guard (WR-04)</name>
  <files>src/kajiba/experiment_store.py</files>
  <read_first>
    - src/kajiba/experiment_store.py:49-105 (`log_experiment` — the EXACT atomic write to mirror: identity compute :81-83, atomic mkstemp+os.replace+BaseException cleanup :90-105; the `dest.exists()` dedup-skip :86-88 is the CR-01 bug to OMIT; the leaf-name guard :72-76 is the WR-04 defect to TIGHTEN)
    - src/kajiba/schema.py:445-492 (`compute_record_id`/`compute_submission_hash` — prove identity excludes outcome, why in-place mutation keeps the filename stable; cite in the docstring)
    - .planning/phases/13-reviewer-critique-drift/13-RESEARCH.md (Pattern 2 — `update_experiment` full shape; WR-01..04 Fix Locations table for the WR-04 guard predicate; Pitfall 3 re-validate-after-mutation; Open Question 1 / A5 — keep `log_experiment` first-log-only, CR-01 closed via the funnel)
    - .planning/phases/13-reviewer-critique-drift/13-PATTERNS.md (§ experiment_store.py — KEY DIFFERENCES from analog: delete dedup-skip, add re-validate, tighten guard)
    - tests/test_experiment_store.py (the RED tests from 13-01 this task turns GREEN: `test_update_overwrites_in_place`, `test_identity_stable_across_mutation`, `test_update_guard_rejects_dir_outside_base`)
  </read_first>
  <behavior>
    - Logging then updating the same identity with a corrected `eval_score` leaves exactly one `exp_<id>.json` whose on-disk score is the corrected value (CR-01).
    - `record_id` and the returned `Path.name` are byte-identical before and after mutating outcome/metadata fields (D-01).
    - Mutating then calling `update_experiment` re-validates: an out-of-range value (if forced into the model dict) is rejected by Pydantic, not persisted.
    - The tightened guard raises `ValueError` for a directory named `experiments` that is NOT the canonical store, and still accepts the canonical store dir.
  </behavior>
  <action>
    Implement `update_experiment(record: ExperimentRecord, store_dir: Path) -> Path`
    by mirroring `log_experiment` (experiment_store.py:49-105) MINUS the
    `dest.exists()` early-return (experiment_store.py:86-88) — it ALWAYS overwrites.
    Order: (1) tightened guard on `store_dir.resolve()`; (2)
    `store_dir.mkdir(parents=True, exist_ok=True)`; (3) re-validate after mutation
    — `record = ExperimentRecord.model_validate(record.model_dump(mode="json", by_alias=True))`
    (models lack `validate_assignment`, so re-validation is the project rule, Pitfall 3);
    (4) `record.compute_record_id()` + `record.compute_submission_hash()`;
    (5) `dest = store_dir / f"exp_{record.record_id}.json"`; (6) atomic write
    copied verbatim from log_experiment :90-105 (`json.dumps(..., ensure_ascii=False, indent=2) + "\n"`,
    `tempfile.mkstemp(dir=store_dir, suffix=".tmp")`, `os.fdopen` write,
    `os.replace`, `except BaseException: Path(tmp_name).unlink(missing_ok=True); raise`);
    (7) `logger.info("Experiment updated in place: %s", dest)`; return `dest`.

    Google-style docstring (Args/Returns/Raises) mirroring log_experiment:50-69;
    state WHY in-place is safe (identity excludes outcome, schema.py:445-467) and
    that it closes CR-01.

    WR-04 guard tightening — define a single tightened predicate and apply to BOTH
    `log_experiment` (replace its :72-76 leaf-name check) and `update_experiment`.
    Since this module is deliberately Click-free and does NOT import `KAJIBA_BASE`
    / `EXPERIMENTS_DIR` from cli.py (per the module docstring), keep the guard
    self-contained: compute the canonical experiments base from the user home
    inside the store module (`Path.home() / ".hermes" / "kajiba" / "experiments"`)
    and require `store_dir.resolve() == <that>.resolve()` — OR, to preserve
    test-isolation where tests pass `tmp_path/"experiments"`, require
    `resolved.name == "experiments" AND resolved.parent.name == "kajiba"`
    (so a same-named dir not nested under a `kajiba` parent is rejected). Pick
    ONE predicate that (a) accepts both the real store and the test's
    `tmp_path/"experiments"` shape used by `_make_record`-based store tests, and
    (b) rejects the 13-01 `tmp_path/"elsewhere"/"experiments"` guard test —
    reconcile with the exact dir shapes those two tests construct in
    tests/test_experiment_store.py and document the chosen predicate in the
    docstring/comment. Do NOT weaken the existing leaf-name acceptance for the
    real store.

    Keep `log_experiment`'s dedup-skip as-is for IDENTICAL re-logs (first-log
    path stays correct; CR-01 is closed by routing corrections through
    `update_experiment`, per D-03 / Open Question 1 / A5). Do NOT touch schema.py.
    NO fenced code blocks in this plan; implement in the source file.
  </action>
  <verify>
    <automated>python -m pytest tests/test_experiment_store.py -q && git diff --quiet src/kajiba/schema.py</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_experiment_store.py -q` is fully GREEN, including the three 13-01 tests (`test_update_overwrites_in_place`, `test_identity_stable_across_mutation`, `test_update_guard_rejects_dir_outside_base`) and all pre-existing store tests.
    - Source assertion: `update_experiment` contains NO `dest.exists()` early-return; it calls `os.replace` and `ExperimentRecord.model_validate` before writing.
    - The tightened guard is applied to BOTH `log_experiment` and `update_experiment` (grep both functions for the new predicate).
    - `git diff --quiet src/kajiba/schema.py` exits 0.
    - Full suite has zero regressions: `python -m pytest -q` (only the not-yet-implemented 13-01 drift/CLI tests remain red).
  </acceptance_criteria>
  <done>`update_experiment()` overwrites in place with no data loss, keeps identity byte-stable, re-validates before write, and shares a tightened guard with `log_experiment`; 13-01 store tests GREEN; schema untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → store write path | An in-memory mutated record crosses into durable storage; must re-validate and write atomically |
| store_dir argument → filesystem | The destination directory is the privacy boundary between the private experiment store and the community staging/outbox namespaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-CR01 | Tampering / DoS (data loss) | `update_experiment` overwrite path | mitigate | No `dest.exists()` skip; atomic temp-file + `os.replace` so a bad mutation can never half-write or corrupt a good record; re-validate before write |
| T-13-ERR | Tampering (path / out-of-base store) | tightened D-13 guard (WR-04) | mitigate | Guard rejects an `experiments` dir outside the canonical base, applied to both write functions; prevents an experiment leaking into community paths |
| T-13-INT | Tampering (invalid persisted data) | re-validate after mutation | mitigate | `ExperimentRecord.model_validate(model_dump(...))` before write enforces eval_score range and action vocab (Pitfall 3) |
| T-13-SCH | Tampering (schema drift) | schema freeze | mitigate | `git diff --quiet src/kajiba/schema.py` in verify; no field added (uses existing optional outcome/metadata fields) |
</threat_model>

<verification>
- `python -m pytest tests/test_experiment_store.py -q` GREEN (13-01 store tests + pre-existing).
- `python -m pytest -q` zero regressions vs the 289-passed baseline (drift/CLI tests still red until 13-03/04/05).
- `git diff --quiet src/kajiba/schema.py` exits 0.
- No `dest.exists()` early-return in `update_experiment`; tightened guard in both write functions.
</verification>

<success_criteria>
- `update_experiment()` exists, overwrites in place, re-validates, keeps identity byte-stable, and never short-circuits on `dest.exists()`.
- WR-04 guard tightened in both `log_experiment` and `update_experiment`.
- 13-01 store tests GREEN; full suite zero regressions; schema untouched.
</success_criteria>

<output>
Create `.planning/phases/13-reviewer-critique-drift/13-02-SUMMARY.md` when done.
</output>
