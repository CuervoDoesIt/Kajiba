---
phase: 13-reviewer-critique-drift
plan: 05
type: execute
wave: 4
depends_on: ["13-01", "13-03", "13-04"]
files_modified:
  - src/kajiba/cli.py
autonomous: true
requirements: [EREV-03, EREV-02]
must_haves:
  truths:
    - "`kajiba experiment drift` computes drift over the store and persists drift_flag on affected records (EREV-03)"
    - "Re-running drift CLEARS drift_flag on records that no longer qualify (idempotent set-AND-clear, D-15)"
    - "`--threshold` overrides the default DRIFT_THRESHOLD; `--id` scopes the scan to one record's group AND writes/clears the WHOLE group's flags (locked Open Question 2, D-14/D-15)"
    - "`experiment list` surfaces a lessons count and a drift flag per record without crashing on a bad file (EREV-02 surface, Pitfall 6)"
    - "schema.py is provably untouched and the full suite is green"
  artifacts:
    - path: "src/kajiba/cli.py"
      provides: "experiment drift command + enriched experiment list (lessons count + drift column); cli.py compute_drift import"
      contains: "def experiment_drift"
  key_links:
    - from: "src/kajiba/cli.py"
      to: "kajiba.experiment_drift.compute_drift"
      via: "drift command computes the verdict (cli.py import owned here, first use)"
      pattern: "compute_drift"
    - from: "src/kajiba/cli.py"
      to: "kajiba.experiment_store.update_experiment"
      via: "_mutate_experiment writes/clears drift_flag for changed records"
      pattern: "_mutate_experiment"
---

<objective>
Add the `kajiba experiment drift` subcommand (EREV-03) and enrich `kajiba
experiment list` with a lessons count and a drift-flag column (EREV-02 surface).
The drift command scans the store, groups runs via `compute_drift` (13-03),
persists `drift_flag` via the `_mutate_experiment`/`update_experiment` funnel on
records whose flag changes, and is idempotent — a re-run SETS and CLEARS flags so
the store always reflects current reality (D-15). Then runs the phase gate.

This plan also OWNS the `cli.py` import of `compute_drift` (`from
kajiba.experiment_drift import compute_drift`), since the drift command is its
first use — 13-04 only added the `__init__.py` re-export, deliberately leaving the
cli.py import here to avoid an unused-import.

Purpose: Close the longitudinal-quality requirement and make lessons/drift
visible at a glance. This is the last wave touching `cli.py`, so it also carries
the phase regression + schema-frozen gate.
Output: `experiment drift` command + enriched `experiment list` + cli.py
`compute_drift` import. Turns the remaining 13-01 RED tests (drift CLI idempotent,
drift `--id` whole-group, list enrichment) GREEN. Full suite green;
`git diff --quiet src/kajiba/schema.py` exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-reviewer-critique-drift/13-CONTEXT.md
@.planning/phases/13-reviewer-critique-drift/13-RESEARCH.md
@.planning/phases/13-reviewer-critique-drift/13-PATTERNS.md
@.planning/phases/13-reviewer-critique-drift/13-VALIDATION.md
@src/kajiba/cli.py
@.planning/phases/13-reviewer-critique-drift/13-03-SUMMARY.md
@.planning/phases/13-reviewer-critique-drift/13-04-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- NEW `from kajiba.experiment_drift import compute_drift` import in `cli.py`
  (owned here — first use; 13-04 added only the `__init__.py` re-export).
- NEW `kajiba experiment drift` command — flags `--id` (optional, scope scan to
  one record's group AND write/clear the whole group's flags), `--threshold`
  (optional float, default `DRIFT_THRESHOLD`).
- MODIFIED `experiment list` — two new columns: "Lessons" (count) and "Drift"
  (⚠ when flagged), read from the raw dict, per-file guarded.
- Phase gate: full suite green + `git diff --quiet src/kajiba/schema.py`.
</artifacts_this_phase_produces>

<locked_decisions>
- Open Question 2 (13-RESEARCH `## Open Questions` #2) is RESOLVED for this phase:
  **`drift --id <x>` computes over and writes/clears the flags of the WHOLE group**
  that `<x>` belongs to (consistent verdict across the group), NOT just the single
  `--id` record. The baseline requires the whole group regardless; persisting only
  the group means the displayed/persisted verdict is always self-consistent. This
  is the locked semantics 13-01's `test_drift_id_writes_whole_group` asserts —
  implement to that test, do not narrow the write scope to one record.
</locked_decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: experiment drift command — store scan, persist + idempotent clear, whole-group --id (EREV-03, D-12..D-15)</name>
  <files>src/kajiba/cli.py</files>
  <read_first>
    - src/kajiba/cli.py:7-60 (imports — ADD `from kajiba.experiment_drift import compute_drift` here; this cli.py import is owned by THIS plan, first use; 13-04 added only the `__init__.py` re-export) and :854-989 (`experiment` group + option idiom incl. `--score` `click.FloatRange` at :867-873 to mirror for `--threshold`) and :1044-1081 (`experiment_score` `@click.argument` + Rich Table/Panel rendering to mirror for the drift summary) and :1015-1041 (`experiment_list` per-file-guarded glob loop)
    - src/kajiba/cli.py:82-128 (`_load_experiment`) and the 13-04 helper `_mutate_experiment` (the write funnel for drift_flag)
    - src/kajiba/experiment_drift.py (the 13-03 `compute_drift(records, threshold) -> dict[str, bool]` + `DRIFT_THRESHOLD` this command calls)
    - .planning/phases/13-reviewer-critique-drift/13-RESEARCH.md (Pattern 3 idempotent set-AND-clear; Open Question 2 — LOCKED: `--id` computes over the whole group and writes/clears flags for ALL records in that group; D-15 store-wide scan)
    - tests/test_cli_experiment.py (the 13-01 `test_drift_persists_and_clears` AND `test_drift_id_writes_whole_group` RED tests this turns GREEN)
  </read_first>
  <behavior>
    - With ≥2 same-(model,task) runs and one drifting score, `experiment drift` sets `drift_flag=true` on the outlier on disk; consistent runs stay/become false.
    - Re-running after the group normalizes CLEARS the previously-set flag (idempotent set-AND-clear, D-15 / Pitfall 4).
    - Only records whose on-disk flag DIFFERS from the verdict are rewritten (no needless disk churn); the Rich summary reports what changed.
    - `--threshold 0.05` flags runs the default 0.15 would not.
    - `--id <id>` computes over that record's WHOLE group (baseline needs the group) and writes/clears flags for ALL records in that group (consistent verdict, locked Open Question 2), not just the `--id` record; records in OTHER groups are untouched.
  </behavior>
  <action>
    First, add `from kajiba.experiment_drift import compute_drift` to the cli.py
    import block (:7-60). This import is OWNED by this plan (its first use is the
    drift command below); 13-04 intentionally left it out to avoid an
    unused-import.

    Add `@experiment.command("drift")` with `@click.option("--id", "record_id",
    default=None, ...)` and `@click.option("--threshold", type=float,
    default=None, help="Override the default DRIFT_THRESHOLD (0.15); both
    directions, D-14.")`. Body: `_ensure_dirs()`; load all experiment records by
    globbing `EXPERIMENTS_DIR.glob("exp_*.json")` with the per-file
    `try/except ... continue` guard (mirror experiment_list :1015-1020), using
    `load_record` + `isinstance(rec, ExperimentRecord)` to collect a
    `list[ExperimentRecord]`. Resolve the effective threshold:
    `threshold if threshold is not None else DRIFT_THRESHOLD`.

    If `--id` given (LOCKED whole-group semantics, Open Question 2): load that
    record (reuse `_load_experiment`), determine its group key
    `(local_model.model_name, task_category)`, and restrict the record set to the
    records sharing that group key. Pass the WHOLE group to `compute_drift` (so the
    baseline is correct) AND persist the verdict for EVERY record in that group
    (write/clear all of them, not just the `--id` record). Otherwise scan the whole
    store.

    Compute `verdict = compute_drift(records, threshold)` (13-03). For every record
    in the scope (whole store, or the whole group when `--id` is given) whose
    CURRENT on-disk `outcome.drift_flag` differs from `verdict[record_id]`, apply
    the change through the single write path: build a mutation closure that sets
    `rec.outcome.drift_flag = verdict[rec.record_id]` and call
    `_mutate_experiment(rec.record_id, fn)` (D-03 — set AND clear, D-15). Records
    already matching the verdict are NOT rewritten.

    Render a Rich summary Table (mirror experiment_score's Table at :1063-1071):
    columns for record_id, model_name, task_category, eval_score, and the
    new/old drift state (e.g. "flagged"/"cleared"/"unchanged"); print a closing
    Panel noting how many records changed. All output via Rich (never `print()`).
    Use `Optional[X]`, double quotes, Google-style docstring. Do NOT touch
    schema.py. NO fenced code in this plan.
  </action>
  <verify>
    <automated>python -m pytest tests/test_cli_experiment.py -k "drift" tests/test_experiment_drift.py -q && git diff --quiet src/kajiba/schema.py</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_cli_experiment.py -k "drift" -q` GREEN, including `test_drift_persists_and_clears` (sets then idempotently clears) AND `test_drift_id_writes_whole_group` (`--id` writes/clears the WHOLE group, leaves other groups untouched).
    - `python -m pytest tests/test_experiment_drift.py -q` still GREEN (unit layer unaffected).
    - CLI: `kajiba experiment drift --help` lists `--id` and `--threshold`.
    - Source assertions: cli.py now imports `compute_drift` from `kajiba.experiment_drift`; the drift command calls `compute_drift` and routes writes through `_mutate_experiment`/`update_experiment` (no direct file open); `--id` persists the verdict for all records in the group; only changed records are rewritten.
    - `git diff --quiet src/kajiba/schema.py` exits 0.
  </acceptance_criteria>
  <done>`experiment drift` scans, persists, and idempotently clears `drift_flag` via the single write path with a Rich summary; `--id` writes/clears the whole group (locked Open Question 2); cli.py `compute_drift` import added; drift CLI + unit tests GREEN; schema untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Enrich experiment list (lessons count + drift column) + phase regression/schema gate (EREV-02 surface, Pitfall 6)</name>
  <files>src/kajiba/cli.py</files>
  <read_first>
    - src/kajiba/cli.py:992-1041 (`experiment_list` — the existing Table columns :1005-1013 and the per-file-guarded loop :1015-1039 to extend; keep the `try/except ... continue` per-file guard, Pitfall 6)
    - .planning/phases/13-reviewer-critique-drift/13-RESEARCH.md (Code Examples — "Enriching `experiment list`": read `lessons_learned`/`drift_flag` from the RAW dict, no full validation needed for display; Pitfall 6 one-bad-file-must-not-blank-the-table)
    - .planning/phases/13-reviewer-critique-drift/13-PATTERNS.md (§ cli.py `list` enrichment — add two columns after the existing five, raw-dict read, keep per-file guard)
    - .planning/phases/13-reviewer-critique-drift/13-VALIDATION.md (phase gate: full suite green + `git diff --quiet src/kajiba/schema.py`; Per-Task map regression row)
    - tests/test_cli_experiment.py (the 13-01 list-enrichment + per-file-guard tests this turns GREEN)
  </read_first>
  <action>
    Extend `experiment_list` (cli.py:992-1041): add two `table.add_column` calls
    after the existing "Confidence" column — "Lessons" and "Drift". Inside the
    existing per-file-guarded loop, read from the RAW `data` dict (no full
    validation needed for display, keep the `try/except ... continue` guard,
    Pitfall 6): `outcome = data.get("outcome", {})`,
    `lessons = outcome.get("lessons_learned", [])`,
    `drift = "⚠" if outcome.get("drift_flag") else ""`. Add `str(len(lessons))`
    and `drift` to the `table.add_row(...)` call (after the existing `band`
    argument). Do NOT add full validation to the listing path — one malformed file
    must still `continue`, not blank the table.

    Then run the PHASE GATE: full suite green and schema provably untouched. This
    is the last `cli.py`-touching task in the phase, so it owns the regression +
    schema-frozen verification for the whole phase.

    Use double quotes; keep Rich rendering. Do NOT touch schema.py. NO fenced code
    in this plan.
  </action>
  <verify>
    <automated>python -m pytest -q && git diff --quiet src/kajiba/schema.py</automated>
  </verify>
  <acceptance_criteria>
    - Full suite GREEN: `python -m pytest -q` shows all Phase 13 tests passing and the prior baseline preserved (the historical 289-passed/2-skip baseline grows by the Phase 13 tests; 0 regressions, 0 unexpected failures).
    - `git diff --quiet src/kajiba/schema.py` exits 0 (Phase 10 schema untouched; golden-ID tripwire `test_schema_backcompat.py` green within the suite).
    - CLI: `kajiba experiment list` renders "Lessons" and "Drift" columns; a store containing one malformed `exp_*.json` still lists the valid records (per-file guard — covered by the 13-01 enrichment/guard test).
    - Source assertion: the list loop reads `lessons_learned`/`drift_flag` from the raw dict and retains the per-file `try/except ... continue`.
  </acceptance_criteria>
  <done>`experiment list` shows lessons count + drift flag without crashing on a bad file; full suite green; schema provably untouched — Phase 13 ready for /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store files → drift scan & list display | Persisted records (possibly one malformed) are read for verdict + display; a bad file must not abort the scan or blank the table |
| drift command → store write path | Flag set/clear must funnel through `update_experiment` (single write path), never a direct CLI file write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-DRIFT | Information disclosure (stale verdict) | `experiment drift` | mitigate | Idempotent set-AND-clear over the full verdict (D-15); `--id` writes the whole group so a partial scope can't leave a self-inconsistent verdict (locked Open Question 2); only changed records rewritten |
| T-13-CR01 | Tampering (data loss) | drift_flag write | mitigate | Writes route through `_mutate_experiment` → `update_experiment` (atomic, re-validating, no dedup-skip) — flag change cannot corrupt other fields |
| T-13-ERR | DoS (one bad file aborts feature) | drift scan + `list` enrichment | mitigate | Per-file `try/except ... continue` guard on both the drift glob and the list loop (Pitfall 6) — a malformed file is skipped, not fatal |
| T-13-SCH | Tampering (schema drift) | phase gate | mitigate | `git diff --quiet src/kajiba/schema.py` exits 0 as a phase-gate acceptance criterion; golden-ID tripwire green in the full suite |
</threat_model>

<verification>
- `python -m pytest tests/test_cli_experiment.py -k "drift" tests/test_experiment_drift.py -q` GREEN (incl. whole-group `--id`).
- Phase gate: `python -m pytest -q` full suite GREEN (all Phase 13 tests pass, 0 regressions) AND `git diff --quiet src/kajiba/schema.py` exits 0.
- `experiment list` renders Lessons + Drift columns and survives one malformed file.
- drift_flag writes funnel through `update_experiment`; only changed records rewritten; cli.py imports `compute_drift`.
</verification>

<success_criteria>
- `experiment drift` persists and idempotently clears `drift_flag` via the single write path with a Rich summary; `--id` writes/clears the whole group (locked Open Question 2); `--threshold` honored; cli.py owns the `compute_drift` import.
- `experiment list` enriched with lessons count + drift column, per-file guarded.
- Full suite green; `git diff --quiet src/kajiba/schema.py` exits 0 — Phase 13 complete.
</success_criteria>

<output>
Create `.planning/phases/13-reviewer-critique-drift/13-05-SUMMARY.md` when done.
</output>
</content>
